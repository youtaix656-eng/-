{
  "name": "taicho-karte",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "体調カルテ — 自分の体の不調と、試した対応策とその効果を蓄積する個人用PWA（端末内保存）",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "tsc -p tsconfig.test.json && node --test .test-build/tests/*.spec.js",
    "icons": "node scripts/gen-icons.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.5.3",
    "vite": "^5.4.11"
  }
}
