{
  "name": "ouro",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Ouro — 自分専用のAI会社（AI Knowledge OS）。端末内保存・BYOK。",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/check-size.mjs",
    "preview": "vite preview",
    "test": "node --test test/*.test.mjs",
    "size": "node scripts/check-size.mjs --strict",
    "perf": "node scripts/perf.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
