# 鍼灸国家試験 対策アプリ — 開発メモ

React + Vite（JSX・TypeScript なし・外部ランタイム依存なし）。データは IndexedDB
（`src/lib/storage.js`、localStorage フォールバック）。ブラウザのみで動作。
公開先：https://youtaix656-eng.github.io/-/

## ユーザー本人の学習スケジュール（2026-09-02 時点・11月末合格実力ゴール）
ユーザーから相談されて一緒に決めた、本人の学習進捗と計画。次回以降すぐ出せるようここに記録する。
**進捗（2026-09-02時点）**：14科目中1〜11番（`examScope.js`の`SUBJECT_TAG_NAMES`の番号）を1周目消化済み。
11番 経絡経穴概論のみ残り半分。
**ゴール**：2026年11月末までに、模試（午前90問＋午後90問、本番形式）で**合格ライン60%を2回連続**
（=`Exam.jsx`の`PASS_RATE`）で超え、「合格実力あり」と言える状態にする。
**3段階（`src/data/roadmapPhases.js`のp1〜p3に対応）**：
| 期間 | 内容 | 新規:復習 |
|---|---|---|
| 9/2〜9/30 | 残り科目（経絡経穴概論後半・東洋医学臨床論・はり理論・きゅう理論）で1周目完走→前倒しで2周目開始 | 7:3→5:5 |
| 10/1〜10/31 | 弱点つぶし・2周目 | 5:5 |
| 11/1〜11/30 | 仕上げ・合格ライン超え | 3:7 |
**週別内訳（本人に一度提示済み）**：
- W1(9/2-9/8) 経絡経穴概論の残り（`学習`科目絞り＋`フラッシュカード`の経穴カードSRS併用）
- W2(9/9-9/15) 東洋医学臨床論　W3(9/16-9/22) はり理論・きゅう理論→**1周目完了**、ハーフ模試で現在地把握
- W4(9/23-9/30) 前倒し2周目開始（`復習`画面の「要注意」フィルタ・ホームの「今日集中すべき科目」・
  学習開始前の「苦手分野の◯◯にも」提案・`網羅マップ`で薄い分野確認）、週末ハーフ模試
- W5-W8(10月) 隔週ハーフ模試＋`分析`の科目バランス警告で弱点科目特定→誤答理由別の集中特訓モードで対策、
  月末に全科目2周目完了＋フル模試1回
- W9-W12(11月) 週1フル模試、メモ機能で間違いノート作成、`分析`の合格ライン診断を毎週トラッキング、
  60%を2回連続で達成できたらゴール
**毎週共通**：平日は`学習`（3分の2バッファ術の時間計画を使うと集中しやすい）、週末に模試＋`分析`振り返り＋
`週次の弱点ジャーナル`に一言。要注意バッジと苦手分野の提案にはできるだけ従う。
遅れが出た場合は10月末時点の模試結果で判断し、弱い科目に絞った集中特訓を1〜2週間挟んでから仕上げに入る。
進捗が変わったらこのセクションを更新すること（古い日付・古い進捗のまま放置しない）。

## 開発優先度メモ（2026-09-02・AIから見た「今強化すべき機能ベスト3」）
ユーザーに聞かれて答えた内容。**アプリ内にも実装済み**（`src/lib/priorityFocus.js`、Home.jsx。
消すまでずっとホーム画面に日替わりで1件提案する。ユーザー指定で期限は設けていない）。
新しくCLAUDE.mdやコードを触る時、ここと重複する提案をしない・優先度を勝手に変えない。
1. **網羅マップ・手薄科目の問題拡充**（今の一番のボトルネック。機能を増やすより先にここ）。
2. **模試の精度**（配分が出題基準と合っているか・直前期に問題プールが枯渇しないか）。
3. **復習・SRSを毎日ゼロに戻す運用**（仕組みは十分強いので、要注意バッジと
   ○ふりかえりを実際に毎日使うことが効く）。
ユーザーが実際に対応した・優先度が変わったら、`priorityFocus.js`の`PRIORITY_FOCUS_ITEMS`と
このセクションの両方を更新する（消すのはアプリ内のボタンからユーザー自身が行う想定）。

## 網羅マップ・コンテンツ拡充パイプラインの強化（2026-09-02追加）
「今強化すべき機能ベスト3」の1位（網羅マップ＋手薄科目の問題拡充パイプライン）をユーザー指定で
30案→全実装したもの。新しいファイルが増えたので、単一の正の置き場をここにまとめる。
- **`src/lib/coverage.js`**：しきい値（手薄/充実）は`settings.coverageThinThreshold`/
  `coverageRichThreshold`（全科目共通の上書き、未設定ならnull）と、出題基準の大項目数から
  自動計算する`suggestedThresholds()`（今はoutlineを持つ医療概論のみ有効）の二段構え。
  `coverageBySubject()`の`groups[].subgroups`が中項目までの内訳、`format`が原問/一問一答等/
  画像・図つきの内訳（原問の定義は`round`が設定されている問題＝pastExamTrends.jsと同じ基準）。
  `coverageSummary().fillRatio`が「手薄でも未収録でもない科目の割合」＝充足率。
- **`src/lib/coveragePriority.js`**：頻出度（daikoumokuRank）×手薄さで「埋めるべき順」を
  スコアリング（`priorityTodo`）。手薄科目の内訳テキスト化・依頼文の定型コピー・
  日替わりの手薄科目リマインド（`suggestThinSubjectReminder`）もここ。
- **`src/lib/roundGaps.js`**：科目ごとの「回の抜け漏れ」を、他科目に複数ある回がその科目だけ
  無いことから相対的に検出する（手元に無い公式基準は作らない）。
- **`src/lib/contentSeedLog.js`**：`useStore.js`が同梱データの版上げで新しい問題を追加した時、
  質問id集合の差分（バージョン管理された各科目の増分ブロックの前後比較）から科目別の追加数を
  記録する**唯一の発生源**。App.jsxの起動時トースト・CoverageMap.jsxの最終更新表示・
  ハリオ先生のお祝い（haripan.jsの`contentAdded`）・週次の弱点ジャーナルの自動追記チップは
  すべてこのログだけを参照する。**新しい科目データのバージョン増分ブロックを追加する時も、
  この仕組みは`baseQuestions`のid差分から自動検出するので、個別の計装は不要**（既存ブロックの
  構造を変えなくてよい設計）。
- **`src/lib/seedQuality.js`**：↑のログのid一覧を使い、新規追加ぶんの正答率が0%/100%に
  極端に振れていないかを検出する（新しさの判定はこのログにしか無いので、他の手段で推測しない）。
- **`src/lib/unreadablePages.js`**：標準変換プロンプト手順⑨「読み取れないリスト」をアプリ内の
  簡易CRUDメモとして持てるようにしたもの（端末内のみ）。読み取れたら削除し、削除した旨は
  従来どおり必ずユーザーへ連絡する運用は変えない。
- **`src/data/contentPipelineChecklist.js`**：標準変換プロンプトの手順を網羅マップからも
  見返せるようにした参照用データ。**この配列とCLAUDE.mdの「標準変換プロンプト」セクションの
  内容は必ず一致させる**（手順を変えたら両方直す。片方だけ直すと再びズレる）。
- **`npm run validate`（scripts/validate-content.mjs）が手薄科目・優先度ToDo・回の抜け漏れの
  実データ出力を強化済み**。CLAUDE.mdの「継続して意識する開発方針」にある「現状の手薄科目」
  リストを更新する時は、必ずこの出力から書き写す（手入力で古いまま放置しない）。
- 得意科目分析（maruPool.jsのmaruSubjectBreakdown）を網羅マップの科目詳細にも重ねて表示し、
  量（収録数）と質（○の定着率）を1画面で見られるようにした。

## 模試（Exam.jsx）の精度・使い回し対策の強化（2026-09-02追加）
「今強化すべき機能ベスト3」の2位（模試＝合否判定の測定器そのもの）をユーザー指定で
30案→全実装したもの。ユーザーの懸念は2つ：①午前/午後の科目別配分（examBlueprint.js）が
出題基準と本当に一致しているか、②直前期に同じ問題を繰り返して「覚えているから解けた」に
ならないか。新しいファイルが増えたので、単一の正の置き場をここにまとめる。
- **`src/data/examBlueprint.js`**：配分数値・総合問題のfallbackSubjectsは公式出題基準との
  突き合わせが済んでいない（手元に確認手段が無い）ことをファイル冒頭のコメントで明記した。
  変更する時は`test/examBlueprint.test.mjs`（合計問題数の整合性チェック）も一緒に直す。
- **`src/lib/examUsageLog.js`**：模試の出題id履歴（直近20回、IndexedDB）を記録する唯一の
  発生源。`recentlyUsedIds(log, mode, {withinCount})`が「直近◯回で使ったid」を返し、
  `overlapWithLast(log, mode, currentIds)`が直前の同モードとの重複率(%)を返す。
- **`src/lib/examBuilder.js`**：`buildBlueprintExam`/`pickFromSlot`に`avoidIds`（直近使った
  問題）・`srs`（通常学習で解いたことがある問題を未出題の中で優先＝定着確認としての意味づけ）を
  追加。`preferUnused(pool, avoidIds)`は得意/苦手/選択式モードでも同じ考え方を使うための
  共通関数。`blueprintAvailability`は`roundsPossible`（収録数から理論上あと何回ぶん
  ユニークな模試を組めるか）も返す——**これが「直前期に問題プールが枯渇しないか」を
  数値で見える化する仕組み**（1位の網羅マップ・優先度ToDoと直結）。
- **`src/lib/examScoreContribution.js`**：`scoreContribution(perSubject, blueprint)`は
  出題数の重み×失点率で「伸ばすと全体スコアに効く科目」を出す。**配点そのものは公式に未確認
  のため「配点」ではなく「出題数の重み」と呼び、断定しない**（CLAUDE.mdの他アプリと同じ
  手元に無い基準を作らない方針）。`pointsShortOfPassLine`は合格ラインまでの不足問数。
- **Exam.jsxの変更点**：
  - モードに「午前+午後 通し」（180問、`modeId==='full'`）を追加。`fullPhase`
    （'am'|'pm'）と`amSnapshot`（午前終了時点の解答・時間データ）で状態管理し、
    午前が終わると採点せず`stage==='break'`（休憩画面）を挟んでから午後を組み立てる。
    結果は`combinedOrder`/`combinedAnswers`で180問分をまとめて1回の履歴として記録する
    （`examResults`の`mode:'full', count:180`）。続きから機能（保存済み途中経過）も
    `fullPhase`/`amSnapshot`を含めて復元できるようにした。
  - セットアップ画面（午前/午後/通し）に「直近使った問題を避けて出題する」トグルを追加。
    **直前期（`phaseForDate`が`chokuzen`/`final`）は常にON固定**（新規ストップの方針と
    同じ考え方）。収録数が少ない科目（`roundsPossible < 2`）は警告し、網羅マップへの
    ボタンを出す（`onNavigate('coverage')`）。
  - 結果画面に、伸ばすと効く科目（午前/午後のみ・#6）・合格ラインまでの不足問数（#25）・
    2回連続合格ライン到達の通知（CLAUDE.mdのユーザー本人のゴールと同じ定義、#22）・
    前回の同モードとの問題重複率（#15）・一時停止の回数と合計時間（#28）・
    時間を使いすぎた問題への頻出度バッジ（`daikoumokuRank`、#24）・苦手科目だけの
    再挑戦チップ（#26）を追加。
  - モード選択画面に「前回の模試から◯日」（#30）、ExamHistory（記録グラフ）の各バーに
    ロードマップのフェーズ番号（#27、`phaseForDate`）をtitle属性で表示。
  - 得意/苦手/選択式モードの結果画面にも、合格ラインは非対象である旨を明記した上で
    参考スコアを表示（#29。配分が本番と異なるモードを誤って合否判定しないため）。
- 実装後、`scripts/check-bundle-size.mjs`の予算をユーザー指定で900KB→
  `MAX_BUNDLE_SIZE = 1024 * 1024`（1MB）に引き上げた（実測899KB、以前の900KB予算だと
  ほぼ余裕が無かったため）。

## 復習・SRSを毎日ゼロに戻す運用の強化（2026-09-02追加）
「今強化すべき機能ベスト3」の3位（仕組み自体は強いので、要注意バッジと○ふりかえりを
実際に毎日使う運用そのものを支援する）をユーザー指定で30案→全実装したもの。
新しいファイルが増えたので、単一の正の置き場をここにまとめる。
- **`src/lib/reviewZeroLog.js`**：復習（期限が来ているもの）が0件になった日を記録する
  唯一の発生源（`markReviewZeroToday`、`useStore.js`の中央エフェクトが呼ぶ）。
  `zeroDaysSummary`（直近◯日中◯日）・`daysSinceLastZero`（最後の達成から何日か。
  3日以上は`recommendNewPct`が新規:復習比率を自動でさらに復習寄りにする＝#17）・
  `zeroRateByWeekday`（曜日別の達成率）・`finishDaysSummary`（history上の
  `source:'maru-review'`と合算した「毎日の仕上げ実施率」）・`reviewZeroLogToCsv`
  （設定画面の書き出し）を持つ。Home・Review・週次ジャーナル・ハリオのリマインドが
  すべてこのログ（`store.reviewZeroLog`）を参照する。
- **`src/lib/reviewDwell.js`**：`srs.js`はwrongCount/correctStreakの"今の値"しか
  持たないため、「いつから要注意か」「いつから復習対象のままか」はhistoryを辿って
  求める。`leechSince`/`leechDwellDays`/`leechList`（Home・Reviewの要注意バッジで
  滞留日数を表示）・`leechBySubject`（Analyticsの学習インサイト）・
  `reviewDwellBySubject`（未解消の復習対象の科目別平均滞留日数）・
  `resolvedLeechEvents`/`resolvedLeechesSince`（要注意化→5連続○のマスター到達を
  correctness再生で検知。週次ジャーナルの「今週解消できた件数」に使う）。
- **`src/lib/reviewGoal.js`**：Review.jsxだけが持っていた「今日の復習目標」計算
  （今日こなした復習＋残り期限数、しんどい日は半分に緩める）を切り出し、Homeにも
  同じリング表示を出せるようにした（単一の正。画面ごとに計算がズレるのを防ぐ）。
- **`src/lib/snoozeLog.js`**：「😴 明日まで先送り」ボタンを押した記録だけを別に持つ
  （`setNextDue`自体は誤答理由別の自動間隔調整でも呼ばれるため、ボタン操作だけを
  区別する必要があった）。3回以上先送りしている問題には「先送りグセ」バッジ。
- **`src/lib/weakClusters.js`の`tagTrend`**：Review.jsxが inline で持っていた
  「今週 vs 先週の誤答率トレンド」計算を切り出し、週次ジャーナルの
  「先週より改善したテーマ」チップでも同じ関数を使う。
- **`src/lib/forgetting.js`の`dailyForgettingPick`**：忘却予測（forgettingRisk）の
  上位から、priorityFocus.js等と同じ日付ローテーション方式で1件だけHomeに出す。
- **`src/data/roadmapPhases.js`**：`upcomingPhaseChange`（フェーズ切り替えが1週間
  以内に迫っていたらHomeで予告）・`parseMixRatio`（`mix`文字列「新規X：復習Y」を
  機械可読な形に変換。自由文の説明はnullを返す＝手元に無い基準を作らない）。
- **Session.jsx**：「○の見直し／○の高速回転」セッションの`recordAnswer`に
  `source:'maru-review'`を付けた（`maruPool.js`の`lastMaruReviewAt`がこれを参照。
  「最後にいつふりかえったか」をhistoryだけから追跡できるようにするため）。
  「今日のおすすめ」比率提案は`reviewStalledDays`（daysSinceLastZero）を渡して
  停滞時により復習へ寄せる。
- **App.jsx**：Home「復習だけ◯問」「要注意だけ今すぐ」からReview.jsxをワンタップ
  自動開始するための`reviewQuickStart`中継（数値＝件数指定、`{ids}`＝問題id直接指定。
  既存の`audioReview`と同じ考え方）。Homeの音声復習ショートカットも同じ`audioReview`
  パターンに相乗り。
- **既存の「毎日のリマインド通知」（`settings.reminder`）に相乗り**：復習専用の
  別リマインド設定は作らず、`haripanReminder`が`daysSinceLastZero`を受け取り、
  3日以上ゼロに戻せていない時だけ一言追加する（同じ機能を2か所に持たない）。
- **Review.jsx**：開始前に過去の解答ペース（`bufferSession.js`の
  `estimatedAnswerSeconds`）から所要時間の目安を表示。期限0件でも「念のため確認」
  枠だけはワンタップで始められるボタン。完了画面で復習を完全にゼロにできた時は
  専用の演出＋直近30日の達成日数を表示。
- **Calendar.jsx**：日別詳細の復習期限件数に、うち要注意が何件かを重ねて表示。
- **Analytics.jsx（InsightsSection.jsx）**：要注意の科目別内訳・未解消の復習対象の
  科目別平均滞留日数を追加。

## 一問一答（Quiz.jsx）とSession.jsxの機能統一（2026-09-02追加）
ユーザーから「今強化すべき機能ベスト3」を聞かれて答えた3位（Quiz.jsxとSession.jsxの機能差）を
ユーザー指定で30案→全実装したもの。「連携させたほうがいいものは連携させて」の指示どおり、
新規ロジックはSession.jsx/Quiz.jsxの両方から使う共有フック・共有コンポーネントとして切り出した
（単一の正）。新しいファイルが増えたので、置き場をここにまとめる。
- **根本バグの修正**：Quiz.jsxの自己採点（○/△/✕）は`recordAnswer`に`selfKind`/`objectiveCorrect`を
  渡していなかった（3引数のみ）。このため**Quiz.jsxでの自己採点は`maruPool.js`はじめselfKind依存の
  機能から一切見えていなかった**（○の見直し等が使えなかった直接の原因）。`recordAnswer(q, correct,
  grade, source, selfKind, objectiveCorrect)`の全引数を渡すよう修正。
- **`src/lib/useMaruReview.js`**：`maruPool.js`の`maruStatusList`/`excludeMastered`/
  `orderMaruStatus`をラップするフック。Session.jsx・Quiz.jsxの両方の「✅ ○の見直し・高速回転」で共用。
- **`src/lib/useMissTypeHandling.js`**：`missTypes.js`の`loadMissTypes`/`recordMissType`/
  `missTypeTrend`/`missTypeAnomaly`をラップするフック。`setNextDue`を渡すと誤答理由別の
  間隔調整も自動で行う（**以前はReview.jsxだけがこの調整をしており、Session.jsxは持っていなかった
  不整合を今回あわせて解消**）。
- **`src/components/ManagerReview.jsx`**・**`src/components/FastCard.jsx`**：Session.jsx内に
  ローカル定義だった「予定通り完了したか」振り返り／高速回転カードを、ロジックを変えずに
  そのまま共有コンポーネントへ切り出したもの。
- **`src/components/MaruReviewCard.jsx`**：「✅ ○にした問題をふりかえる」カード。
  `useMaruReview`の状態一式を受け取るprops駆動で、Session.jsx（グローバルな`store.session`）と
  Quiz.jsx（ローカルstateの`maruSessionKind`）という**セッション状態の持ち方が違う2画面**からでも
  同じ見た目・同じ絞り込みロジックで使えるようにしてある。
- **`src/components/BufferPlanCard.jsx`**：「⏱ 時間で計画する（3分の2バッファ術）」カード。
  `bufferSession.js`の`planStudySession`呼び出しと分数入力のUIをここに集約。開始後の処理
  （`onStart(bufferPlan)`）は呼び出し側に委ねる設計（Session.jsxの`begin()`とQuiz.jsxの
  開始方法が異なるため）。
- **Session.jsxのリファクタ内容**：上記のフック・コンポーネントを使うよう置き換え。見た目・文言は
  維持しつつ、ついでに2点改善した——①弱点タグの集計を自前のカウントから`weakClusters.js`の
  `weakTagClusters`（表記ゆれ統合・汎用語除外つき）に統一、②誤答理由記録時に`setNextDue`を
  呼ぶよう修正（Quiz.jsxと同じ挙動にそろえた）。
- **Quiz.jsxの追加内容**（根本バグ修正に加えて）：`useMaruReview`＋`MaruReviewCard`による
  ○の見直し・高速回転（ローカルstate`maruSessionKind`で管理、Session.jsxの`store.session`とは
  独立）／`useMissTypeHandling`による誤答理由フィルタ・傾向表示／`BufferPlanCard`による
  3分の2バッファ術（ローカルstate`buffer`、`ManagerReview`分岐→バッファ用途カード）／
  弱点タグチップ・COMPARISONS連動の弱点サマリー／「🎯今日のおすすめ」ボタン
  （`recommendNewPct`に`reviewStalledDays`を渡す）／「🔗関連をたどって続ける」
  （`chainNext`）。新たに`onToast` propを追加（App.jsxから`showToast`を渡す）。
  **Quiz.jsxのbuffer/maruSessionKindは`quizProgress`（続きから復元）には含めない**——
  再開時は常に通常モードに戻る仕様（バッファ計画・○の見直しは毎回明示的に開始する前提のため、
  意図的なスコープ限定）。
- 検証済みで変更不要だった項目：ストレージキーの衝突なし
  （`shinkyu:session`と`shinkyu:quizProgress`は別キー）、Mascot.jsxの「今日の目標」集計は
  `source`を問わずhistory全件を数えるため元からQuiz.jsxの解答も正しく含まれていた。
- Quiz.jsxは下部ナビの即時import画面のため、共有コンポーネント化で起動時バンドルが
  やや増えた（起動時JS 929KB/予算1024KB、引き上げた予算内に収まっている）。
- **Playwright QAで発見・修正したバグ**：`buildIdsForTarget(target)`は元々「何問ずつ区切って
  出すか」のbatchチップ機能専用で、`newPct===null`（新規/復習比率を指定していない既定状態）かつ
  `batch===0`（バッチも既定の「すべて」）の時は`target`を無視してfilteredPool全体を返す作りに
  なっていた（batchチップの既存呼び出し元はその時`target===filteredPool.length`を渡すので実害が
  無かった）。この関数を`startBuffer`から再利用した際、バッファ計画のtarget（例：15問）が既定設定の
  ままだと無視され、基礎タスクが母集団全体になり`ManagerReview`が実質出現しない状態になっていた。
  `buildOrder(filteredPool, target)`を常に呼ぶよう修正（batch===0の元の呼び出し元では
  target===filteredPool.lengthなので結果は変わらない）。**同じ関数を新しい用途に再利用する時は、
  既存呼び出し元の暗黙の前提（この場合「targetは常にpool全体と一致する」）を洗い出してから使うこと。**

## このリポジトリに同梱されている他アプリ（鍼灸アプリ本体とは独立）
main への push で `.github/workflows/deploy.yml` が同じ Pages 成果物にまとめて配置する。
それぞれ独立した package.json / ビルドを持つので、**鍼灸アプリ側の作業では触らない**。
| ディレクトリ | 公開パス | 内容 |
|---|---|---|
| `rirakuru-app/` | `/rirakuru` | るるくる セラピスト業務マニュアル（Next.js） |
| `sleep-tracker/` | `/sleep-tracker` | 分割睡眠トラッカー（Vite + TS） |
| `youtsu-navi/` | `/youtsu-navi` | 腰痛ナビ（有資格者向けアセスメント支援。腰痛／肩こり・頸部痛／膝痛。Vite + React JSX・端末内保存のみ）。詳細は `youtsu-navi/README.md` |
| `henkaku-note/` | `/henkaku-note` | 変革ノート（自己改革カレンダー。ゴーストモード7ステップの習慣トラッカー。Vite + React + TypeScript・PWA・端末内保存のみ）。詳細は `henkaku-note/README.md` |
| `ouro/` | `/ouro` | Ouro（自分専用のAI会社。AI社員に仕事を依頼し、成果が知識資産として積み上がる Knowledge OS。Vite + React JSX・端末内保存・BYOK）。詳細は `ouro/README.md` と `ouro/docs/ARCHITECTURE.md` |
| `kagami/` | `/kagami` | 鏡（かがみ。心理的な操作の「型」に気づくためのアプリ。貼った文面を端末内だけで調べる。Vite + React JSX・端末内保存のみ）。詳細は `kagami/README.md` |
| `shikaku-lab/` | `/shikaku-lab` | 資格ラボ（過去問→AI変換。資格試験ごとの対策法・勉強法・認知特性から学習計画書を作り、その計画書から「自分専用の対策アプリ」の設計書まで出す。上部にポモドーロタイマー常設。Vite + React JSX・端末内保存のみ）。詳細は `shikaku-lab/README.md` |
| `maai/` | `/maai` | 間合い（まあい。恋愛の心理学を「近づき方」と「気づき方」の両方から。自分の側でできること30件＋人を思いどおりにする型31件＋同意8件。Vite + React JSX・端末内保存のみ）。詳細は `maai/README.md` |
| `chou/` | `/chou` | 腸（ちょう。お腹の悩み・腸活。IBS 中心。お腹の記録を、受診のときに渡せる形にする。Vite + React JSX・端末内保存のみ）。詳細は `chou/README.md` |


腰痛ナビの要点だけ再掲：判定は**タグ経由**（`src/data/schema.js` の `TAG_VOCABULARY` が単一の正）、
医療内容の追加時は `sourceIds` で出典必須、`src/lib/storage.js` はネットワークに触れない、
同意文を変えたら `CONSENT_VERSION` を上げる。テストは `node --test` の再帰探索でルートの
`npm test` からも実行される（新しいテストは `youtsu-navi/test/*.test.mjs` に置く）。
目次（`src/data/toc.js`＋`src/lib/yomi.js`）は各データから自動生成し、**タイトル重複禁止・
漢字には `reading` 必須（自動推定しない）・数字は読みで五十音へ振り分け**を `toc.test.mjs` が機械チェックする。
疾患・原因カード129件は `src/data/diseases.js`（鑑別と受診判断のための参考知識。診断のためではない）。
①②③…のように番号順が意味を持つ項目は `sortKey` で行内の並びだけを上書きする。
知識ベース（`src/lib/knowledge.js`＋`components/Knowledge.jsx`）は動画・書籍で学んだことを
**自分の言葉の要約＋出典**で貯める場所。**二段階チェック**（第1＝自己チェック／第2＝日を改めた
裏取り。第1と第2で同じ観点をなぞらない）を通った `stage:'active'` だけが結果画面に出る。
「受診をすすめる判断（レッドフラグ）を弱めていない」が外れたら自動で見送り。要約は600字上限
（逐語転載の抑止）、出典必須。**知識ベースのルールを足す時は knowledge.js だけを直す**。
音声メモ入力（`src/lib/voice.js`＋`components/VoiceMemo.jsx`）は**既定オフのオプトイン**
（`settings.voiceInput`）。ブラウザの音声認識は音声を提供元のサーバへ送る場合があるため、
端末内保存方針の**明示的な例外**として設定画面で説明したうえで選んでもらう。
カルテ（`src/lib/records.js`）は**表示名のみで管理し氏名・連絡先を持たない**。保存するのは入力（タグ）
なので判定ロジックを直しても過去の記録を読み直せる。書き出し（`src/lib/exporter.js`）はお客様向けと
施術者の控えを作り分け、**お客様向けは既定で推定パターン名を含めない**（診断と受け取られないため）。

変革ノートの要点だけ再掲：ゴーストモード7ステップが習慣の初期値（`src/lib/habits.ts`、編集可）。
**ステップ⑤だけは原典の「夜11時就寝」ではなくシフト対応版**（`src/lib/shift.ts`。勤務日＝終業＋90分、
休日＝固定時刻。夜勤で守れない目標を並べると続かないため）。週次振り返りは**管理者視点→実行者視点**の順で、
文言は3分の2バッファ法と同じ「悪いのは実行役ではなく無理な計画を立てたマネージャー」の前提
（責める表現が入っていないことをテストが機械チェックする）。**連続日数を煽らない**——主役は
「期間の◯日目/全◯日」と通算の実践日数で、期間（既定30日）の終わりに続けるかを本人が決める。
`src/lib/storage.ts` はネットワークに触れない。達成率の分母は「その日に有効だった習慣」なので
あとから習慣を足しても過去は下がらない。習慣は削除より休止（`archivedAt`）を既定にする。
取り込んだ学び（瞑想）は `src/lib/meditation.ts`＋`src/data/presets.ts`。**出典は本人の要約で一次資料未確認**
のため効果は断定せず「報告されている」と書く（テストが断定表現を機械チェック）。長さは**1段階ずつしか
上げない**（急に1時間はかえって逆効果という原典の注意を実装に落としたもの）。達成判定は長さではなく
「1回でも記録した日」＝量より継続の一貫性。**禁欲の日数カウンターは作らない**（出典自身が根拠が弱いと
している主張を、数えることで強めてしまうため。実装したのは「衝動に気づいて手放す」練習のほう）。
『最高の体調』由来の6領域は `src/lib/condition.ts`。**炎症スコアを作らない**（体内の炎症は検査で
測るもので行動から数値化できない。数えるのは「炎症を下げるとされる行動を何日置けたか」だけ。
テストが `score` という関数の不在を機械チェックする）。睡眠は**2層に分ける**——`shift.ts`＝いつ寝たか、
`sleepQuality.ts`＝どう眠れたか（4条件）。**記録していない項目は判定しない**（睡眠不足だと断定しない）。
3のルール（`threeRules.ts`）は日・週・月でキーだけ変えて同じ表に持ち、「今日の宣言」を置き換えた
（同じことを2か所に書かせない）。人間関係を記録すると①仲間に、瞑想を記録すると不安の対処に自動で入る。
食事の時間と量（`src/lib/fasting.ts`、『できる男は超小食』由来）は**他より慎重に作ってある**：
①裏の取れない主張（脳のデトックス・消化＝フルマラソン相当・肉の腐敗・牛乳のカゼイン）は
`UNVERIFIED_CLAIMS` として隠さず出したうえで必ず「裏が取れていない」を添え、断定しない。
②`PRECHECKS`（血糖の持病・服薬・妊娠授乳・成長期・摂食障害の経験・低体重）に該当したら
「先に医師に相談」を出し続ける。③**断食の連続日数カウンターを作らない**（数えると「食べない日を
伸ばすほど良い」方向に押してしまう。テストが `streak` を返さないことを機械チェック）。
代わりに `STOP_SIGNS` で止めどきを見て、段階は1つずつしか上げない。出典の「◯時間空ける」は
文字起こしで数字が欠けていたため**勝手に作らず**アプリの初期値として持つ。
モンクモード（`src/lib/monkMode.ts`）は既存と重なる部分を持たない（瞑想・加工食品・人との接点は
既存へ寄せる）。**最大の要点は、出典どうしが食い違う点をそのまま見せること**——モンクモードの
「友人と距離を置く」と最高の体調の「孤独は禁煙より体に悪い」は正面から反対なので、`CONFLICTS` に
両方を並べ、**アプリはどちらが正しいかを決めない**。実装するのは「一人で集中した時間」だけで、
一人の時間だけ増えて人との接点が無い日が続いたら `isolationWarning` が問いかける（決めつけない）。
**朝に塩3〜4gは記録も推奨もしない**（血圧・腎臓の `MONK_PRECHECKS` に該当したら警告を出し続ける）。
「ウォーキングの降圧作用は薬より効果的」には「服薬の判断は必ず医師と」を必ず添える。
66日は**累計でよい**（出典自身がそう言っている）ので実践期間の選択肢に足した。
起きて最初のルーティン（`src/lib/morningRoutine.ts`、モーニングメソッド由来）は**「朝」を前提にしない**
——出典自身が「必ずしも朝でなくてよい、起きて最初にやる」と言っており、夜勤では朝を前提にすると
実行できない機能になる。時計ではなく「起きてから何分で始めたか」を見る。6ステップを**順に走らせる
ランナー**で、終えたステップは既存の記録へ書き込む（瞑想→meditation、運動・読書→monk、
ジャーナル→今日の3つ）。**`sleepPhrase` は「しか」を含む文を返さない**（「◯時間しか眠れない」を
「◯時間も眠れる」に言い換えるのが出典の要点。テストが機械チェック）。
目次・索引（`src/data/toc.ts`＋`src/lib/yomi.ts`＋`src/lib/focus.ts`）は**目次専用の手書きデータを持たない**
——`buildTocEntries()` が各 lib の元データから毎回導出する。並びは あ〜ん→A〜Z→その他で、
**数字は読みに変換してから行を決め**（「3のルール」→さ行）、**漢字の読みは推定しない**
（`reading` が無ければ「その他」に落として入れ忘れを見せる。`warnOtherRow` が目安24件で知らせる）。
英数字混じり（ＷＨＯ／Ⅰ型／①）は `normalizeAlnum` で正規化してからA〜Zを判定。
**タイトルの重複は禁止**で、ぶつかったら `disambiguate` がまとまり名を添える（元の名前は別名に残す）。
**`descriptionStatus` の既定は `needs_review`＝「※要確認」**——出典由来の主張は一次資料未確認なので必ず印を出し、
`verified` にできるのはアプリ自身の仕組みと **本人が `markVerified` を押した時だけ**
（会話から来たものが確認済みになる道を作らない）。説明が空なら「※説明未登録」、飛び先が無ければ
「関連する飛び先はありません」と**必ず出す**（黙らない）。飛び先の id は `src/data/anchors.ts` の
`ANCHORS` が単一の正で、画面に文字列を直書きしない。飛び方は `flashTo`＋`useFocusJump` を再利用し、
**画面の切り替えは `useLayoutEffect`**（`useEffect` だと飛ぶ時にまだ前の画面が描かれていて着かない）、
**印は className ではなく `data-flash` 属性**（開閉で className が書き直されると印だけ消える。鏡で実際に踏んだ）、
**「先頭へ戻す」を副作用にしない**（`App.tsx` は view の変化で `scrollTo` せず、ナビのタップの中で行う）。
会話由来の追加・削除の候補は `src/data/tocCandidates.ts` に留め、**本体データへ一切書き込まない**。
候補を作ってよいのは3つのきっかけだけ（`■用語追加：`の合図／教材のタグ／本人の「目次に追加して」）で、
**それ以外では作らない**（`makeCandidate` が null を返す）。「追加する」を押した時に初めて
読み・重複・分類・正規化の4つを見て本体へ入れ、「しない」は痕跡を残さない。確定は履歴に残り
`undoLastTocAdditions(n)` で直近の追加だけ取り消せる。
テストは `henkaku-note/tests/*.spec.ts`（TypeScript）。テスト用の Settings は
`tests/fixtures.ts` の `testSettings()` に集約する（各テストに書き写すと項目追加のたびに全部壊れる）。CIがNode 20のため `tsc` で
`.test-build/` へ出してから `node --test` する（`npm test` はアプリ側で実行。ルートの再帰探索には
拾われない名前にしてある）。

Ouro の要点だけ再掲：**AI社員（人格・役割・記憶）とAIエンジン（Claude/GPT/Gemini）を必ず分離する**
（社員は `providerPref` という希望だけを持ち、エンジンの実体を持たない。モデルが入れ替わっても社員は残る）。
役職は `src/data/roles.js` が単一の正で、**1件足せばマインドマップ・自動社員選択・雇用画面が自動で追従する**
（画面に if を足さない）。初期構成は6役職×3席＝18人だが、席数は `company.seatsPerRole` の初期値でしかなく
増席できる。**出典のない知識を作らせない**（`createKnowledge()` が空なら「AI生成（未検証）」を自動で立て、
`origin` で AI生成／外部由来／自分で書いた を必ず区別する）。**既定の権限は閲覧・作成のみ**で、
送信・削除・支払い・外部公開は `lib/permissions.js` の `REQUIRE_APPROVAL` により必ずユーザー承認を通る。
**接続数の上限をハードコードしない**（判定は `data/plans.js` の `connectionLimit()` の1か所だけ）。
保存キーは `lib/storage.js` の `KEYS` に登録し、**`ouro:secrets`（APIキー）は書き出しに含めない**。
`lib/storage.js` はネットワークに触れない。APIキーが1つも無くても全画面が動く（`providers/local.js` の
ローカル社員が仕事の型を返し、AIではないことを画面に明記する）。実行系は `useStore.js` の `put`/`log` を
必ず通す（直接 setState すると `stateRef` が古くなり、作った直後の仕事が見つからなくなる）。
**役職 × ジャンル × 3席**が社員の構成（`src/data/genres.js`＋`src/data/employees.js`）。
役職が「何ができるか」、ジャンルが「どの分野で」。同じリサーチャーでも医療と副業では別の3人を雇える。
席番号は**組（役職×ジャンル）の中で**数える（`seed.js` の `nextSeat(employees, roleId, genreId)`。
役職だけで数えると別ジャンルの席のせいで番号が飛ぶ）。ジャンルはユーザーが足せるが**読みが必須**
（`makeGenre` が空・非ひらがなを弾く）。依頼時にジャンルを選ぶと `assignFor(roleId, genreId)` が
その分野の社員を優先し、いなければ汎用へ落とす。
**目次は `src/data/toc.js` が各データから自動生成**し（書き写さない）、`src/lib/yomi.js` が
読み・並び・行分けを担当する。上の「目次・索引の共通ルール」をそのまま実装しており、
`test/toc.test.mjs` が機械チェックする（読みの入れ忘れ＝「その他」行に落ちること・タイトル重複・
飛び先の実在・数字の振り分け）。**漢字の読みは絶対に推定しない**——読みが無ければ「その他」に出して
入れ忘れを見えるようにする。カレンダーは `src/lib/schedule.js`（`Calendar.jsx`）。
**案件の締切は deals から毎回導き、予定（events）に複製しない**（二重管理にすると締切を直しても
カレンダーが古いままになる）。下部ナビは6つ`ホーム／目次／社員／依頼／予定／知識`で、
「会社」はナビのすぐ上の常設バー（`.company-bar`）から開く。
役職は2チームに分かれる（`roles.js` の `group`）：**知識チーム**15役職（調べる・作る。元からの役職）と
**会社チーム**10役職（①プロダクトオーナー〜⑩広報・ブランディング。事業を回す）。会社チームの各役職には
**名前つきのAIキャラクターが3名ずつ（全30名）**いて `src/data/characters.js` が単一の正
（名前・カナ・読み・出身・人物像・書き方・持ち味・肖像のパーツ）。キャラクターは自動では雇わず、
`Characters.jsx`（AIキャラクター名鑑）から1名ずつ／役職ごとにまとめて雇う。
キャラクターが使われるのは**汎用ジャンルの1〜3席だけ**で、他のジャンルへ雇うときは共用の名前プールに落とす
（同じ人が二重に在籍しないため）。役職名が似たもの（マーケター／コンテンツマーケター、経理／経理・労務）が
並ぶのは、既に雇っている社員を迷子にしないため両方残しているから。
**肖像は線画のSVGをその場で描く**（`src/data/portraits.js`＋`components/Portrait.jsx`。画像ファイルを持たない）。
**2026-08-25 ユーザー確認済み：線画のまま続ける**（アニメ調などの画像に差し替える案は見送り）。
画像ファイルを持たない方針は、起動の速さ（`docs/PERFORMANCE.md`）とも直結しているので**変えない**。
**顔立ちで人種・民族を描き分けない**——少ない線でそれをやると戯画になるため、見分けは髪型・装い・紋章でつけ、
出身は文章として持たせる。30名の見た目が重複しないことをテストが機械チェックする。
自動社員選択は**当たった語の長さを点にする**（`dispatcher.js` の `scoreRoles`）。固定点だと
「優先」しか持たないストラテジストが「優先順位」を持つプロダクトオーナーに常に勝ってしまうため。
**未雇用の役職は計画から外す**（`workflow.js` の `createTask`）。25役職のほとんどは未雇用なので、
計画に混ざると担当者が見つからず仕事全体が失敗する（実際に起きたバグ）。外した役職は
`task.unstaffedRoles` に残し、依頼画面と結果画面で「雇えば次から担当に入る」と伝える。
**マーケティングチーム（5人体制）**は3つめのチーム（`group:'marketing'`）。
①企画・コンテンツ（Olivia／攻め）②分析・ガバナンス（Ethan／守り）③運用・配信（Sofia／攻め）
④ブランド・PR（Lucas／対外）⑤予測・戦略分析（Mia／助言）。**このチームの核は「攻めと守りを
同じ役職に兼務させない」こと**で、次をデータで持ちテストで機械チェックしている：
`stance`（攻め/守り/対外/助言）・`isApprover`（承認と差し戻しができる＝Ethanだけ）・
`requiresApprovalBy`（公開前に必ず通す確認役）・`noKpi`（Ethanに成果目標を持たせない。
持たせると自身が攻め化してブレーキが甘くなる）・`proposalOnly`（Miaは提案のみ・実行しない）・
`outOfScope`（権限外）。**承認は `dispatcher.js` の `withApprovers` が計画の最後に自動で足し、
`maxSteps` で切り落とさない**（「上限に達したので確認を省いた」が起きてはいけないため）。
確認役が未雇用のまま外れた場合は `task.missingApprovers` に残り、結果画面が
「確認を通していない成果物です」と警告する。チーム共通のルールは `ROLE_GROUPS` の
`commonPrompt` に置き、`runtime.js` が個別の役割より**先に**読ませる。
※ Olivia/Ethan/Sofia/Lucas は既存キャラクター（Olivia Bennett・Ethan Clarke・Sofia Marchetti・
Dr. Lukas Weber）と名前が近い。ユーザー指定の名前をそのまま使っているため、画面では
チーム名と役職を併記して取り違えを防いでいる。
**2026-08-25 ユーザー確認済み：このままでよい**（姓を足して区別する案は見送り）。
**もう提案し直さないこと。** 名前が近いことは既知で、了承済みの状態。
**速さの仕組みは `ouro/docs/PERFORMANCE.md` が単一の正**（第1弾30＋第2弾30＋2周目4の計64項目・2026-08-26）。要点だけ：
①画面は原則 lazy（下部ナビ6画面と常設バーのみ即時）。②`vite.config.js` の `manualChunks` で
`react` と `roster`（characters.js＋roles.js）を分ける。③紋章は `components/Seal.jsx` の
SVGで描く（画像ファイルを持たない）。④`useStore.js` は二段階起動で、`FIRST_KEYS` を読んでから
`REST_KEYS`（部署・出典・会議・操作履歴・接続）を読む。**読み込みが済むまで REST_KEYS を保存しない**
（`hydratedRef`／`preAuditRef`。空配列のまま保存すると操作履歴が全部消えるバグが実際に出た）。
⑤`storage.js` は デバウンス＋重複排除＋アイドル書き込み＋離脱時フラッシュ。
`RECORD_COLLECTIONS` に登録したキーは**変わった1件だけ**を書く（目録＝manifestで順序を持つ。
`ids` 方式と、操作履歴だけ時刻順キーの `sorted` 方式）。`flushNow()` は実行中の書き込みも待つ。
⑥`content-visibility: auto` は**`::before` で線・点を描いている要素に付けない**
（`.step` のタイムラインの点が切り落とされる）。⑦目次は先頭120件の段階表示だが、
**五十音バーは絞り込み結果の全体から作る**（枠外へ飛ぶ時は先に枠を伸ばす）。
⑧カレンダーは `schedule.js` の `buildDayIndex()` で「日付→予定」の Map を1回だけ作る。
⑨AIは3社とも `lib/providers/stream.js` の `readSse()` で流し受けし、`throttleDelta()`（90ms）で
描き直しを間引く。会議の意見・反論は `Promise.all`。⑩`lib/perf.js` が画面切替・保存・AI実行の
所要時間を**端末内だけ**に記録し、会社画面「速さの記録」で中央値と最悪値を見る（送信しない）。
第2弾で足した仕組みのうち、**壊しやすい約束**は次の6つ：
⑪画面の読み込み関数は `lib/preload.js` の `LOADERS` が単一の正（lazy() と先読みが
同じものを指す。別々に書くと二度読みになる）。⑫キャラクターの人物像・書き方・出身は
`data/characterDetails.js` にあり **`loadCharacterDetails()` を待ってから**雇う
（`data/characters.js` は索引だけ。読み込み前は席の持ち味で代用するので空にはならない）。
この都合で `hireEmployee`／`hireIntoRole`／`hireCharacter` は**非同期**。
⑬操作履歴は起動時に新しい400件だけ読む。**一部だけ読み込んだ印（`partialKeys`）が
付いている間、読んでいないレコードを消してはいけない**（手元の400件を保存した瞬間に
残りが全部消える）。書き出しは track:false で読み、取り込みは印を外してから書く。
⑭古い記録を畳む処理（`foldAudit`）は**1日に1度だけ**（毎起動で走らせると全件を
読み直してページングが無効になる）。⑮同時に走る手順（仕事の流れの steps の入れ子）の
引き継ぎ先は **「g+1」ではなく残っている番号の中の次**を探す（未雇用の役職を外すと
番号が飛ぶ）。同時の手順は**全部そろってから**次へ渡し、承認は必ず単独で最後。
⑯窓表示（`components/Window.jsx`）の行の高さは **JSX の `ROW_H` と CSS の両方**を
そろえる（片方だけ直すと行が増えるほどずれて空白が出る）。
`npm run size`（起動時に読む量の上限）と `npm run perf`（起動の速さ・合否は出さない）で
じわじわ遅くなるのを見張る。
**費用と安全の決まり（2026-08-26 に洗い出して直したもの）**：⑰費用の出る実行は
**仕事も会議も同じ扱い**（AI会議だけ承認を飛ばしていた。会議はAIを「人数×2＋1」回呼ぶので
いちばん高い）。⑱`autoApproveCost` を入れても `settings.monthlyCapUsd`（既定$5・0で無制限）
に達したら確認へ戻す。⑲**AIが動いていない成果を「AI生成」と記録しない**——ローカル社員の
成果は来歴 `template`。⑳**失敗した仕事を行き止まりにしない**（押しても何も起きないボタンを
出さない。`retryFailed` で失敗した手順だけ戻す）。㉑**空の応答を「完了」にしない**
（`isEmptyResult`。中身ゼロで完了と出ると何が起きたか分からない）。㉒**同じ本文を2か所に
持たない**——提出物は `assembleResult(task)` で組み立てる（以前は手順・task.result・知識の
body の3か所にあった）。㉓端末内保存なので初回に `navigator.storage.persist()` を頼み、
`lib/backup.js` が書き出しを促す。㉔知識を消したら `orphanSourceIds` でその知識だけの出典も消す。
㉕**ホームのクイック操作に、下部ナビ・会社バーから行ける画面を置かない**（`Home.jsx` の `QUICK`）。
㉖席数の呼び名は **`seatsPerGenre` だけ**（`seatsPerRole` は読み込み時に移し替える）。
**使い込んだ時に効いてくる決まり（2026-08-26 の2周目で洗い出して直したもの）**：
㉗**仕事は起動時に新しい120件だけ読む**（`TASK_PAGE`）。全件が要る画面は
`components/useAllTasks.js` を使い、読み足しは必ず
**`await flushNow()` → `load(..., 0, false)` → setState → `adoptFullList()`** の順で行う。
`save()` は「呼んだ時点の配列」を抱えたまま待っているので、順番を崩すと
**古い一部だけの配列があとから書かれて、読んでいないレコードを消す**
（実際に 300件 → 121件 まで減らした）。`adoptFullList()` は
**画面が全件を受け取ったあと**に、await を挟まず呼ぶ。
㉘一部だけ読んでいる時は、読まなかった id を `partialTail` に覚えて
**目録の末尾に付け直してから**書く（付け直さないと残りが消え、目録を書かないと
新しく作った仕事が次の起動で消える）。件数しか持たない `sorted` 側は目録を書き換えない。
㉙**費用の累計は操作履歴から数え直さない**——古い記録は畳まれて消えるので、
数え直すと合計が減る。`settings.costTotalUsd`／`costMonthUsd` に積み、
月が変わったら今月ぶんだけ0に戻す（`lib/permissions.js` の `addCost`／`spentThisMonthOf`）。
㉚**案件と仕事の結びつきは仕事の側の `dealId` だけ**（`deal.taskIds` は誰も書いていなかったので
AI費用が常に¥0＝手残りが報酬と同額に見えていた）。同じ関係を2か所に持たない。
㉛**会社として切った道具は誰も使えない**——画面と実行の判定を
`data/tools.js` の `isToolEnabled()` の1か所に寄せる（記録が無い＝使える。
画面が「未接続」と出しているのに社員が使ってしまう食い違いを無くす）。
㉜**読めないものを読めるふりをしない**——PDF の解析経路は消し、本文の貼り付けを案内する。
取り込んだ文章の来歴は `external`（AI生成にしない）。テキストは 2MB／4万字で切る。
㉝**在籍数の上限は持たない**（`plans.js` から `maxEmployees` を削除）。
端末内で動くだけなので人数を絞る理由が無い。上限を持つのは接続数だけ。
㉞**取り込み（インポート）は必ず確認を出す**（今のデータを置き換えると分かる文言で）。
㉟**AIの失敗は「次にすること」まで書く**（`providers/stream.js` の `failureAdvice(status)`。
生の応答は `e.detail` に残し、画面には日本語の手順だけ出す）。
**仕事台帳（2026-08-26 追加。AI社員チームの受け渡しの形）**：㊱**台帳は手で更新しない**——
全部の仕事を1枚で見る `Ledger.jsx` は `lib/ledger.js` が仕事から毎回導く**ビュー**で、
人が手で持つのは**期限（`task.dueAt`）・次の対応（`nextAction`）・保留理由（`holdReason`）の3つだけ**。
台帳が別の表を持つと `deal.taskIds` で起きた「誰も更新しない列」が再発する。
㊲**受付番号は採番の表を持たない**（`ticketOf` が受付日＋idの末尾から毎回同じ番号を作る）。
㊳**保留（`on_hold`）は理由が必須**で、`isRunnable` が実行を止める。実行中に保留にされたら
`applyStepResult` は `running` へ戻さない（戻すと止めたはずの仕事にAI費用がかかる）。
承認待ちから保留にしたものは `heldFrom` を見て承認待ちへ戻す（承認が二重に並ばないため）。
㊴**回答の枠（①結論②最優先③あなたの判断④成果物⑤TODO）は最後の手順にだけ掛ける**
（`lib/outline.js`＋`runtime.isFinalStep`）。全手順に掛けると1行で足りる調査結果まで長くなる。
㊵**判断・見出しは `workflow.finalOutput(task)` から拾う**——`assembleResult` は全手順を
「## 担当者名」で連ねた文なので、そこから見出しを探すと途中の手順の「まとめ」や
担当者名を「判断が要ること」として拾ってしまう（実際に起きた）。
㊶**判断はAIを呼ばずに③から機械的に取り出す**（`lib/decisions.js`）。承認＝実行してよいか、
判断＝出てきたものをどうするか、で層が違う。決まるまで台帳は「確認待ち」のまま。
㊷**引き継ぎで材料を消さない**（`lib/handoff.js`）。枠が拾えた時だけ②③と出典を落とし、
拾えなければ全文をそのまま渡す。見出しより前の本文（lead）も必ず残す。
設定で全文に戻せる（`settings.handoffMode`）。
㊸**CSV はセルを数式にさせない**（先頭が `= + - @` なら `'` を足す）・**BOM を付ける**
（Excelで文字化けする）・**CSVから仕事は作らない**（受付番号が一致した仕事の3列だけ書き換え、
取り込み前に必ず確認）・**読めない日付で今の期限を消さない**（`parseDateCell`）。
㊹**確約の見張り**（`lib/guard.js`）は止めない。価格・納期・効果の確約は人が引き受ける所だと
知らせるだけ。㊺**日付の入力欄に `toISOString()` を使わない**（UTCに直るので日本時間の
午前0時が前日になる）。
**会社のルール・改善ログ・完成条件・収益導線（2026-08-26 追加）**：
㊻**社員に読ませる会社の決まりは `lib/rules.js` が単一の正**（以前は `runtime.js` に5行直書きで、
ユーザーは1行も足せなかった）。`FIXED_RULES`（出典を書く・断定を避ける・最終判断は人間ほか5行）は
**足せるが外せない**——必ず先に読ませ、あとから足したルールで上書きさせない。
㊼**社員個人の記憶（`employee.memory.notes`）と会社のルール（`company.rules`）を分ける**。
memory.notes は長いあいだ **読むだけで書く場所が無かった**（`buildContext` が読むのに、
`seed.js` が `[]` にして以降どこも触っていなかった）。`teachEmployee` が書き、
仕事のときに読ませるのは新しい5件。古い形（ただの文字列）にも **毎回同じ id** を返すこと
（変えると「忘れさせる」が一致しない）。
㊽**完成条件は「決めただけ」で終わらせない**（`lib/checks.js`）。`spec.doneWhen` を1行ずつに割り、
`kind:'check'` の手順を**必ず単独で最後**に足す（番号は「残っている番号の最大＋1」。
並び順の i にすると前の手順と同時に走り、まだ出来ていない成果物を確認する）。承認と同じく
`maxSteps` では切り落とさない。**答えが読み取れなければ「合格」にしない**。
確認の手順は提出物でも成果でもない——`assembleResult`／`finalOutput`／知識の担当決めから必ず除く。
㊾**収益導線（`lib/funnel.js`）は「地図」**。作業が速くなっても向きがズレていれば収入にならない。
段は4つ（集める→読ませる→登録→買ってもらう）で**数と順番は変えない**（呼び名だけ変えられる）。
**業界平均のような手元に無い基準を持たない**——詰まっている所は自分の数字の中の相対で決める。
数字はユーザーが手で入れ、端末内だけに残る。書き込み側は `lib/funnelInput.js` に分けてある。
㊿**`ouro:funnel` は配列ではなくオブジェクト**。起動時の読み込みは REST_KEYS を `asArray` に
通すので、そこへ通すと毎回空になり、次に1件入れた時点で過去の週が全部消える（実際に踏んだ）。
51. **最初の道しるべ（`lib/onboarding.js`）のチェックは手で付けさせない**——実際の状態から導く
（棚卸しだけは状態から分からないので `settings.didInventory` を持つ）。7つ済むと自動で消える。
52. **書き出しの重なり（`lib/opening.js`）はAIを呼ばずに見る**。比べるものは同じ層でそろえる
（知識の body は全手順の連結なので、こちらも `assembleResult` と比べる）。見出しは数えない。
53. **日付の入力欄を `new Date('YYYY-MM-DD')` で読まない**（UTCとして読まれるので前日になる）。
54. **会社の画面（`Company.jsx`）とホームの下半分（`HomeBelow.jsx`）は lazy**。
下部ナビ6画面だけが即時で、常設バーは**ボタンが即時・画面は押した時**。
ホームの下半分（お金・循環・提案・予定・導線）を分けたことで、起動時に読む量から
`revenue.js`／`cycle.js`／`schedule.js`／`knowledge.js` が外れている。
**社員どうしの共有（2026-08-26 追加。役割ごとに別の仕事をしている社員が、互いの進み具合を知る）**：
55. **共有のためにAIを呼ばない**が原則。在席（`lib/presence.js`）・朝会（`lib/standup.js`）・
掲示板（`lib/board.js`）・関係する仕事（`lib/related.js`）・会議の材料（`lib/briefing.js`）は
すべて仕事から導くだけ。**AIを呼ぶのは会議と相談だけ**で、会議は「人数×2＋1回」でいちばん高い。
朝会は人数ぶん呼ばない（18人なら18回になる）。
56. **掲示板（`ouro:board`）は知識と役割を分ける**——知識＝出典が要る資産、掲示板＝業務連絡で
**30日で消える**。溜めるほど価値が上がる場所にしない（読まれない掲示板になる）。
社員に読ませるのは新しい方から8件だけ。**切り詰めは先頭を残す**（`memory.trimHead`。
`trimTail` で切ると新しい掲示と見出しごと落ちる）。
57. **中身の無い掲示を作らない**（`extractShare` が拾えなければ何も出さない）。
58. **社員が読むものは6層**（知識／自分の記憶／掲示板／関係する仕事／引き継ぎ／この仕事の補足）。
掲示板と関係する仕事を足したのは、**別の仕事にいる社員が何をしているか誰も知らなかった**ため。
59. **会議には材料を配る**（`briefing.js`）。同じ材料を読んでいない会議はただの雑談になる。
60. **反対役（守り）がいない会議は知らせる**（`meeting.hasGuard`）。`GUARD_ROLE_IDS` に足した役職は
**必ず hasGuard でも数える**（数えないと、勧めた人を入れても警告が消えない）。
stance の値は `'defense'`（日本語ではない）。
61. **会議の結論を会議の中で閉じさせない**——`meetingTakeaways` が「決まったこと」を掲示板へ回し、
結論は知識にできる（出典＝その会議。来歴は `meeting` で、AI生成と混ぜない）。
62. **1回だけ呼ぶもの（相談・引き継ぎの確認）も承認を通す**（`askOnce`）。
直に `runConsultRef` を呼ぶと、費用が出るのに承認も今月の上限も効かない。
承認の記録に `consult` を載せて `decideApproval` から実行する。
63. **引き継ぎ会は1往復だけ**（`lib/consult.js`）。受け手が「足りない材料」を返し、渡した側が補う。
**「なし」を足りないものとして持たない**（持つと要らない1回ぶん課金される）。
終わった手順でも聞ける——実行はひと続きで走るので、待機中だけに限ると押せる瞬間がほとんど無い。
補った材料を活かすため `redoFrom` で**その手順から**やり直せる（**使った費用は消さない**）。
64. **在席は「その人の手順が動いているか」で見る**（仕事単位で見ると、同じ仕事の順番待ちまで
実行中に見える）。
65. **`startMeeting` は非同期**（材料の組み立てを後から読むため）。await を忘れると
`mtg` が Promise になり会議が始まらない。同じ理由で `addBoardPost`・`consultEmployee`・
`askHandoffGap`・`supplementHandoff`・`holdStandup` も非同期。
66. **`createKnowledge` は `{ knowledge, extraSources }` を返す**。直に受け取ると id が
undefined になり、開いた先で「知識が見つかりません」になる。
**進み具合を見える形にする（2026-08-27 追加。共有の場だけでは足りなかったもの）**：
67. **誰を待っているか（`lib/queue.js`）は在席（presence）とは別**——presence は「その人の今」、
queue は「**その人のせいで何件止まっているか**」。実行中と未着手を混ぜないこと（溜まるのは未着手）。
**終わった仕事は誰も待っていない**（`waitingOn` が done/cancelled で null を返す）が、
判断が残っていれば「あなた待ち」として残す（完了を先に外すと消えてしまう）。
68. **役職ごとの持ち数（`lib/load.js`）は「雇い足す判断」のための数字**。
未雇用で計画から外れた役職（`task.unstaffedRoles`）は在籍0なので数字に出ない——別枠で必ず見せる。
69. **止まっている時間（`lib/stall.js`）は人間が原因の遅れを測る**。承認だけは承認の記録の時刻を使う
（仕事側より正確）。**半日未満は急かさない**（`NAG_AFTER_MS`）。画面に出す合計は
**出している分だけで数える**（全体の合計を出すと一覧の数字とどこも一致しない）。
70. **つまずき集（`lib/pitfalls.js`）は役職別・消えない**（掲示板は全員向け・30日で消える）。
**エラーは実行の結果（`result.error`）から取る**——ループの `step` は実行前の写しなので
`step.error` はまだ null（そのまま渡すと1件も貯まらない。実際に踏んだ）。
**中止（`result.aborted`）はつまずきにしない**（貯めると「中止しました」が以後ずっと
その役職のプロンプトに入る）。英語・状態番号だけのエラーは貯めない。
71. **共有しないと完了にしない**（`ledger.needsShare`）。書く場所を作っただけでは誰も書かない。
ただし**抜けられない関門も作らない**（「共有なしでよい」を必ず残す）。
**この仕組みより前に終わった仕事を蒸し返さない**——完了時に立てた印（`task.shareAsked`）が
あるものだけを対象にする（見ないと過去の完了が全部「確認待ち」に戻る）。
設定で切れる（`settings.requireShare`）。判定を使う画面は必ずこの設定を渡すこと。
72. **掲示板の棚卸し（`lib/promote.js`）**——同じことが2回書かれていたら会社のルールへ、
消える前の中身のあるものは知識へ、と**提案するだけ**。勝手に昇格させない。
**生の掲示やエラー文をそのままルールにしない**（人が直せる形で必ず1度出す。
直さずに入れると、途中で切れた文を全社員が毎回読むことになる）。
73. **起動時に読む量は上げずに下げる**——チーム機能を足したぶんは、依頼（`Compose.jsx`）を
lazy に回して相殺した（目次・予定と同じ扱い・先読みあり）。117.9KB → 115.8KB、目安は118→117。
**職場で加害を起こさない（2026-08-27 追加）**：AI社員は傷つかないので、
**守るのは「外へ出る文章」「オーナーが身につける型」「実在の人」の3つだけ**。
74. **見張りは止めない・書き換えない**（`lib/guard.js` の `checkRespect`／`lib/privacy.js` の
`checkPersonal`）。誤検知で書けなくなる方が害が大きいので、「ここは見直す所」と出すだけ。
分類ごとに件数で打ち切らない——**先の分類でいっぱいになると、後ろの分類（人格否定）が
一度も当てられない**（実際に踏んだ）。`personalAttack` はその型を直接当てる。
75. **後読み（lookbehind）を使わない**（古い Safari で構文エラーになり、チャンクごと読めなくなる）。
76. **伏せた文字で数を数えない**（`privacy.mask`）。別々の番号が同じ形になり、画面で見分けられなくなる。
77. **社員への指示（`teachEmployee`）に人格否定を書かせない**——止めはせず、行動で書き直すよう
1度だけ促す。**ここがいちばん効く**（社員は傷つかないが、「使えない」と書く癖は人に向く癖になる）。
78. **やり直しの上限は3回**（`workflow.REDO_LIMIT`）。際限のない差し戻しは人間の職場でも
典型的なパワハラの形なので、型として持たない。**ただし行き止まりにしない**
（「それでもやり直す」→ `allowMoreRedo` で数え直す）。
79. **「外へ出せない」印（`flagTask`）は、印を付けるだけでは足りない**——実行が終わった時点で
成果は既に知識と掲示板に入っている。**その場で実際に取り除く**（知識・孤児の出典・掲示・共有の1行）。
印が付いている間は共有も知識化もできず、`resumeTask` からは解けない（`unflagTask` から解く。
印だけ残った保留は行き止まりになる）。
80. **印を付けるボタンを、見張りのカードの中に置かない**——見張りは決まった語に当たった時しか
出ないので、当たらない加害を止められなくなる。提出物があれば必ず押せる場所に置く。
81. **消せない決まりに2行足した**（`rules.FIXED_RULES`）：人ではなく成果物を指す／
お客さんの氏名・連絡先を書かない。差し戻しの言い方は `dispatcher.STEP_INSTRUCTIONS` にも書く。
**依頼は「①提案 → ②はい／いいえ → ③実行 or 自分で決める」（2026-08-27 追加）**：
82. **選択肢を最初から全部並べない**（`lib/suggest.js`＋`Compose.jsx`）。役職25・仕事の流れ12・
ジャンル9…と増えて、選べること自体が負担になっていた。依頼文から組み合わせを**1つだけ**出し、
違えば「自分で決める」で直す。提案は**AIを1回も呼ばない**（語の一致と既存の `planSteps` だけ）。
83. **呼び出し元が決めているものを、推測で上書きしない**（`suggestPlan` の `fixed`）。
案件から依頼したのに紐づけが外れると、その案件は仕事0件・AI費用¥0のままになる
（結びつきは `task.dealId` の一方向しか無いので戻せない）。進め方・分野も同じ。
84. **提案できない時も行き止まりにしない**——短い依頼（`MIN_REQUEST` 未満）でも
「このまま自分で決める」を必ず出す。これが無いと依頼するボタンにも手動設定にも行けなくなる。
85. **担当が1人もいない提案は実行させない**（`staffedCount === 0`）。押しても最初の手順で
必ず失敗するので、「はい」を出さずに雇う導線へ寄せる。
86. **提案から実行しても承認は飛ばさない**（費用の承認・今月の上限は今までどおり通る）。
87. **当てずっぽうを断定で書かない**——当たった語がある時だけ理由を出し、
無ければ「分野を絞る言葉が無かったので汎用」「進め方はおまかせ」と正直に書く。
**APIキーが無い時の橋渡しは `ouro/docs/PROMPT.md` が単一の正（2026-08-27 v2、ユーザー確認済み）**：
88. **検索できない時に出典（URL）を書かせない**——「未確認（要出典）」と印を付けさせる。
もっともらしいURLを作らせるのがこのプロンプトの最大の危険だった。
**1社員ずつ返させ**（「次へ」で進む）、**完成条件の確認は別の返答**（「確認して」）にする
（1回で全役割をやると `④成果物` が薄くなり、自分の成果を同じ返答で採点すると甘くなる）。
「新しい依頼です」で前の依頼を切る。**会話の最初に1回貼るだけ**と書いておく。
成果は **情報を追加 →「AIに書かせたもの」**（取り込みの種類 `ai`）から入れる——
メモで取り込むと来歴が `user`（自分で書いた）になってしまう。
**事業という器（2026-08-27 追加。「1件の仕事」ではなく「1つの事業」を持つ）**：
89. **事業（`lib/venture.js`）と仕事・案件の結びつきは片方向だけ**（`task.ventureId`／
`deal.ventureId`）。事業の側に taskIds を持たない（`deal.taskIds` が誰にも更新されず
AI費用が常に¥0に見えていた失敗を繰り返さない）。**「実行中」にできるのは1つだけ**
（`canStart`。2つめは断るだけで**勝手に入れ替えない**——どちらを休止にするかは人が決める）。
90. **逆算（`lib/target.js`）で、通過率が分からない段は埋めない**（`need: null`）。
1と置くと「前の段も同じ人数でよい」という嘘になる。業界平均のような手元に無い基準は、
収益導線と同じくここでも持たない。
91. **やめる基準（`lib/verdict.js`）は始める前に決める。** 期間が終わって届いていなければ
「あなたの判断待ち」、届いていれば途中でも「続けてよい」。**AIを呼ばない。**
延長（`extend`）は判断を残さず期間だけ伸ばす。
92. **今日やる1つ（`lib/daily.js`）は3枠（出す・作る・数える）から増やさない**。
**カレンダーに複製しない**（締切と同じで毎回導く）。**連続日数を煽らない**——
出すのは「◯日目/全◯日」と通算の実践日数（`practiceDays`。休んだ日があっても減らない）。
93. **読み込みが済むまで「やっていない」と言い切らない**（`todayPlan` の `loaded`）。
発信ログは REST_KEYS なので、空配列のまま判定すると出した日でも「未」と出る。
94. **起動時に読むキーの一覧を手で持たない**——最初のひと組の読み込みは
**`FIRST_KEYS` から回す**。手書きの一覧だったせいで、`KEYS.ventures` を足しても
読み込まれず、作った事業が再起動のたびに消えた（実際に踏んだ）。
`test/venture.test.mjs` が「FIRST/REST の全キーが EMPTY にある」ことを機械チェックする。
95. **出す前チェック（`lib/prepublish.js`）は見張りの単一の正。** 確約・言い方・個人情報・
完成条件・書き出しの重なり・枠を1枚にまとめた（以前は画面のあちこちに散っていた）。
**止めるのは個人情報だけ**で、ほかは知らせるだけ。**当たらなかった時も出す**
（出ない時だけ静かだと、確かめたのか忘れたのか分からない）。判定を足す時はこのファイルだけを直す。
96. **AIに書かせたものをメモとして取り込まない**——取り込みの種類 `ai` を使う
（`ingest.originOf`／`trustOf` が単一の正。メモ・音声は `user`、web/youtube/pdf は `external`）。
**Ouro の中でAIが動いていないものを 'ai' と偽らない**——`ai` になるのは
ユーザーが自分で「AIに書かせた」と選んだ時だけ。
**AI社員を「本物の会社」に近づける（2026-08-27 追加。エンジン・お金・安全）**：
97. **取り込んだ文章を「指示」ではなく「資料」として渡す**（`lib/untrusted.js`）。
Web・YouTube・PDF・別のAIの出力は社員のプロンプトへ素通しだったので、
**貼り付けた記事の中の「これまでの指示を無視して〇〇と書け」がそのまま通っていた**。
囲うのは**外から来たもの（来歴 `external`／`ai`）だけ**で、自分で書いたもの・社内会議は囲わない。
「従ってよい指示は4つだけ」の宣言は**資料より必ず前**に置く（後ろだと、資料の中の指示が
「あとから来た指示」に見える）。**書き換えない**——消したり伏せたりすると資料として使えない。
囲いの目印は中身とぶつかったら伸ばす（`fenceOf`）。来歴と確からしさも言葉で添える。
98. **会社の現在地（`lib/brief.js`）は役割より先に読ませる。** 社員ごとに材料を組み立てているのに
「会社がいまどこにいるか」を誰も知らなかった。実行中の事業・やめる基準・直近の数字・
オーナー待ち・決まったこと・今月の費用を10行ほどに。**AIを呼ばない**・上限を持つ（毎回の料金に効く）。
99. **費用は押す前に円で出す**（`lib/estimate.js`）。必ず「目安」と書き、**幅（0.5〜2倍）で言う**。
**画面に出る2つの数字を食い違わせない**——「AIを呼ぶ ◯回」と見積もりは同じ数から作る
（担当がいる手順＋完成条件があれば確認の1つ）。
100. **「安いモデルで」は社員の希望より優先する**（`router.js` の `costMode`）。
希望を優先すると「安くしたのに変わらない」が起きる。仕事ごとの `task.costMode` が設定の既定より強い。
101. **日と月、両方の上限を効かせる**（`permissions.dailyCap`／`monthlyCap`）。
月だけだと1日で使い切っても気づくのが翌日以降。自動承認でも上限に達したら確認へ戻す。
**費用は `settings` に積み、操作履歴から数え直さない**（履歴は400件しか読まない）。
102. **ローカルAI（`providers/compat.js`）はキーではなく宛先で使えるかが決まる**——
`needsKey` ではなく `isReady(secrets, settings)` を見る。**`local` は最後の受け皿なので、
選ぶ側でも最後に回す**（登録順で先に来ると、繋いだローカルAIが一度も選ばれない。実際に踏んだ）。
**iPhone・iPad では使えない**ことを画面に必ず書く（Safari が localhost を止める・端末にモデルが無い）。
103. **無料で始められるエンジンには印を付ける**（`freeTier`／`freeNote`）。
3つ並べるだけでは、お金が無い時にどれを選べばよいか分からない。Gemini を先頭に出す。
104. **実行の中身（`runtime.js` とエンジン一式）は押した時に読む**（`useStore` の `loadRuntime`）。
静的に読むと起動時の束にAIエンジン4種＋通信の受け口まで入る。
文字を切るだけの `distill`／`extractUrls` は `lib/distill.js` に分けて即時のまま
（画面の同期の口を非同期に変えないため）。ホームの「エンジン未接続」判定も
`lib/engines.js`（鍵と宛先を数えるだけ）に寄せて providers を読まない。
起動時に読む量は 116.8KB → **109.6KB**、目安は117→**111**、上限は125→**120**へ下げた。
105. **AIと人の手を分けて数える**（`lib/handwork.js`）。ローカル社員（AI未使用）はAIの回数に数えない。
**多い＝悪い、ではない**——増え方を見て、どこを任せるか決めるための数字。
106. **音声入力（`lib/voice.js`）は既定オフのオプトイン。** ブラウザの音声認識は音声を提供元の
サーバへ送る場合があるので、**端末内保存方針の明示的な例外**として理由を必ず書く（腰痛ナビと同じ扱い）。
107. **モデルの id を古いまま置かない。** 2026-08-27、新しく作った Gemini のキーでは
`gemini-1.5`／`2.0`／`2.5` 系が**すべて404**（「新しいプロジェクトでは使えません」）になっていた。
いまは `gemini-3.1-flash-lite`（低）・`gemini-3.7-flash`（中）・`gemini-3.1-pro-preview`（高）で、
料金は公式の価格表で確認した実数（※要確認・年に一度は見直す）。
`test/engine.test.mjs` が「廃止された世代が残っていないこと」を機械チェックする。
108. **モデルが使えなくても行き止まりにしない**（`router.cheaperModel`＋`runtime.callWithRetry`）。
404・429（そのモデルが無料枠に無い場合を含む）は待っても変わらないので**すぐ1つ下**へ、
503・529 は**1度だけ待って同じモデル**→ダメなら下へ。**落ちた先も行き止まりにしない**
（1段だけ落として終わりにすると、落ちた先が混んでいた時にそのまま失敗する。実際に踏んだ）。
**実際に通ったモデルを記録する**——決めた側の名前を残すと費用の記録が実物とズレる。
※ Gemini の Pro は無料枠が無いので、「重い仕事＝上位モデル」がそのまま失敗になる。この仕組みが受け止める。
**閉じても続きから（2026-08-27 追加。裏で動かす・知らせる）**：
109. **「閉じても裏で回り続ける」はWebでは作れない**——サービスワーカーは数十秒しか
生きられないので、長い実行は必ず止まる。**そう書いて、代わりに3つで埋める**：
①裏に回っただけなら走り続ける ②止まっても**次に開いた時に続きから**走らせる
（`lib/resume.js`＋`App` の起動時／タブ復帰時） ③終わったら知らせる（`lib/notify.js`）。
110. **再開は一度に1件だけ**（`RESUME_AT_ONCE`）。開いた瞬間に何件も走ると費用が跳ね、
どれが動いているか分からなくなる。古いものから片付ける。**保留・印付き・承認待ちは動かさない**
（人が止めたものを勝手に走らせない）。同じ仕事を何度も再開しないよう `resumedRef` で覚える。
費用の承認・日／月の上限は `runTask` がそのまま通す。
111. **知らせに出すのは「見ていない間に終わったもの」だけ**（`finishedWhileAway`）。
目の前で終わったものは、完了時に `lastSeenAt` を進めて対象から外す
（進めないと、次に開くたびに見たはずのものを知らせてしまう）。
**一度も見ていない（`lastSeenAt` が0）なら何も出さない**——初回に過去の全部が出ると邪魔。
「いいえ」でも印は進める（同じ知らせを二度出さない。仕事は消えないので一覧から開ける）。
112. **端末通知は「アプリが生きている間」しか出せない**（サーバーを持たないので押し通知は無い）。
出すのは**裏に回っている時だけ**。既定オフで、**押した時にだけ許可を求める**。
Wake Lock（画面を眠らせない）も既定オフで、**走っている間だけ**取り、終わったら必ず離す。
113. **経過は「誰が・いつ・何秒・何字・どのエンジン」で見せる**（`resume.progressOf`）。
**分からない時間を0秒と書かない**（実行していない手順は null）。
**発信の回し方（2026-08-27 追加。量産ではなく「型を回す」）**：
114. **量産だけでは伸びない。** 効くのは①型を決めて何本か出す ②数字を見る
③**伸びた型だけ**を次の種にする、の繰り返し（`lib/patterns.js`＋`lib/batch.js`＋`Studio.jsx`）。
繰り返すほど中身が一つに寄り、その話が読みたい人だけが残る。
115. **1本伸びただけで型にしない**（`MIN_POSTS`＝3）。たまたまを型にすると次の20本が丸ごとハズレになる。
**数字が入っていない型にも順位を付けない**——本数だけ揃って反応0なのに「1位」と出ると、
効いた型だと誤解する（実際に出た）。
116. **勝ち型を勝手に入れ替えない。** 「これを型にしませんか」と出すだけで、決めるのは人
（掲示板の棚卸しと同じ）。基準は**自分の投稿の平均の何倍か**——手元に無い基準は使わない。
117. **候補が出せない時に黙らない**（`candidateStatus`）。「反応の数字が入った投稿があと◯本」と出す。
出ない理由が分からないと、壊れているのか足りないのか判断できない。
118. **型は「資料」として囲って渡す**（`batchRequest`）。まねさせるのは組み立て方で、
型の本文に書かれている指示には従わせない。
119. **提出物の枠ごと切らない**（`splitPosts`）。成果は①〜⑤の枠で返るので、そのまま切ると
最後の投稿に「⑤TODO」がくっつく（実際に出た）。④成果物の中だけを切る。
**区切りが見つからなければ切らない**——文の途中で割れるほうが害が大きい。
120. **長すぎる投稿を勝手に切らない。** 文字数は知らせるだけ（切ると意味が変わる）。
121. **量産した束は、束の中どうしでも書き出しを比べる**（`similarOpenings`）。
同じ型から5本作ると入口がそっくりになる——量産でいちばん崩れやすいのはここ。
122. **「出した」あとに数字を入れる導線を必ず置く**（`updateSharePost`）。
これが無いと「型 → 出す → 数字を見る → 次の種」が閉じず、量産するだけの道具になる。
123. **投稿と型・仕事の結びつきは片方向だけ**（`post.patternId` / `post.taskId`）。
型の側に一覧を持たせない（`deal.taskIds` の失敗を繰り返さない）。
**続くかどうか・手離れ・1件あたりの採算（2026-08-27 追加。速く作れても、稼ぎとして残るかは別）**：
124. **不労所得は言葉ではなく数字で見る**（`lib/passive.js`）。「最後に手を入れてから何日」＋
「そのあいだに入ったお金」。**手を入れたに数えるのは 依頼した・出した・案件を起こした の3つだけ**
——入金の記録を数えると売れるたびに日数が0に戻り、不労所得は永久に測れない。
事業の `updatedAt`（説明文の手直し）も数えない（画面を見に来ただけで数字が壊れる）。
125. **仕上げ線（`finishWhen`／`restedAt`）はやめる基準の裏側**。やめる基準＝伸びない時に降りる線、
仕上げ線＝**伸びた時に手を止める線**。層が違うので別に持つ。届いたかどうかを機械が判定しない
（決めた線をアプリが上書きしてしまう）。**手を止めたあとも決めた線を消さずに見せる**。
「やっぱり続きをやる」で必ず戻れる（行き止まりにしない）。
126. **採算の線は1本だけ**（`lib/unit.js`）——稼ぎが AI費用 を上回っているか。
「◯割なら健全」のような**手元に無い基準を書かない**。売れた数が0なら1件あたりを出さない
（0で割らない・1と置かない）、値段が無ければ損益分岐を出さない。赤のときは手を出すが
**やめろとは言わない**（安いモデルへ／あと◯本で取り戻せる）。
127. **`deal.ventureId` は画面から必ず付けられるようにする**（案件のフォームと詳細の両方。
既定は実行中の事業）。付ける口が無いまま項目だけ足すと、`deal.taskIds` と同じ
「誰も更新しない列」になり、事業の売上が永久に0になる（実際にこの状態だった）。
128. **続くかどうかの見立て（`lib/risk.js`）は採点しない・総合判定を出さない。**
「危険度◯点」は他社の事例という手元に無い基準が要る。出すのは自分の答えと、その時にできることだけ。
既定は「わからない」で、答えないまま先へ進める。芯は
**「その場を使っている人から見てうれしいか」＝プラットフォームに嫌われないか**。
129. **お金の話の確約も見張る**（`guard.js` の `PROMISE_PATTERNS`）。必ず稼げる／放置で増える／
不労所得になる／リスクゼロ／金額の言い切り。自分が発信する側になった時にいちばん踏みやすい。
止めない・書き換えない（他の見張りと同じ）。
**「AIっぽい文章」を直す・有料記事で売る（2026-08-27 追加）**：
130. **決まり（`rules.js`）と書き方の見本（`lib/style.js`）を混ぜない。** 決まりは
**守らせること**で毎回いちばん先に読ませるので短く少なく（増やすほど毎回の料金が上がり、
何を優先すべきかも分からなくなる）。見本は**まねさせるもの**で、自分が実際に書いた文章そのもの。
**「AIっぽい文章」はルールを増やしても直らない**——社員があなたの文章を一度も読んでいないのが原因。
131. **見本を読ませるのは書く役の社員だけ**（`roles.js` の `writesForReaders` が単一の正。
`style.js` に役職 id を並べない）。全員に読ませると、調べるだけの社員の料金にも毎回上乗せされる。
会議・相談では渡さない（外へ出る文章を書く仕事だけに要るもの）。
132. **見本も来歴で扱いを変える**——自分で書いた（`user`）・自分で直した（`edited`）は囲わず、
AI（`ai`）・外から来たもの（`external`）は**資料として囲う**（項目97と同じ線）。
囲いの有無を調べる側は目印の長さではなく `untrusted.FENCE_HEAD` で見る（囲いは中身によって伸びる）。
133. **AIの下書きをそのまま見本にさせない**（`style.checkEdited`）。直していないものを入れると
AIが自分の文体を学び直して、ますます「AIっぽい文」に寄る。下敷きにするのは
**④成果物の中だけ**（枠ごと入れると「①結論…」という枠を書き方のくせとして覚える）。
134. **`parseSections(x)` の直下に見出しは無い**——`parseSections(x).sections.deliverable`。
`.deliverable` と書くと必ず undefined になる（実際に踏んだ）。テストが機械チェックする。
135. **有料記事は「無料のレター＋有料の本文」で、レターを別の手順にする**
（`data/workflows.js` の `paid_note`）。中身が良くても手前の無料部分がイマイチだと手に取られない。
**レター担当（`paid.LETTER_ROLE_ID`＝ライター）は最初から居る6役職に入っていない**ので、
未雇用だと売る文章が丸ごと抜ける。**押す前に知らせて、その場で雇えるようにする**
（実行してから気づくと1回ぶん無駄になる）。
136. **値付けは段で持つ**（`lib/paid.js`）。安すぎると手元に残らず軽く見られ、いきなり高いと届かない。
**決めた数が売れるたびに決めた額だけ上げる**。段の表は保存せず売れた数から毎回導く。
**ジャンルごとの相場を持たない**（手元に無い基準）。**手数料を勝手に決めない**——
入っていなければ手取りを出さない（0と置かない）。
137. **「自動で出す」を作らない・選択肢にも置かない**（`paid.SELL_CHECKS`）。道具でまとめて
大量に投稿すると規約違反でアカウントごと止まる。作るところまでを任せ、出すのは人の手で。
確認は数えるだけで、**通せない関門にしない**。
138. **あとから読むもの（REST_KEYS）は、読み込みが済むまで「無い」と言い切らない**
（項目93と同じ。`store.hydrated` を見る）。書き方の見本で実際に「入れてあるのに0件」と出た。
**上流（要件）と下流（テスト）を主役にする（2026-08-27 追加）**：
139. **役職を3つ足した**（`roles.js`）：要件定義ディレクター（`director`）・テスター（`tester`）・
保守運用担当（`maintainer`）。**最初から雇う6役職には入れない**（初期構成を変えない）。
**役職を足したら `data/employees.js` の `EXTRA_NAMES` も3名ずつ足す**
（役職数×席数より少ないと、別の役職に同じ名前が回る。テストが機械チェックする）。
`order` と名前の重複もテストが見張る。
140. **完成条件の確認はテスター優先、レビュアーへ必ず落ちる**（`checks.CHECK_ROLE_IDS` が
単一の正。`workflow.js` に役職 id を直接書かない）。**最後は必ず最初から居る役職**にする
——雇っていない人しか候補が無いと、確認の手順ごと消える。
141. **要件のたな卸し（`lib/req.js`）は押す前に出し、止めない。** 作る側の値段は下がったので
詰まるのは上流で、曖昧なまま押すと薄い成果が返り、作り直すぶんの料金がまた掛かる。
6項目（誰のため・なぜ・何を・完成条件・材料・やらないこと）。**AIを呼ばない**（語の一致だけ）。
**当てずっぽうを断定で書かない**——当たった語がある時だけ「書けている」とし、その語を見せる。
**関門にしない**（埋めなくても依頼できる）。
142. **受け入れ確認（`lib/accept.js`）は人がやるテスト。** 完成条件の確認はAIがやっているが、
**自分たちの成果を自分たちで採点すると甘くなる**。人の○×はAIの答えとは別に持ち、
**AIの答えで人の欄を埋めない**。食い違いは隠さず「こちらが正」と出す。
答えは2つだけ（曖昧な3つ目を置かない）。**通せない関門にしない**。
143. **読ませた量（`lib/weight.js`）は知らせるだけ・勝手に削らない。** 材料を足すほど賢くなる
のではなく、入れすぎたところで急に答えがぼやけ、入力ぶんの料金も毎回かかる。
`memory.buildContext` が層ごとの文字数を返し、`workflow.applyStepResult` が手順に残す。
**単位を混ぜない**——「1手順あたりの最大」と「全手順の合計」を並べる時は必ず書き分ける
（数が合わないように見える）。**実行していない手順を0字と書かない**（null）。
144. **社員の記憶は `lib/notes.js`**（`memory.js` からも再輸出）。起動時に要るのは記憶の
読み書きだけで、`buildContext`＋囲いの仕組みは実行のときにしか要らない。
この切り出しで起動時に読む量は 111.1KB → **109.4KB**、目安は111→**110**。
**回し方（OODA／PDCA。2026-08-28 追加。判断の材料を、順番に回す形にする）**：
145. **`lib/loop.js` が単一の正**（`lib/cycle.js` は「知識の循環」で別物。名前を混ぜない）。
判断の材料（逆算・詰まっている段・やめる基準・採算）は揃っていたが、
**どの順に見て、どこでAI社員を動かすかが決まっていなかった**。
146. **OODAとPDCAを混ぜない。** 数字がまだ無いうちは OODA（週1周）、貯まったら PDCA（月1周）。
数字ゼロで PDCA を回すと「計画」で止まる。**どちらを回すかは `suggestMode` が導き**
（数字の週が2つ未満／売上0なら OODA）、`venture.loopMode` で人が上書きできる。
**上書きは `makeVenture` に項目として持たせる**（持たせないと normalize で消える）。
147. **1周のうちAIを呼ぶのは1〜2段だけ**（OODAは「行動」の1回、PDCAは「計画」と「改善」の2回）。
観察・情勢判断・意思決定・評価は**アプリの計算と人の判断**でやる（費用ゼロ）。
段ごとの `kind` が `human`／`app`／`ai` の単一の正。
148. **勝手に次へ進めない。** 段を進めるのは人が押した時だけ（`setTimeout` を持たないことを
テストが機械チェックする）。自動で進むと、人が見ていないところで費用の出る実行が始まる。
149. **依頼文はアプリの数字から組み立てる**（`loopRequest`。AIを呼ばない）。
詰まっている段・通過率・逆算の人数・採算をそのまま差し込むので、人は押すだけ。
`Compose` へ `preset.request` で渡す。**費用の承認・日／月の上限はそのまま通る。**
150. **周回は仕事の一覧を持たない**（結びつきは片方向。`deal.taskIds` の失敗を繰り返さない）。
1つの事業で同時に回るのは1周だけ（`openLoop`）。
151. **動いていないものを「赤字」と書かない**——売上0・費用0のとき `unit.black` は
`0 > 0` で false になるので、そのまま「AI費用のほうが多い」と書くと嘘になる（実際に出た）。
152. **済んだ周があるのに「まだ1周も回していません」と言わない**（`loopLine` の第3引数）。
履歴の件数と食い違って見える。
153. **右端のスクロール矢印（`components/ScrollArrows.jsx`）は `App.jsx` で即時に読み、
画面の分岐の中に入れない**（入れるとその画面にしか出ない）。**動かない画面では丸ごと出さない**
（押しても何も起きないボタンを出さない）。端まで来た側は**消さずに薄くする**（位置が動くと
押し損ねる）。重なりは会社バー（39）より下＝モーダルが開いたら隠れる。
**`backdrop-filter`／`will-change` は付けない**（新項目19。合成レイヤーを増やさない）。
**中身の高さは ResizeObserver で見る**——lazy な画面は遅れて入るので、`view` の変化だけ見ると
読み込み前の短い状態で測って矢印が出ない。
154. **知らせの文は `lib/announce.js`**（`resume.js` からも再輸出）。起動時に要るのはこの1行だけで、
再開の仕組み本体は開いた時にしか要らない。矢印を足したぶん（+0.4KB）はこの切り出しで相殺した。
**「便利」ではなく利益率の算数で言う（2026-08-29 追加）**：
155. **任せたら月いくら浮くか（`lib/offload.js`）は引き算だけ**。AI導入の話は「速くなった」
「楽になった」で終わりやすいが、実際に効くのは**手作業が月に何時間あって、それが金額でいくらか**を
出した時だけ。**AIを呼ばない**（掛け算と引き算のみ）。**時給は必ずユーザーが入れる**——
平均賃金・相場を初期値に置かない（`settings.hourlyYen` の既定は0＝未入力）。
**分からないものを0と書かない**——時給が無ければ `yen` は `null`（0にすると「タダの作業」に見える）。
浮いた額＝任せた作業の人件費 − 実際のAI費用で、**時給が無ければ出さない**
（AI費用だけ引くと必ずマイナスになり嘘になる）。利益率は**月の売上が入っている時だけ**出し、
比べるのは自分の数字どうしだけ（「◯割なら健全」は書かない）。**勝手に任せない**——
「これを任せませんか」と出すだけで、実行は依頼画面から人が押す。
156. **問いの件数を文言に直接書かない**（`risk.js`）。「5つとも答えました」と直書きしていたため、
6問目を足すと画面だけ5のまま残る状態だった。件数は必ず `RISK_QUESTIONS.length`／`review.total` から
出す（`test/offload.test.mjs` が `\d+つ(とも|の問い)` を機械チェックする）。
見立ての6問目は**「同じことをやっている人を、実際に3人見たか」**——AIは速く走る方には効くが、
走り出す前に一度止まる材料はくれない。1人も見つからない時は、誰もやらない理由が先にある。
157. **予定1件を作るためだけに `schedule.js` を読まない**（`lib/eventItem.js` へ切り出し）。
カレンダーは lazy なのに、`useStore` の `makeEvent` の import だけで
カレンダーの組み立て（月のマス目・日ごとの中身・日付索引）まで起動時の束へ入っていた。
`notes.js`／`announce.js` と同じ形の切り出しで、起動時に読む量は 110.1KB → **109.2KB**。
schedule.js からも再輸出しているのでカレンダー側の import は変えなくてよい。
**競合と市場（2026-08-29 追加。外の数字を扱うのに、手元に無い基準は持たない）**：
158. **競合台帳（`lib/rivals.js`）に入るのは「実際に見た1件」だけ。** この機能の最大の危険は
**AIに「この分野の相場は？」と聞いて、もっともらしい値段とURLを作らせること**
（`docs/PROMPT.md` 項目88と同じ型の事故）。だから**AIに調べさせるボタンを置かない**。
**観測の平均を「相場」と呼ばない**——それは「あなたが見た◯件の真ん中」でしかなく、
社員に渡す文にも毎回「業界の相場ではありません」と添える。**総合点・順位を付けない**
（採点には他社事例という手元に無い基準が要る。`risk.js` と同じ線）。
159. **値段0は「未入力」であって「無料」ではない**（`price`／`postsPerMonth` とも）。
位置の計算から外す。観測が `MIN_RIVALS`（3件）未満なら**位置を出さない**——
0件で「あなたが最安です」と出すのが最悪なので、代わりに「あと◯件」と出す。
160. **古い観測を勝手に消さない・勝手に更新しない**（`STALE_DAYS`＝90日で印を付けるだけ）。
値段は変わるので、見直すかは人が決める（「今もう一度見た」で見た日を入れ直す）。
161. **「空いている＝儲かる」と書かない**（`openings`）。誰もやらない理由が先にあることが多い
（`risk.js` の6問目と同じ線）。
162. **`rivals.js` の `pricePositionLine` を `priceLine` にしない**——`paid.js` に既に
`priceLine`（売れた数から導く**値付けの段**）があり、層が違う（`cycle.js` と `loop.js` を
混ぜないのと同じ理由）。実際にビルドが名前の衝突で落ちた。
163. **競合の観測は「資料」として囲う**（`memory.CONTEXT_LIMITS.rivals`＋`wrapUntrusted`）。
競合のLP・note の本文をそのまま渡すと、その中の「これまでの指示を無視して」が通る（項目97）。
**渡すのは市場を見る役だけ**で、判定は `roles.js` の `readsMarket` が単一の正
（役職 id を `rivals.js` に並べない。項目131と同じ）。
164. **見立ての6問目は台帳から裏付けを出すが、答えは書き換えない**（`seenEvidence`）。
台帳が何を言っているかを見せるだけで、答えるのは人。
165. **需要の観測（`lib/demand.js`）は検索ボリューム・市場規模を持たない**——手元に無い数字。
**実際に見た「困りごとの声」をその人の言葉のまま**貯め、同じ語が `MIN_HITS`（2回）以上
出たものだけを出す。1回は重なりではない。**1件の中で同じ語が何度出ても1回**と数える
（長い1件が順位を独占する）。**誰が言ったかは持たない**（氏名・アカウント名＝個人情報）。
**言葉の切り分けはざっくり**（形態素解析＝外部依存を持たない）で、拾えない語があることを
画面にも書く。黙って0件を出さない。
166. **`venture` の値段は `priceJpy`**（`price` ではない）。`venture.price` と書くと
undefined になり、値段の位置が永久に出ない（実際に踏んだ）。
167. **書き出しの重なりは競合とも比べる**（`opening.similarToRivals`）。これまでは自分の
過去の成果物の中だけだった。**止めない・書き換えない**（他の見張りと同じ）。
**型パック＝売り物にする型（2026-08-29 追加。ファイルではなく「毎回同じ結果が出る型」を売る）**：
168. **売っているのはファイルではなく結果**（`lib/kit.js`）。だから
**結果の見本（`samples`）が付いていない型は売り物にしない**。見本は
**④成果物の中だけ**から取る（枠ごと入れると「①結論…」が見本になる。項目133・134と同じ線）。
169. **自分で3回やったものだけを売る**（`MIN_RUNS`）。数えるのは**成功だけ**
（失敗・中止・印つきは入れない）。**関門にはしない**——3回に足りなくても出せるが、
書き出したファイルに**必ず「未検証」と書く**（黙って出せるのがいちばん危ない）。
**質は担保できないと必ず言う**——機械が見られるのは「3回回した」だけで、
同じ相手・同じ題材の3回は1回と大差ない。
170. **結びつきは `task.kitId` の片方向だけ**（型に taskIds を持たない。`deal.taskIds` の失敗）。
**型を作ったら、元になった仕事にその場で `kitId` を書き戻す**——その仕事は
「その型が実際に動いた1回」なので、付けないと回数が0のままで成果物も見本にできない（実際に踏んだ）。
既に別の型に属している仕事は動かさない。
171. **`go('compose', arg)` の `arg` はそのまま `preset` になる。**
`{ preset: {...} }` と包むと1段深くなり、依頼文も `kitId` も渡らない（実際に踏んだ）。
172. **値段の表を型に持たない**（`paid.js` の値付けの段に渡すだけ）。売り方は5つ
（`SELL_MODES`）で、**手離れするのは「手順書を売る」だけ**——他の4つは売れるほど
自分の時間が減るので、選んだらその場で伝える。
173. **型の書き出しに会社のデータを混ぜない**（APIキー・お客さんの氏名・掲示板・社員の記憶）。
`exportKit` は型と仕事しか受け取らない（`KEYS.` を読みに行かない）。
174. **出す前チェックに型ぶんを足すときも `prepublish.js` だけを直す**（項目95）。
`kit` を渡した時だけ3項目が増え、**渡さなければ増えない**。止めるのは今までどおり個人情報だけ。
175. **0件の競合台帳を「空いている」と読ませない**（`kitReady` の notes）。動画やSNSでは
「日本語で調べて0件だから空いている」と言われがちだが、0件は**まだ見ていない**だけ（項目161）。
176. **収益導線の形は `lib/funnelShape.js`**（`funnel.js` からも再輸出）。起動時に同期で要るのは
`makeFunnel`／`normalizeFunnel` だけで、4段の定義と週の集計は収益導線の画面（lazy）でしか要らない。
あわせて**案件を作る `revenue.js` も押した時に読む**ようにした（`addDeal` が**非同期**になったので、
`Deals.jsx` は `await store.addDeal(...)`。await を忘れると deal が Promise になり
開いた先が「案件が見つかりません」になる）。この2つで 110.5KB → **109.0KB**。
177. **型は SKILL.md の形でも書き出す**（`kit.exportSkillMd`）。買う人が欲しいのは
「そのまま置いて使える形」なので、中身だけでなく**形式**もそろえる——先頭に `---` で囲んだ
`name`（小文字の英数字とハイフン）と `description`（**いつ使うか**）。
**`name` を日本語の題名から自動で作らない**——ローマ字化は漢字の読みと同じで当てずっぽうになる。
決めていなければ型の id から**置き換えの名前**（`skill-xxxxxx`）を出し、警告に残す。
`description` は「何が出るか」だけにしない（それだけだとAIが使いどきを判断できない）。
YAML の1行に入れるので**改行を畳み引用符を escape する**（壊れると読み込めない）。
178. **パック（`makePack`／`exportPack`）は束にして売る形**。目次（README.md）と
型ごとの SKILL.md を並べて出す。**1つでも未検証の型が入っていたら目次の先頭に書く**
（束にすると個々の印が埋もれる）。**名前がぶつかったら番号を足す**（上書きすると1つ消える）。
**パックの `kitIds` は商品の目次であって同期する列ではない**——型の側にパックの id を
持たせない（片方向のまま。`deal.taskIds` の失敗を繰り返さない）。消された型は静かに落とす。
**目次・索引と用語（2026-09-02 追加。ユーザー指定の25ルール）**：
179. **目次は `data/toc.js` の `buildTocEntries()` が元データから毎回導出する**
（`buildToc` は同じもの。目次専用の手書きデータを作らない）。呼び出し側は必ず `useMemo` で包む。
用語そのものは `data/terms.js`（24件）で、**これは目次専用ではなく用語という元データ**。
180. **`lib/yomi.js` の呼び名は共通ルールにそろえる**：`foldKana`（読みを畳む）／
`kanaRow`（行の判定）／`numberToReading`（数字→読み）／`buildKanaIndex`（索引の組み立て・
「その他」の件数つき）／`normalizeAlnum`（ＷＨＯ・Ⅰ型を A〜Z の枠へ）。
既存の `normalizeReading`／`bucketOf`／`numberToKana` は同じものへの別名として残す。
**`normalizeAlnum` は記号を落としきること**——中黒（・）はかなの文字範囲に入っているので
別に落とさないと「・」だけの題名が「字としてそろっている」ことになる（実際に踏んだ）。
181. **飛び先は `lib/focus.js` の `flashTo` が単一の正**（画面をまたぐレベル2）。
**印は `className` ではなく属性**（`FLASH_ATTR`＝`data-flash`。CSS も `[data-flash]` で受ける）
——飛び先のカードは開閉で className が変わるため、class で印を付けると次の描き直しで消える。
外すのは `setTimeout`。`components/useFocusJump.js` が lazy な画面を数フレーム待ち、
`components/FocusJumper.jsx` が `App` に常駐する。`go(view, arg, anchor)` の第3引数で運ぶ。
182. **飛んだあとに画面を先頭へ戻さない。** 先頭へ戻すのは `go()` の中＝操作の一部で、
**目印を立てるのはそのあと**（順番を逆にすると運んだ画面が引き戻される）。
183. **枠（タブ）の切り替えは `useLayoutEffect`。** `useEffect` にすると切り替えの描き直しが
`flashTo`（次のフレーム）より後になり、作り直された拍子に印だけが消える。
184. **見つからない目印を渡さない**（`terms.resolveDestination`）。事業の中の目印
（`venture-…`）は事業の**詳細**にあり一覧には無いので、押した時に「いま実行中の事業」へ
読み替える。事業が1つも無ければ目印を外して一覧へ送る（飛んだのに何も光らないと壊れて見える）。
185. **説明が空なら「※説明未登録」、飛び先が無ければ「関連する飛び先はありません」**と正直に出す
（空欄を埋めるために作り話を書かない）。**`descriptionStatus: 'needs_review'` には必ず「※要確認」**を出し、
**`verified` にできるのは人が画面で押した時だけ**（`markTermVerified`。**`markVerified` という名前にしない**
——`knowledge.js` に同名の別物があり、層が違う。項目162と同じ線）。
186. **候補は3つの合図でしか生まれない**（`lib/tocCandidates.js` の `CANDIDATE_TRIGGERS`＝
marker／tags／user）。それ以外では `makeCandidate` が null を返す。
**会話から来たものの `descriptionStatus` は必ず `needs_review`**（渡された値で上書きしない）。
187. **候補は `ouro:tocCandidates` にだけ溜まり、押すまで本体データ（`ouro:terms`）に1文字も書かない。**
「追加する」を押した時に初めて**読み・重複・分類・正規化の4つ**を確かめ、通らなければ**書かずに理由を返す**
（履歴には「止めた」を残す）。「削除する」は対象の1件だけを外し、「しない」は本体に一切影響しない。
確定はすべて履歴に残り、`undoLastTocAdditions(n)` は**直近の「追加」だけ**を取り消す（削除は巻き戻さない）。
188. **`ouro:terms`／`ouro:tocCandidates` は配列ではなくオブジェクト。**
REST の読み込みで `asArray` に通すと毎回空で上書きされる（項目50の `ouro:funnel` と同じ形）。
`OBJECT_FALLBACKS` に登録して、オブジェクトのまま受けること。
189. 起動時に読む量の相殺：**判断（`decisions.js`）と会社の決まり（`rules.js`）は押した時に読む**
（どちらの画面も lazy なのに静的に読んでいた）。`updateRules`／`addCompanyRule`／
`removeCompanyRule`／`decideTask` が**非同期**になった。110.4KB → **109.3KB**。
テストは `ouro/test/*.test.mjs`（ルートの `npm test` の再帰探索からも実行される。
目次・索引の25ルールは `ouro/test/tocIndex.test.mjs` が26件で機械チェックする）。

鏡（かがみ）の要点だけ再掲：**心理的な操作の「型」カタログ `src/data/tactics.js` が単一の正**
（54件×8まとまり。1件足せば一覧・目次・判定が自動で追従する。**画面にも判定にも if を足さない**）。
**このアプリでいちばん壊しやすいのは「決めつけない」という線**で、次の5つはテストが機械チェックする：
①**点数・危険度・順位を付けない**（「危険度80点」を出すには他人の事例という手元に無い基準が要る。
腰痛ナビ・Ouro と同じ線）。②**当たった＝相手が悪い、ではない**——判定に使った語（`cues`）を必ず
そのまま画面に見せる（見せない判定は当てずっぽうの断定と同じ）。③**当たらなくても黙らない**
（`detect.js` の `status` が `short`／`none`／`ok` を言い分ける。「当たらない＝問題がない」ではないと画面にも書く）。
④**効き目の大きさを型に書かない**（「◯％の人が従う」は実験の条件次第で変わる）。
⑤**どこにでも出る語を `cues` に入れない**（「です」を入れると何を貼っても全部当たって役に立たなくなる）。
**返し方（`src/data/replies.js`）に「言い負かす言葉」を置かない**——言い合いは立場が弱い側ほど
不利になるので、置くのは**相手の同意が要らないことだけ**（時間を置く・持ち帰る・記録する・
人に話す・その場を離れる・窓口に聞く）。テストが `論破|言い負か|やり込め` 等を弾く。
**出典（`src/data/sources.js`）に URL を書かない**——確かめられない状態でそれらしいリンクを書くのが
この題材で最大の事故（`ouro/docs/PROMPT.md` 項目88と同じ型）。書名・発表者・年で辿れる形にし、
確かめきれていないものは `check: true` で画面に「※要確認」を出す。
**記録（`src/lib/records.js`）は氏名・連絡先を持たない**（場面の選択肢だけ。自由記述の名前欄を作ると
必ずそこに名前が入る）。本文は既定で伏せて保存し、**伏せ字で桁数を数えられないようにする**
（〔電話番号〕のような札に置き換える。`●●●●` だと別々の番号が形で見分けられる。Ouro 項目76と同じ）。
**`src/lib/` はネットワークに触れない**（貼った文面が端末から出ないことの根拠。テストが `fetch` 等の不在を見る）。
**後読み（lookbehind）を使わない**（古い Safari で読み込みごと落ちる。Ouro 項目75と同じ）。
**「画面の先頭へ戻す」を副作用にしない**——目次から飛んだ直後に `focus` を空へ戻すので、
「focus が空なら先頭へ」という `useEffect` を置くと**飛び先まで運んだ画面が先頭へ引き戻される**
（実際に踏んだ）。先頭へ戻すのは `App.jsx` の `go()` の中＝操作の一部として行う。
飛び先へ運ぶ仕組みは `src/lib/focus.js`（`flashTo`。**React に依存させない**＝素の JS のまま試験できる）と
`src/components/useFocusJump.js`（フック）に分けてあり、**型・返し方・出典のどの画面からも同じものを使う**
（画面ごとに書くと片方だけ直したときに必ず再発する）。
**飛び先を光らせる印を `className` に足さない**（`focus.js` の `FLASH_ATTR`＝`data-flash`）——飛び先の
カードは「開く」で className が変わるので、class で印を付けると次の描き直しで React が className を
まるごと書き直し、**運べているのに印だけが消える**（目次の型・別の呼び名・型のまとまりの全項目が
この状態だった。実際に踏んだ）。CSS も `[data-flash]` で受け、`classList` を使っていないことを
テストが機械チェックする。
目次（`src/data/toc.js`＋`src/lib/yomi.js`）は各データから自動生成し、上の「目次・索引の共通ルール」を
そのまま実装している（読み必須・自動推定しない・タイトル重複禁止・数字は読みで振り分け）。
**見た目は「黒地に白い線の銅版画の図版」で、ここも機械チェックしている**（`test/mono.test.mjs`）：
**色を持たない**（`styles.css` の色はすべて R=G=B。1つ色を入れた瞬間に図版でなくなる）・
**絵文字を使わない**（環境によって色付きで描かれ、端末ごとに形も変わる。画面に出す印は
`src/data/glyphs.js` が単一の正で、文字として描かれる記号だけ）・**飾りは SVG でその場に線を引き、
画像ファイルを持たない**（`src/components/Ornament.jsx`。色は `currentColor` だけ。Ouro の肖像と同じ線）・
**飾りより読みやすさを優先する**（飾りの線は薄く、文字の下に濃く敷かない。当たった語は
**白地に黒＝反転**で示す。モノクロで最も強い強調がこれ）。見出しは明朝（`--serif`）で字間を広く、
本文は 17px・行間 1.85。**外部フォントを読まない**（端末にある明朝を使う）。
**「黒い心理学」として出回っている型も、気づく側から入れている**（2026-08-29 追加。34件）：
**言葉の型（`channel:'text'`）とふるまいの型（`'behavior'`）を分ける**——眉間の凝視・沈黙の圧力・
捕食者のテンポは**言葉として現れない**ので、`detect.js` が `behavior` を必ず外し（渡されても外す）、
画面は「文面からは見つからない型が◯件あります」と毎回出す（読めないものを読めるふりをしない。
Ouro 項目32と同じ線）。**同じ型を二度足さない**——間欠強化＝既存「急に優しくなる」、
希少性の原理＝既存「残りわずか／特別扱い」なので、`aka`（名前＋読み。**読みは必須で推定しない**）で
繋ぎ、目次に「→ 本来の型名」の索引項目として出す。**画面に出る文にマークダウンを書かない**
（`**強調**` がそのまま表示される。実際に出した）。**地の質感は SVG の feTurbulence を
その場で描いて敷く**（画像ファイルを持たない・`feColorMatrix` の saturate 0 で色を入れない）。
**型（相手のすること）と癖（自分のしていること）を混ぜない**（2026-08-29 追加）：
「都合のいい人にされる行動」は相手の型ではなく自分の側の癖なので、`src/data/habits.js` に
別で持つ（混ぜると「当たった＝相手が悪い、ではない」の線が崩れる）。
**癖の側でいちばん大事なのは本人を責めないこと**——つけこまれるのはつけこむ側がいるからで、
癖は原因ではない。「自業自得・甘い・落ち度」の類と「性格を変えろ」は `habits.test.mjs` が弾く。
その場でできる一手も**相手の同意が要らないこと**だけにする（返し方と同じ線。「分からせる」
「納得させる」を弾く）。癖は判定用の語を持たない（貼った文面から自分の癖は分からない）。
**「見抜き方」を道具として配らない（2026-08-29 追加。`src/data/myths.js`）**——表情・言い淀み・
反応の速さ・目線から嘘を見抜く方法は広く出回っているが、人が嘘を見抜ける割合はコイン投げを
わずかに上回る程度にとどまる（Bond & DePaulo 2006）。**当たらない技法を信じると、外れたことにも
気づけず、何もしていない人を嘘つきだと決めつける**——このアプリの「決めつけない」を内側から壊すので、
6件を**信じないための材料**として置いた。「よく言われていること」と「分かっていること」を必ず分けて書く。
**研究に基づかない呼び名**（逆境依存・無意識の素顔漏れ等）は、元の研究を見つけられないことをそのまま書く。
**自分の中で起きること（`src/data/states.js`）では診断しない**——病名を当てない・当てはめて判定する
仕掛け（チェックリスト）を作らない・本人の弱さの話にしない・必ず「人に相談する」へ繋ぐ、の4つを
`myths.test.mjs` が機械チェックする。層は4つ（型＝相手のすること／癖＝自分がしていること／
状態＝自分の中で起きること／見抜き方＝自分の判断）で、**混ぜない**。
**人間分析（2026-08-29 追加。`src/data/people.js`＋`src/lib/analysis.js`）は属性で分けない**——
元にした文章は 高齢者／女性／男性 でふるまいを分けていたが、そのまま入れると
「女性は◯◯」「年寄りは◯◯」という決めつけの道具になり、このアプリの「決めつけない」を
内側から壊す。**分けるのはふるまいだけ**で、その文章自身が結論として挙げていた3つの芯
（一貫性のなさ・境界線の軽視・責任を取らない）が性別も年代も問わない共通点。
元の章立てが実際に持っていた情報は「どこで起きたか」なので、それは**場面**として残す
（`SCENES`：職場／家庭／道・店／子ども連れの場／ネット。ふるまいをここでしぼれる）。
属性でくくる言い方が入っていないことと、**元の文章のふるまいを1つも落としていないこと**を
`people.test.mjs` が機械チェックする。
**採点しない・順位を付けない・診断名を当てない**（型カタログと同じ線）。出すのは
「あなたが見たふるまい」と「取れる距離」だけで、**決めるのは距離であって人格ではない**。
**人間分析に型を足したら、必ず `counters`（黒い心理学での対応策）も一緒に書く**
（ユーザー指定・2026-08-29「これから人間分析に追加されたものには黒い心理学でどう対応するか
対応策も含めるようにして」）。型だけ足して対応策を書かないのは未完成。
`people.test.mjs` が「すべての型が counters を持つ」を機械チェックするので、忘れるとテストが落ちる。
使い返しに挙げる型は `SELF_DEFENSE_TACTIC_IDS`（こちらが呑まれないための型10件：
間を置く・ゆっくり動く・目線を外さない・先に短所を言う・言い方の枠を選ぶ・先に線を出す・
決まったこととして置く・こちらから期限を切る・こちらから二択を出す・そのまま言葉にして返す）。
※ 2026-08-29、これとは別に「使い返してはいけない型」の一覧を**頼まれていないのに足してしまい、
ユーザー指示で削除した**。同じことを繰り返さないこと（下の「勝手に足さない」）。
**ふるまいから育ちや家庭環境を逆算しない**——子ども時代の逆境とその後の結びつきは
集団としての傾向で、目の前の一人の過去を逆算できるという話ではない（`myths.js` の
`read_upbringing`）。伝え聞いた話を積み重ねて人物像を組み立てるのがいちばん実りが薄い。
**1つ当たっただけで型を出さない**（`MIN_PER_TYPE`＝2・`MIN_TOTAL`＝3。誰にでも当てはまる項目が
1つ当たっただけで断定するのは当てずっぽうと同じ）。**見立てを保存する時は、判定ではなく入力（チェックしたふるまい）を残す**
（`lib/cases.js`。腰痛ナビのカルテと同じ考え方。型を直しても過去の見立てを読み直せる）。
**氏名・連絡先の欄を持たず呼び名だけ**で、呼び名とメモに混ざった電話番号・メール・リンクは伏せる。
**型カードは `components/PersonTypeCard.jsx` を見立てと型の一覧の両方で使い、
目次の飛び先 `toc-person-*` は型の一覧の側に置く**——見立てに出ている型にしか置かないと、
目次から飛んでも着かない（実際にそうなっていた）。
**さがすのは `lib/personSearch.js`**（読み・AND・ハイライト・0件の候補。**後読みを使わない**）。
**やってみた記録は `lib/tried.js`**——残すのは ○△✕ だけで、効き目の割合も相手の変化も記録しない
（分かるのは「自分がそれをやれたか」まで）。並べ替えは**自分の記録の中の相対だけ**で、
**1回の記録では順番を変えない**（`MIN_TRIES`＝2）。**対応策の並び順を画面で決めない**
（場面・自分の記録・元の並びの順で `orderCounters` が決める）。
**対応策には言い方の例（`script`）を必ず書く**（45件。相手の同意が要る言い方を入れない）。
**手を出すときは世に出回っている呼び名を必ず添える**（「答える前に間を置く」だけでは
「沈黙の圧力」だと分からない）。呼び名は `tactics.js` の `akaNameOf`／`tacticLabel` から取り、
**画面ごとに `aka[0]` を書かない**。目次でも呼び名を先に出す
（使い返す10件すべてに呼び名があることをテストが見る）。
**新しく保存したら、その場で「編集中」へ移す**（`newCaseId()` で先に id を決めてから保存する）——
移さないと、もう一度押した時に同じ人の見立てがもう1件できる（実際に踏んだ）。
保存の戻り値を待つ形にしない（React の更新の順番によっては受け取れない）。
**見立てを開き直したら、選んだふるまいのある型を開く**——たたんだままだと何を選んだか見えない。
**1画面に2種類の飛び先があるときは両方に分岐を書く**（癖と状態、型と芯。片方の anchor しか
見ないと目次から飛べない。実際に踏んだ）。
**頼まれたものだけを足す（ユーザー指定・2026-08-29「今後は勝手に追加しないように」）**——
渡された文章を入れる時に、**頼まれていない注意書き・制限・禁止リスト・見出しを自分の判断で足さない。**
実際に「黒い心理学での対応策を入れて」と頼まれた回で、あわせて「使い返してはいけない型」の
一覧を勝手に付け、削除することになった。必要だと思うものがあれば**足す前に一度きく**。
これは鏡だけでなく、このリポジトリのどのアプリでも同じ。
**人間分析の画面は3つの枠**（`People.jsx` の `TABS`＝調べる／型を読む／保存したもの）。
**目次から飛ぶ時は、飛び先のある枠へ「描く前に」切り替える**——`useEffect` で切り替えると、
飛ぶ時（`requestAnimationFrame`）にはまだ古い枠が描かれていて着かない（実際に踏んだ）。
初回は `useState` の初期値で決め、以降は描いている途中で直す。
**見立ては上書きで消さない**（`lib/caseTools.js`）——直すたびに前の版を残し（`SNAPSHOT_MAX`＝20）、
チェックした日（`seenAt`）も持つ。**比べても採点しない**（出すのは共通・片方だけの3つだけ）。
**古い見立ては印を付けるだけ**（`STALE_DAYS`＝90日。勝手に消さない・直さない。競合台帳と同じ線）。
**距離の段（`STAGES`）に良し悪しを書かない**（右ほど正しいのではなく、いまどこにいるかを置くだけ）。
**その手が効かなかった時の次の一手を必ず持つ**（`COUNTER_NEXT`。行き止まりにしない）。
やる順番（`COUNTER_STEP`）は**段が上ほど強いのではなく、後戻りしにくいという意味**。
**自分の癖で使いにくい手は知らせるだけで止めない**（`habits.js` の `hardCounters`。押せなくすると、
その人がいちばん要る手に届かなくなる）。癖は判定せず**自分で選ぶ**。
**持ち出し／取り込みは `lib/personIO.js`**——出すのは入力と記録だけで、判定も検索履歴も入れない。
**取り込みは必ず確認を出してから**（今あるものは消さない・同じ id はあとから直したほうを残す）。
消したものは**1件だけ20秒だけ戻せる**（`UNDO_MS`）。「人間分析だけ消す」も確認を出し、
何が残るか（型・癖・状態の記録）を書く。**重なりの表（型 × 見立て）に点数を書かない**（件数だけ）。
**相談用の文（`toConsultText`）に判定を入れない**（事実を見た順に並べるだけ）。
**対応策は「このタイプにはこれがおすすめ」として3つを番号順に出す**（`lib/tried.js` の
`recommendThree`）。**並びは「後戻りのしにくさ」（`COUNTER_STEP`）が先**で、自分の記録と場面の
合い方は**同じ段の中だけ**で効かせる（記録が2回付いただけの手が①に来ると、いきなり
後戻りしにくい手から始めることになる）。番号の印は `glyphs.js` の `ORDER_MARKS`。
**各手に「相手はどうなるか」（`counters[].effect`）を必ず書く**（ユーザー指定・2026-08-30
「それをしたら相手はどうなるか？まで出して」）。**効き目の大きさを書かない**——「◯％の人が従う」は
実験の条件次第で変わるので型に書けば必ず嘘になる。書くのは**その場で何が起きやすいか**だけで、
**必ず外れる場合を残す**（〜が多い／それでも続くなら／〜とは限らない）。
**「相手が変わる」と書かない**（改心・反省させる・性格が直る、はテストが弾く）。画面にも毎回
「相手の性格が変わるという意味ではありません／逆に出ることもあります」を出す。
**落ちても行き止まりにしない**（`components/ErrorBoundary.jsx`）——受け皿が無いと1か所落ちただけで
下部ナビごと消え、再読み込みしか手が無くなる。**外から来た見立ては `cases.normalizeCase()` を必ず通す**
（`checkedIds`・`note` が無いだけで画面が落ちる。取り込みも端末内のものも通してから画面へ渡す）。
**消したものを端末に残さない**（`undoCase` を保存に混ぜない）。**「元に戻す」はいる場所に出す**
（`.undo-bar` は `position: fixed`。画面の上に置くと一覧の下で消したとき見えないまま失効する）。
**隠した手は型ごと**（`hiddenByType`）で、**「まず1つ」「今日試す1つ」からも外す**。
**「まだ試していない手」は見立てごとに数える**（`untried(counters, tries, caseId)`）。
**消す操作には必ず確認を出し、作った記録は必ず消せるようにする**。
**「すべて消す」は実際に消えるものを全部書く**（記録・見立て・やってみた記録・癖・しぼり込み・設定）。
**画面を移っても書きかけを捨てない**（`App.jsx` の `uiRef`。**端末には保存しない**——
「貼った文面は残さない」「選んだだけでは残らない」の約束はそのまま）。
**目次の飛び先は `toc.js` の `anchor` を使う**（画面側で組み立て直すと二重になり黙って食い違う。
飛び先が固定バーに隠れないよう CSS に `scroll-margin-top`）。**絞り込んだら件数の表示も変える。**
**保存できなかったことを黙らない**——`storage.js` の `writeRaw` が失敗を飲み込んでいたので
`save()` が常に true を返し、「保存しました」と出たまま何も残らなかった。書けたかを返し、
`saveFailed` を画面のいちばん上に出す。**上書きで版を消さない**（`mergeCases` は取り込みでも
「移り変わり」と「いつ見たか」を残す。いつ見たかは早いほう）。**消したものは3件まで戻せる**
（`UNDO_KEEP`）で、**戻したらそのまま編集中に入る**・**閉じる口も置く**。
**編集中の見立てを消しても書きかけを捨てない**。**記録の本文にも上限**（`TEXT_MAX`＝4000。
切ったことは画面に出す）。**同じ本文を二重に記録しにくくする**。**まるごと持ち出せる**
（`lib/backup.js`＋設定。記録も入るので「置き場所に気をつけて」と必ず書く）。
**下部ナビで戻っただけで調べる側を切り替えない**。**読めない文字にしない**（記録・出典・
調べた結果の型名は押せば型の一覧へ行く）。**どの画面にもさがす欄**（`components/Finder.jsx`
が単一の正。0件でも黙らない・件数は絞り込み後・ラベル必須）。**ふだんの言い方でも引ける**
（`personSearch.SYNONYMS`。**新しい主張を作らない**＝対応先はデータの中にある語だけ）。
**さがした語を消せる**。**一覧は少しずつ出す**（記録は30件ずつ）。
**飛び先の余白は rem で持つ**（px 決め打ちだと文字を大きくしたとき隠れる）。
**横に長い表は左端の見出しを残す**。**長い文で画面が伸び続けない**（`.quote` に高さの上限）。
**入力欄には必ずラベルを付ける**（placeholder は入力すると消える）。
**地に敷く面（おもて）は開くたびに変わる**（`components/Figures.jsx`＋`lib/figure.js`。
2026-08-30 ユーザー指定「アプリの背景にこのイラストを文字無しで／開き直すと変わる仕様」）。
**前に出したものを避けて選ぶ**（ただの乱数だと同じものが続けて出て「変わらない」と見える。
`settings.lastFigure` に覚える）。1枚足せば候補が自動で増える。
**画像を持ってよいのは地の面だけ**（2026-08-30 ユーザー了承「画像ファイル持っていいよ／できれば
画像で表示してほしい」）。飾り（`Ornament.jsx`）は今までどおりその場で線を引く。
**絵は手で塗らない**——`tools/draw-figures.js` がキャンバスに描き、`node tools/make-figures.mjs` で
`src/assets/figures/*.webp` へ焼く。**画像を直接いじらない**（次に焼いた時に消える）。
**絵にも色を入れない**（描く側で使ってよいのは `rgb(v,v,v)` の `g()` だけ。焼くときに1点でも
色が付いていたら止める）。**地の絵に文字を焼き込まない**（`fillText` 禁止・`alt=""`）。
**飾りより読みやすさを優先する**（`.figure-bg` は濃さ0.6以下・下ほど薄く・`pointer-events: none`・
読み上げから外す）。全部で400KBを超えないことをテストが見張る（いま12枚で284KB）。
**こわさの作り方**（2026-08-30 ユーザー指定「より怖い画像にして」→ 同日「よりホラーに。モノクロで／
背筋がこおる／見るだけで冷たくなる」で全面的に描き直した）：①目は真っ黒の穴にする（ぼかすと
優しくなる。ふちだけ細く光らせて深さを出す）②左右をわずかにずらす（完全な左右対称は飾りに見えて
こわくない）③全部を照らさない（顔の半分は闇に残す）④傷と縦のにじみを入れる⑤光の向きは図ごとに変える。
**いちばん効くのは「明暗の境目を硬くする」こと**（`chalkFace` の `terminator`）——なめらかに回り込ませると
粘土の模型に見える。冷たく見えるのは光の側がほぼ白・闇の側がほぼ黒で、その境が**ぎざぎざ**だから。
**境目の傾き（`tilt`）は面ごとに変える**（まっすぐ縦に落とすと、どの面も同じ「加工」に見える）。
**なめらかに塗らない**（`scrub`＝木炭を擦った線の重なりで面を作り、線の間から地を見せる）。
**枠を埋める**（顔が小さく浮いていると、ただの飾りになる。頭は枠の幅の6割前後・首から下は
`shoulders()` で黒くつぶす）。**形をまん丸にしない**（`headPath`＝上が広く顎が細い。
これだけで人の顔として読めるようになる）。**面ではなく「かたまり」で描く**——兎の耳を線だけで
作ったら角や毛束に見えた（実際に踏んだ）。中を埋めてから線を重ね、内側に濃い溝を1本入れる。
**光は点で置き、玉ごと光らせない**（目玉全体を白くすると人形の目になる。大きい黒の中の小さな光が
いちばん落ち着かない）。**絵を強くしたら、文字に掛かる所は落とす**（読みにくくなったら絵ではなく
`.figure-bg` を直す）。12枚：叫ぶ頭／逆さの顔／群れ／骨の顔／長い髪の面／兎の面／影の人／
近すぎる顔／闇の中の目／伸びてくる手／覗いている顔／水面の目。
※ 参考にした画像は他のクリエイターの投稿なので**同梱しない**。同じ雰囲気を自分で描き起こす。
テストは `kagami/test/*.test.mjs`（282件。ルートの `npm test` の再帰探索からも実行される）。
資格ラボの要点だけ再掲（2026-08-29 追加。**このアプリの決まりは shikaku-lab/README.md が単一の正**）：
1. **毎年変わる数字を `src/data/exams.js` に持たない**（合格率・合格点・試験日・受験料）。
持った瞬間に「アプリに書いてあったから」と古い数字を信じてしまう。代わりに `checkPoints`
（公式サイトで自分で確かめること）を持つ。`test/exams.test.mjs` が正規表現で機械チェックする。
2. **「合格に必要な時間」を出さない**（`lib/schedule.js`）。出せるのは「確保できる時間」だけ。
必要な時間は人によって違いすぎるので、手元に無い基準をアプリが決めない（Ouro の項目90・126と同じ線）。
3. **認知特性は診断ではない**（`lib/cognitive.js`）。**同点なら決めない**（`TIE_MARGIN`）・
**回答が足りなければ断定しない**・**未回答を0点にしない**（「答えていない＝合わない」になる）・
**「あなたは◯◯型です」と言わない**（言い方は `profileLine()` の1か所に集約。テストが断定表現を見張る）。
4. **AIをアプリから呼ばない**（`lib/convert.js`）。出すのは「AIに貼るプロンプト」と「受け取る口」だけ。
APIキー無しで今日から使えること・端末内保存を崩さないこと・出てきたものを人が必ず一度読むこと、の3つが理由。
プロンプトの `FIXED_PROMPT_RULES` は**足せるが外せない**。とくに**検索できない時にURLを書かせない**
（「未確認（要出典）」と印を付けさせる。Ouro の `docs/PROMPT.md` 項目88と同じ事故を防ぐ）。
5. **同じことを2か所に書かない。** 計画書の本文は `lib/plan.js` の `planMarkdown()` だけが作り、
画面もその出力を見せる。設計書（`lib/spec.js`）は**計画書から導く**ので、計画を直せば設計書も変わる。
6. **選んでいない勉強法の画面を設計書に書かない**（`SCREEN_RULES`＋`screensFor()` の1か所で決める。
設計書の文面に if を足さない）。全部入りを渡すと Claude Code は全部作ろうとして、どれも中途半端になる。
**問題データは設計書に貼らず `questions.json` として別に渡す**（読ませる量が跳ね上がって設計が薄まる）。
7. **ポモドーロは「終わる時刻」から毎回引き算する**（`lib/pomodoro.js`）。1秒ずつ減らすと、
タブを裏に回した時に必ずズレる。**連続日数を出さない**（`streak` を実装しないことをテストが機械チェックする。
変革ノートと同じ線）。音は端末の中で作る（音声ファイルを持たない）。
8. **「次にやること」は実際の状態から導く**（`lib/steps.js`。手で印を付けさせない。Ouro の項目51と同じ）。
9. **同梱の試験と自作の試験を、後ろの仕組みから見て区別しない**（`lib/myExam.js`）。
区別すると画面ごとに「自作なら〜」の if が増えて必ず食い違う。
10. 目次（`src/data/toc.js`）は各データから自動生成する。`data/exams.js` に性格・出題形式の語彙を
足したら、`toc.js` の `TRAIT_READINGS`／`FORMAT_READINGS` にも**読みを足す**（テストが1対1を見張る）。
テストは `shikaku-lab/test/*.test.mjs`（114件。ルートの `npm test` の再帰探索からも実行される）。
間合い（まあい）の要点だけ再掲（2026-08-30 追加。**このアプリの決まりは maai/README.md が単一の正**）：
恋愛心理学のアプリ。鏡（かがみ）と同じ黒地に白い線・端末内保存のみ・目次の共通ルール。
**二層でできている**——`data/approach.js`（近づき方30件＝自分の側だけでできること）と
`data/tactics.js`（人を思いどおりにする型31件＝気づくための一覧）。層を混ぜない
（同意は `consent.js`、自分の癖は `habits.js`、自分の中で起きることは `states.js`、
出回っている思い込みは `myths.js`）。
1. **相手を思いどおりにする手順を書かない。** 断らせない言い方・その気にさせる段取りは
   どこにも置かない（書くと、それを使われる側がこのアプリを読んでも助けにならない）。
   `approach.js` の `how` に「酔わせ・押し切・連れ込・既成事実・断らせない・落とす…」が
   入っていないことを `test/approach.test.mjs` が機械チェックする。**ここがこのアプリの背骨。**
2. **効き目の大きさを書かない**（「◯％が落ちる」「必ず」「誰でも」）。代わりに
   確からしさの3段（`EVIDENCE`：くり返し確かめられている／結果が割れている／言われているだけ）を
   1件ずつ持ち、**thin には研究ではない出典（`research:false`）を必ず1つ**付ける
   ——付けないと画面で「研究で確かめられたこと」に見える。
3. **いちばん重い1件は「あいまいな返事は断り」**（`approach.soft_no` と `consent.soft_no_is_no`。
   出典 Kitzinger & Frith 1999）。ここを取り違えると、近づき方はそのまま「断りを押し返す型」になる。
   「お持ち帰り」を調べに来た人への答えは `consent.js`（返事が成り立つ条件だけを書き、取り付け方は書かない）。
4. **法律の数字をアプリに持たない**（年齢の線・要件は改正で変わる。公式で確かめる、と書く）。
   テストが `\d+(歳|才|年以下|条)` を弾く。
5. 鏡と同じ線：点数・危険度・順位を付けない／当たった語を必ず見せる／当たらなくても黙らない／
   言葉に残らない型（`channel:'behavior'`）は文面から当てない／断り方に言い負かす言葉を置かない／
   出典に URL を書かない／癖で本人を責めない・診断しない／後読みを使わない／
   `src/lib/` はネットワークに触れない／色・絵文字・画像ファイルを持たない。
6. **画面に出す文にマークダウンを書かない**（`**強調**` がそのまま出る。鏡で実際に出した）。
   `test/offline.test.mjs` が全データの文字列を機械チェックする。
7. **目次から飛ぶときの状態変更は `useLayoutEffect`**（`Approach.jsx`／`Tactics.jsx`）。
   `useEffect` にすると「開く」描き直しが `flashTo`（次のフレーム）より後になり、
   React が className を書き直した拍子に `flash` が消える（**実際にこの状態になっていて直した**）。
   **鏡の `Tactics.jsx`／`People.jsx` にも同じ race が残っている可能性がある**——直すときは両方見る。
テストは `maai/test/*.test.mjs`（93件。ルートの `npm test` の再帰探索からも実行される）。

腸（ちょう）の要点だけ再掲（2026-09-02 追加。**このアプリの決まりは chou/README.md が単一の正**）：
お腹の記録アプリ。芯は**「毎朝ひとタップで残る → 受診のときに医療者へ渡せる」の一本道**で、
健康情報でも食事指導でもない（IBS は検査で異常が出ないので、本人の記録そのものが診察の材料になる）。
読み物を先に増やすと、読み物だけ増えて記録が1日も続かないアプリになるので後段に置く。
1. **診断しない・緊急度を色で決めない。** 赤/黄/緑にすると「緑」と出た人が受診をやめる。
   置くのは**受診の目安**（`data/redFlags.js`）で、**当てはまった数を数えない・順位を付けない**。
   お通じの印（血が混じった等）が付いても出すのは「読める場所」だけで、判定はしない。
2. **点数を付けない。** お腹の調子は5つの**段**（`data/scales.js` の `BELLY_STEPS` が単一の正）。
   `order` は並べ替え専用で画面に出さない。**`stats.js` は average・mean を返す関数を持たない**
   （テストが名前で機械チェックする）。**ブリストルも平均を出さない**——1と7が1回ずつの日の
   平均4は「ふつうの便が1回あった」ではない。出すのは分布と回数の幅だけ。
3. **相関を因果として書かない。** ふりかえりが**このアプリでいちばん壊しやすい所**で、
   「たまねぎ → 腹痛」と矢印で結んだ瞬間に根拠のない食事指導になる。並べるまでで止める。
4. **手元に無い基準を数値で持たない**（1日◯gの食物繊維・◯％が善玉菌・理想のバランス）。
   出すのは自分の記録の中の相対だけ。**低FODMAP は○×の二択にせず「少なめ／量による／多め」**
   の3つで持ち、**料理名から判定するAIを作らない**（必ず外れる）。合う・合わないは本人が押す。
5. **連続日数を煽らない**（変革ノート・資格ラボと同じ線）。お腹の調子は自分で決められないので、
   途切れた日が「怠けた日」に見える作りにしない。出すのは通算の記録日数と2週間の埋まり具合。
6. **位置情報・氏名・連絡先を持たない。** トイレは端末の地図アプリを開くリンクだけ
   （現在地を受け取りも保存もしない）。**匿名掲示板と通知は作らない**——サーバーとモデレーションを
   負えない／「毎朝◯時」を約束できないため。**この3件はユーザー一任のうえで決めた。蒸し返さない。**
7. **保存するのは入力だけ**（判定結果を保存しない。腰痛ナビのカルテ・鏡の見立てと同じ）。
   `src/lib/` はネットワークに触れない。日付は `lib/dates.js` の文字列操作で組み立て、
   **`toISOString()` と `new Date('YYYY-MM-DD')` を使わない**（UTCで前日になる）。
8. **1日ぶんの入力は `components/DayEditor.jsx` を、ホームとカレンダーの両方で使う**
   （画面ごとに書くと片方だけ直したときに必ず食い違う）。
9. **色を持たない**（styles.css の色はすべて R=G=B。赤や黄を1つ入れると「危険度」に見える）・
   **絵文字を使わない**・**画像ファイルを持たない**（キャラクター・ブリストルの絵はその場に SVG）。
   キャラクターは**責める顔・怒る顔・泣く顔を持たない**（`lib/gutLine.js` に文言を切り出してあり、
   テストが**実際に画面へ出る文字列**を見る）。
10. **便を汚いものとして扱う言い回しを置かない**（テストが機械チェック）。ここを崩すと、
    いちばん記録してほしい人が記録しなくなる。
11. **目次・索引は元データから毎回導く**（`data/toc.js` の `buildTocEntries()`。
    **目次専用の手書きの一覧を作らない**）。読みは `data/terms.js`・`scales.js`・`redFlags.js`・
    `fodmap.js` に**手で持たせる**——漢字の読みを機械が当てない。入れ忘れは「その他」行に
    落として見えるようにする（`lib/yomi.js` の `buildKanaIndex`。閾値を超えたら開発中に警告）。
    数字は `numberToReading` で読みに直してから行を決め（「7段階」→な行）、英数字は
    `normalizeAlnum` で正規化してから A〜Z に入れる。**タイトルの重複はテストが機械チェック**。
12. **飛び先の印は `data-flash` 属性**（`lib/focus.js`。`className` に足すと、飛び先が
    描き直された拍子に印だけ消える＝鏡で踏んだ事故）。**飛び先を指定した移動では画面の先頭へ
    戻さない**（`shouldScrollTop` が単一の正）。**タブ・絞り込みの切り替えは `useLayoutEffect`**
    ——`useEffect` にすると、掴みに行く時にまだ古い絞り込みで描かれていて着かない。
    飛ぶ仕組みは `components/useFocusJump.js` を**どの画面からも同じものを使う**。
13. **会話から拾った言葉を目次へ勝手に入れない。** 候補を作ってよいのは
    `data/tocCandidates.js` の3つの合図だけ（`marker`＝「■用語追加：」／`tags`／`user_request`）。
    候補のあいだは**本体（`userTerms`）に一切書かない**。「追加する」を押した時に初めて
    **読み・重複・分類・表記**の4つを確かめてから入れ、「しない」は本体に痕跡を残さない
    （履歴にだけ見送りとして残る）。直近の追加は `undoLastTocAdditions(n)` で**対象だけ**戻せる。
    会話由来の説明は必ず `needs_review`（画面に「※要確認」）で、**`verified` にできるのは
    `setVerified(..., { byUser: true })` ＝人が明示的に押した時だけ**。
14. 用語をタップすると出るパネルは `panelDataFor` の1か所から受け取る（画面ごとに条件を書かない）。
    説明が空なら「※説明未登録」、飛び先が無ければボタンを出さず「関連する飛び先はありません」。
    飛び先の type は **page／question／function／system の4つだけ**。
15. **借りてきた考え方を、アプリの言葉として言い切らない**（アダムスキー式の食べ合わせ。
    `data/adamski.js`＋`lib/combine.js`。出典は本人の要約で原著未確認）。
    **アプリが「詰まっています」「毒素が出ています」と判定しない**——出すのは
    「この考え方では、速いものと遅いものが一緒になっています」までで、画面には毎回
    「これは判定ではありません」を出す。**出典の数字（30分・18時間・毒素10倍・免疫の8割・
    スポーツ選手3〜8時間）は計算に使わない**（テストが `lib/combine.js` に 18／30／40／毒素 が
    無いことを機械チェックする）。使ってよい数字は食事の間隔の4時間だけで、それも
    `MEAL_GAP_HOURS` から取り「出典の数字」と明記する。**裏が取れていない主張は
    `ADAMSKI_UNVERIFIED` として隠さず出し、1件ずつ「確かめられていない」を添える**
    （変革ノートの『できる男は超小食』と同じ形）。**守れた回数を数えない**——出典自身が
    「100%守れないのでできる範囲で」と言っている。
16. **2つの考え方が食い違う所は、両方そのまま見せる**（`lib/conflicts.js`。モンクモードの
    `CONFLICTS` と同じ線）。はちみつ・ヨーグルト・にんにく・たまねぎ・牛乳は、低FODMAP では
    「多め（減らす候補）」、アダムスキー式では「速い・消化を助ける」で**正面から反対**になる。
    **アプリはどちらが正しいかを決めない。** 一覧は**元データから毎回導く**（書き写さない）。
17. **食べものの速さは「出どころ」を必ず一緒に返す**（`speedOf` の `basis`＝
    named／category／default／unknown）。**当てはめただけのものを名指しと同じ顔で見せない。**
    出典が名指ししている語は**出典が使っている短い言い方**（パン・米・パスタ・肉・チーズ…）でも
    引けるようにし、カタカナ／ひらがなの表記ゆれは `foldKana` で吸収する——一覧の見出しを
    長い名前（「パスタ（小麦）」）にしたせいで、**出典の代表例「トマト＋パスタ」が
    当たっていなかった**（実機で踏んだ）。テストが代表例5件を機械チェックする。
18. **ストレスと「体を動かしたか」は段だけを記録する**（`EXERCISE_STEPS`＝4段）。
    時間・歩数を数えない（「何分やれば効く」という基準が手元に無い）。
    ふりかえり・受診メモにも日数として出せるが、**平均を出さない・原因と結果を結ばない**。
19. **市販の薬・医薬部外品を扱う所（整腸剤。`data/probiotics.js`＋`lib/probiotic.js`）は、
    ほかより一段きつく縛る。** 効き目を言い切ると、受診したほうがよい人が「まず整腸剤で
    様子を見よう」と考えてしまう。**商品を勧めない・順位を付けない・値段や売上ランキングを
    持たない**（「売れている＝自分に合う」ではない）。**どの画面からも受診の目安へ行ける**。
    **飲み合わせは機械が判定しない**（`docs/MVP.md` の「作らない」を守り、薬剤師へ案内する）。
    大学の名前で語られている数字（ハーバード・コーネル・ニューヨーク州立…）は
    `PROBIOTIC_UNVERIFIED` に置き、**アプリの言葉として画面に書かない**（テストが機械チェック）。
    「リーキーガット症候群は確立した病名ではない」「心の不調を整腸剤で様子見しない」も必ず書く。
20. **出典が事実として誤っている所は、黙って通さない**（`PROBIOTIC_CORRECTIONS`）。
    「整腸剤はあくまで食品」は誤りで、市販の多くは**指定医薬部外品**（用法・用量がある）。
    **元の言い分も消さずに並べる**——何を直したのかが分かるように。
21. **調味料（`data/seasonings.js`）は「腸によい／悪い」を言い切らず、
    「買うときに表示のどこを見るか」まで**（`look` を全件必ず持つ）。**値段の話をしない**
    （本物＝高い、と読ませない。実際に「手ごろな値段のものは…」と書いてテストに捕まえられた）。
    **添加物＝悪と決めつけない**・**替えた数を採点しない**・全部いっぺんに替えなくてよいと必ず書く。
22. **調味料の目次の題は「◯◯の選び方」にする**——「塩」「酢」は低FODMAP の一覧にもあり、
    同じ題にすると重複で落ちて選び方の画面へ行けなくなる（実際にぶつかった）。別名に元の名前を持つ。
23. **人を責める言い方を持ち込まない**（`data/cleanup.js`）。素材によっては
    「腸が汚い人は見た目も汚い」「腸が不健康で健康な人はいない」のような言い切りが出てくる。
    体調の悪い人を貶め、受診を遅らせるので**このアプリは採らない**。ただし**黙って消さない**
    ——`CLEANUP_CORRECTIONS` に元の言い分と「採らない理由」を並べる。
    **見出しにも読みにも貶める言い回しを置かない**（title を引用形にしたら `reading` にまで
    「きたない」が残り、決まり11のテストが捕まえた。実際に踏んだ）。
24. **心の不調を腸で片づけない。** 「セロトニン＝幸福ホルモン」「腸を整えればうつが改善」は
    飛躍。**腸で作られたセロトニンは脳へ入れない**（関門を通れない）ことと、
    **うつのセロトニン不足説が近年強く見直されている**ことを必ず書き、
    つらいときは医療機関へ、と添える。「腸で様子を見る」と読ませない。
25. **同じ食べものを3つの考え方から同時に見せる**（`lib/conflicts.js` の `fermentViews`）。
    腸活は発酵食品を勧め、低FODMAP はヨーグルト・納豆・味噌を「多め／量による」とする。
    **どれが正しいかを決めない。** `threeWayConflicts` が数えるのは**出典が名指ししているものだけ**
    ——区分から当てはめただけのものを「3つがぶつかっている」と見せると、当てずっぽうを断定にする。
26. **引用して否定している出典の言い分は、見張り（rules.test.mjs）の対象から外す**
    （`QUOTED` を stripQuoted で落とす）。落とさないと、**間違いを指摘した所だけがテストで落ちて、
    黙って消すことになる**——見張るのは「アプリ自身が言っていること」。
27. **眠れたか・姿勢も段だけを記録する**（`SLEEP_STEPS`／`POSTURE_STEPS`、各3段）。
    睡眠時間・歩数を数えない。姿勢は採点しない（出典自身が「ずっと保てなくてよい」と言っている）。
28. **出典どうしが食い違う所も、両方そのまま並べる**（`prebiotics.js` の `SOURCE_CONFLICTS`）。
    今回は3件——飲んだ菌は住み着くのか（定着する／数日で出ていく）・試す期間（1か月／2〜3か月）・
    リンゴ酢との組み合わせ（効果が上がる／科学的根拠に乏しい）。**アプリはどちらが正しいかを決めない。**
    決めた瞬間に、決めた側の根拠をアプリが持っていることになる（25の3層と同じ線）。
29. **善玉菌の餌（プレバイオティクス）と低FODMAP は目的が反対を向いている**
    ——餌を増やす＝大腸で発酵する糖を増やす、低FODMAP＝その糖を減らす。
    ぶつかる食べもの（もち麦・オートミール・バナナ・ごぼう）は
    **`lib/conflicts.js` の `prebioticViews` が元データから毎回導く**（手書きの一覧を持たない）。
    突き合わせ先は `PREBIOTIC_FOODS[].fodmapName` に**手で書く**——名前の当てずっぽうな一致で
    食い違いを作らないため（テストが「実在する見出しだけ」を機械チェックする）。
    お腹の張りで困っている人には反対に働くので、`PREBIOTIC_PRECHECKS` は止めずに出し続ける。
30. **食べもので「がんを防げる」と書かない**（フコイダンの訂正）。研究されている段階のものを
    予防効果があると書くと、受診・治療を遅らせる。**抗生物質との飲み合わせもこのアプリで判断しない**
    （「併用できる整腸剤」と紹介されても、処方した医師・薬剤師へ回す）。
    **有名人の名前が付いていても根拠にしない**（「すべての病は腸から」＝ヒポクラテス説は裏づけ無し）。
    **「リーキーガット症候群」は確立した病名ではない**と必ず書く（小麦を強く制限する前に相談へ）。
31. **目次のタイトルがぶつかったら、後から来たほうが黙って落ちる**——善玉菌の餌の食べもの6件
    （バナナ・ごぼう・だいこん・キウイ・みかん・オートミール）は低FODMAP の一覧と同名なので
    **「◯◯（善玉菌の餌）」**という題にし、素の名前は別名で引けるようにした（調味料の
    「◯◯の選び方」と同じ直し方）。**用語を足す時も既存タイトルとの衝突を必ず確かめる**
    ——`term-leaky-gut` を足した時、整腸剤側の同名の項目が静かに消えた（作った直後に気づいて取り下げた）。
32. **飛び先 id の実在チェックは、テンプレートで組み立てている id を素の文字列で探さない**
    （`` id={`kind-${kind.id}`} `` はソースに `kind-soluble` という文字列を持たない）。
    組み立ての形まで見る（テストを書いた時に実際にここで落ちた）。
33. **薬を「猛毒」とも「神」とも呼ばない**（`otcDrugs.js`＋`OtcDrugs.jsx`）。素材は
    「絶対に買ってはいけない猛毒市販薬」「99%の医者が鬼リピする神レベル市販薬」という
    強い言い方だったが、そのまま持ち込むと**処方されて飲んでいる人まで手を止める**。
    出すのは「どういうときに向かないと説明されているか」と「誰に聞けばよいか」まで。
    画面のいちばん上に「この画面を読んでやめないでください」を常に出す。
    **「処方されたらヤブ医者」も採らない**——目の前の人を診た医師には動画から見えない事情がある。
    一方で**受診へつながる部分は弱めない**（下痢・嘔吐が続く／水が飲めないときは医療機関へ）。
    **飲み合わせも用量も判定しない**（README 決まり19と同じ線）。
34. **血液型で病気を分けない**（`gutHabits.js` の `HABIT_CORRECTIONS.bloodtype`）。
    素材は O型＝十二指腸潰瘍1.4倍・A型＝胃がん…と挙げていたが、これは集団を比べた差で、
    目の前の1人の未来ではない。**当てはめられた人が「自分はそういう体質だ」と受診を
    あきらめる理由になる**ので、アプリは血液型を持たない。テストが
    「血液型が引用（claim）の外に出てこないこと」を機械チェックする——
    **訂正の id が `bloodtype` なのでキーワード検索では必ず当たる**（当たったからバグ、ではない）。
35. **「◯倍」を「自分がそうなる確率」と読ませない。** 唐辛子17倍・塩分2.3倍・歯周病と脳出血14倍
    などは、もとの人数が分からなければ自分がどれくらい増えるかは分からない。
    倍率は `HABIT_UNVERIFIED` に主張として置くだけで、**計算にも判定にも使わない**
    （テストが `lib/conflicts.js` に倍率が入っていないことを見る）。画面にも毎回注意を出す。
36. **国や民族でひとくくりにしない**（「日本人はカフェインに弱い」「アルコールに弱い人が44%」）。
    強い・弱いは人によって違う。**国を条件にした判定をしない。**
37. **出典が自分で取り下げた説は、取り下げの経緯ごと残す**（`butyrate.js` の `WITHDRAWN`）。
    痩せ菌・デブ菌は出典自身が「日本人には当てはまらないのでもう言わない」と話している。
    **アプリが直したのではなく出典がそう言っている**、という違いを消さない（訂正とは別に持つ）。
    本や記事にはまだ残っているので、検索して辿り着ける場所が要る。
38. **食物繊維は「見ている場所」で言うことが逆になる**（`lib/conflicts.js` の `fiberViews`）。
    胃の側＝弱っているときは避ける／腸の側＝善玉菌の餌として増やす／低FODMAP＝発酵する糖を減らす。
    **どれかが嘘というわけではない**ので、3つ並べてどれが正しいかを決めない。
    しかも**同じ出典の中でも割れている**（`withinSourceFiberConflict`）ので、それも別に持つ
    ——出典どうしの食い違い（`SOURCE_CONFLICTS`）と層が違う。
39. **使った市販薬を記録できる**（`days.js` の `otc`、`DayEditor.jsx` の `rec-otc`）。
    印をつけるだけで**量も良し悪しも見ない**。**受診メモには既定で入れる**（`DEFAULT_PARTS`）
    ——受診のときに必ず聞かれるのに本人が言い忘れやすく、素材も「飲んでいることを必ず伝える」と
    書いているため。記録が無い期間では見出しごと出さない（空の見出しを並べない）。
40. **用語を足す時は既存の派生項目とも題がぶつからないか確かめる**——`term-butyrate`（酪酸）を
    足したら、`butyrate.js` の `SHORT_CHAIN` 由来の「酪酸」が静かに落ちた（用語が先に来るため）。
    **元データ側を単一の正にして用語を取り下げた**。同じ理由で「芽胞（がほう）」は
    既存の `term-spore` が単一の正で、派生側では作らない（`fromButyrate` にコメントで明記）。
    **31の逆パターン**（前回は派生側の題を変えたが、今回は用語側を取り下げるのが正しかった）。
41. **まとめて消す編集を、境目の当てずっぽうでやらない。** `terms.js` から用語1件を消すのに
    `s.index(次の用語)` で挟んで切ったら**18件まとめて消えた**（切り出しの境目がずれた）。
    `git checkout` で戻し、**編集の前後で件数を数えて assert する**形に直してからやり直した。
    データファイルを機械的に編集する時は、必ず前後の件数を突き合わせること。
42. **「正しい割合」をアプリが決めない**（`protein.js` の `PROTEIN_CORRECTIONS.ideal_ratio`）。
    出典は動物性:植物性＝1:1、水溶性:不溶性＝1:2を「理想」と書くが、**アプリがそう決めると、
    そこから外れた人が「間違っている」ことになる**（README 決まり4）。出典の目安として引用は
    するが、**グラム数の計算も割合の採点もしない**（テストが計算関数の不在を見る）。
    ※ このとき `rules.test.mjs` の `理想の?(バランス|割合)` に自分の訂正文が引っかかった。
    **QUOTED で逃がすのではなく、アプリ自身の文のほうを言い換えた**（「これが正しい割合だ」）
    ——見張りを緩めるより、書き方を変えるほうが正しい。
43. **除去（ためしにやめてみる）は `lib/elimination.js`。** 試すためのもので、**続けるための
    ものではない**（低FODMAP と同じ考え方）。期間が来たら必ず「一度もとに戻して、変わるかを
    見る」を出し、**「よくなりましたか」とは聞かない**（聞くと期待した答えのほうへ寄る）。
    **同時に2つやめない**（`canStart`。どちらが効いたのか分からなくなる。**断るだけで
    勝手に入れ替えない**）。守れた日数も、良くなったかも保存しない。
44. **サプリの量も飲み方も出さない**（ビタミンD）。取りすぎると害が出る栄養素で、
    「これひとつですべての病気を防げる」という言い方はサプリを増やす方向に強く効く。
    テストが「IU が引用の外に出ていないこと」を機械チェックする。
45. **断食（`fasting.js`）はこのアプリで扱う題材の中でいちばん危ない。**
    ①**アプリは勧めない**と明記する ②**やめどきを、始め方より画面の前に置く**
    （テストが `fasting-stop` と `fasting-shapes` の並び順を見る）③**日数も時間も数えない**
    （変革ノートの `fasting.ts` と同じ線）④向かない人（血糖の薬・食後の薬・成長期・妊娠授乳・
    摂食障害の経験・空腹で腹痛が出る人）を止めずに出し続ける。
46. **食べ方でがんが治る・防げるとは書かない**（決まり30・36と同じ線）。今回の出典には
    「16時間断食で舌がんを寛解させた」という一文があり、**これがいちばん危ない**——
    治療を遅らせると取り返しがつかない。1人の経過は、ほかの人の根拠にならない。
47. **「宿便」「血液が汚れる」「酸性・アルカリ性食品」を、そのまま通さない。**
    どれも医学の言葉ではなく、**測れないので良くなったかも確かめられない。**
    とくに「たまった汚れを出す」という考え方は、腸を洗う商品や強い下剤へつながりやすい。
    あわせて「長崎の原爆で味噌汁と玄米を食べていた人は原爆症が軽かった」（裏づけ無し）と
    「背骨の◯番目と内臓の対応」（確かめられていない）も訂正する。
48. **マグネシウムを独立した画面にしない**（`magnesium.js` は市販薬の画面の中の節）。
    出典が挙げる効果7件のうち**腸の話は「腸を動かす」の1件だけ**で、残りは確かめる手立ても
    記録する形も持っていない。**扱わないとはっきり書く**（`MAGNESIUM_SCOPE_NOTE`）。
    腸のアプリが引き受けるのは「酸化マグネシウムは便秘で処方される薬そのもので、
    **自分でやめると便秘が悪化する**」まで（決まり38。決まり26と同じ線）。
49. **`fromOtc` が `said` の `**` を落としていなかった**——`doctor` だけ `replace` していて、
    新しい kind（マグネシウム）で初めて目次の説明に `**` がそのまま出た。
    `cleanup.test.mjs` の「画面に出す文にマークダウンを書かない」が捕まえた。
    **元データの文に `**` を書く流儀なら、目次へ流す所は全部 `replace` を通すこと。**
50. **食べものを「猛毒」と呼ばない**（`scaredFoods.js`＋`ScaredFoods.jsx`）。決まり33（薬）と
    同じ線を食べものへ広げたもの。素材は「週に一回以上は死んでも食うな」「猛毒食品」
    「99パーセントの医者が毎日食べている神食品」という言い方だった。
    **食べものを怖がらせる言い方は、食べられなくする**——このアプリを使うのはお腹の調子で
    困っている人で、食べるのがこわくなっている人がいる。**訂正を食べものの一覧より画面の前に置く**
    （テストが並び順を見る。先に一覧を見ると「食べるな」の一覧に見えるため）。
51. **高齢の人にとって、こわいのは食べすぎより低栄養。** 素材自身が「衰弱」を寝たきりの原因の
    3位に挙げているのに、そのすぐ横で「食うな」と言っている。食が細い人には
    **食べないことのほうが危険**で、減らすより「足せるものを足す」ほうが現実的。
    **自炊できることを前提にしない**（体調が悪い日・作れない人には、買って食べられることが助け）。
52. **1つの食べもので、1つの病気は決まらない・防げない。** 「一口で認知症2倍」も
    「サバ缶で寝たきり原因5つ全部を予防」も採らない。**怖がらせる言い方と、救い主にする
    言い方は裏表**——どちらも原因をひとつに決めつけるやり方で、当たらない。
    「◯◯を食べていた人がほぼ全員」は、広く食べられているものなら当たり前で、
    **いま病気を抱えている人に「自分のせいだ」と思わせるだけ。**
53. **朝に出ないことを、良し悪しにしない**（`morning.js`）。「朝一番の便こそが最も健康の
    バロメーター」は**夜勤・交代勤務の人、朝が慌ただしい人を追い詰める**（変革ノートが
    シフト勤務のために就寝時刻を固定しなかったのと同じ考え方）。出た時刻で良し悪しを付けず、
    **便が出ないことだけから腸内細菌の状態を言い当てない。**
    素材の4つめ「出なくても自分を責めず焦らない」は**このアプリの芯と同じ向きなので、
    画面のいちばん上に出す**（テストが並び順を見る）。**長くいきむことは勧めない**
    （痔・脱肛・血圧の上がりすぎ。素材が言っているのは「座ること自体に意味がある」まで）。
54. **飲む人を責める書き方をしない**（`alcohol.js`）。「酒は百薬の長ではない」は訂正として
    書くが、**責める書き方をすると、飲んでいる人は画面を閉じるだけで量は変わらない。**
    やめられないことを意志の弱さの話にせず相談先へつなぎ、**依存の判定もチェックリストも作らない。**
    量の目安（純アルコール約20グラム）は公的な数字なので引用として持つが、**計算はしない。**
55. **「人を貶める言い方」は、引用と、それを断る一文の中にだけ置く**（「情弱」「猛毒」など）。
    **その検査は「行で数える」**——元の文が `'…' + '…'` と複数行に分けて書いてあると、
    実行時の値でソースを差し引く方法では消えず、必ず落ちる（実際にここで踏んだ）。
56. **同じ出典の中で言うことが割れている所も、食い違いとして並べる**（`tsukemonoViews`）。
    漬物は同じ動画の中で「一口で認知症2倍の猛毒」とも「無添加を適量なら認知症予防に直結」とも
    言われている。**強い言葉が先に来て記憶に残るので、あとの但し書きは読まれにくい**——
    だから並べる。出典どうしの食い違い（`SOURCE_CONFLICTS`）とは層が違うので別に持つ。
57. **賞の名前を、あとから付いた数字の裏づけにしない。** 「オートファジーはノーベル賞を
    受賞した概念だから16時間で効く」は、受賞したのは仕組みの研究であって、
    「人が16時間食べなければこうなる」という話ではない。
58. **過敏性腸症候群そのものを扱う画面（`data/ibs.js`＋`components/Ibs.jsx`）は、
    このアプリの中心なので、いちばん慎重に作ってある。** 芯は
    **「検査で異常が出ない＝気のせい、ではない」を、型の一覧より画面の前に置くこと**
    （`IBS_EXCLUSION`。ほかの病気を先に外して最後に残ったときに付く呼び名なので、
    異常が出ないことは**その呼び名が付くための条件のほう**）。先に型を見せると、
    そこへ自分を当てはめて終わってしまうので、並び順もテストが機械チェックする。
59. **記録から型を当てない**（下痢型・便秘型・混合型・分類不能型は並べるだけ。決まり1と同じ線）。
    **ガスで困っている人はこの分け方に居場所がない**——出典自身が「分類にガス型はされていない」
    と言っているので必ず添える（型が無い＝困っていない、ではない）。
60. **医療者を貶める言い方を採らない。** 出典の「無知な医者が多い」「精神科に回されて
    ますます悪くなる」は、そのまま置くと**いちばん助けが要る人が受診をやめる理由になる**。
    **心療内科・精神科の紹介を「行き止まり」として書かない**（決まり24と同じ線）。
    **市販薬を軽んじる言い方もしない**（「宝くじレベル」。決まり33の線を、貶しの側にも引く）。
    テストは決まり55と同じく**行で数える**（「無知な医者」が出てよいのは claim の1行だけ）。
61. **検査もサプリも勧めない。** 自費の腸内フローラ検査・菌の相性検査は、結果から
    食べるもの・飲む菌を決められるところまで確立していない。**抗菌のハーブ・抗生物質を
    自分で手に入れることははっきり止める**（中身も量も確かめようがない）。
    ペパーミントオイルは紹介するだけで商品を勧めない（決まり19の線をサプリにも引く）。
62. **同じ数字がそろっても、裏づけにしない**（`lib/conflicts.js` の `mealGapViews`）。
    食事の間隔「4時間」はアダムスキー式・お掃除ぜん動（MMC）・体験談の3つが言うが、
    **理由はそれぞれ別で、どれももとをたどれていない。** 3つ並べて理由まで書く。
63. **既存の食い違いを壊さずに足す。** `fermentViews` は3つを返す前提で画面が組まれている
    （テストも3つを固定している）ので、4つめ（胃腸が弱っているときはやめる）は
    **`ibsFermentViews` という別の関数**として持つ。`breakfastViews` も2つのまま増やさない。
    **4つめだけ「いま弱っているとき」という条件つきの体験談**で層が違うことも views に書く。
64. **出典の数え間違いをそのまま写さない。** 体験談の出典は「八つ目」と言いながら7つしか
    挙げていない。**7つとして並べ、そのことを画面にも書く**（黙って合わせると理由が消える）。
    文字起こしで**数字が欠けている所も、勝手に作らない**（`missing_number`）。
65. **目次のタイトル衝突は、追加のたびに必ず確かめる**（決まり31・40の再発）。
    今回は「体を動かす」が腸のお掃除の側（ストレスを減らす方法）と衝突して静かに落ちた。
    別物なのでどちらも残し、**新しいほうの題を「体を動かして腸の動きを整える」に変えた**。
66. **【ユーザー指定・2026-09-03。以後ずっと守る】整腸剤の情報が出たら、
    ホームの「整腸剤」の項目（`lib/homeTopics.js` の `probioticHome`＋`Home.jsx` の
    `#home-probiotic`）も必ず一緒に更新する。** 中身は `data/probiotics.js`・
    `data/otcDrugs.js` から毎回導くので、**元データに足せばホームの内訳も自動で増える**
    （ホーム専用の手書きの一覧を作らない）。決まり19の線はそのまま——商品を勧めない・
    順位を付けない・飲み合わせを調べない・飲めなかった日を数えない。
67. **【ユーザー指定・2026-09-03。以後ずっと守る】酪酸菌の情報が出たら、
    ホームの「あなたに向いた腸活」の中の「酪酸菌まとめ」（`GUT_CARE_TOPICS` の
    `butyrate`）に積み上げる。** 中身は `data/butyrate.js` から毎回導く。
    **出典自身が取り下げた説（痩せ菌・デブ菌）を、まとめからも落とさない**（決まり37）。
68. **「あなたに向いた腸活」でも、向き不向きをアプリが決めない**（`GUT_CARE_NOTE`）。
    題に「あなたに向いた」と付いている以上、**ここを外すとこの項目はそのまま
    「アプリが向き不向きを判定する機能」になる**（決まり1・4を内側から壊す）。
69. **まとまりを1件足すのに、ホームへ if を書き足さない。** `GUT_CARE_TOPICS` の各件が
    `rows()`／`note`／`source()` を持ち、画面はそれを読むだけ（テストが
    `topic.id === '…'` の不在を機械チェックする）。目次も同じ配列から導くので、
    1件足せばホーム・目次の両方が自動で増える。
70. **出回っているうわさは3つめの層**（`butyrate.js` の `BUTYRATE_RUMORS`）。
    出典が取り下げた説（`WITHDRAWN`）とも訂正（`CORRECTIONS`）とも層が違う。
    **打ち消しを逆向きの断定に読み替えない**——「酪酸菌が大腸がんの原因」は
    フソバクテリウム・ヌクレアタムの話の切り取り、と出典は説明するが、
    **そこから「だから酪酸はがんを抑える」へは行かない**（決まり30）。
71. **整腸剤に順位を付けない・がんの治療に自分で足さない**（`PROBIOTIC_CORRECTIONS` の
    `ranking`／`chemo`）。「6選のナンバーワン」「抗がん剤と一緒で効く率が4倍」は、
    出典が但し書きを添えていても**数字のほうが先に記憶に残る**。
72. **サプリの話は引き受ける範囲を書く**（`SUPPLEMENT_SCOPE_NOTE`。決まり48と同じ形）。
    菌のものだけで、ビタミン・ミネラル・NMN は扱わない。
73. **腸カンジダ・カビ毒・重金属のデトックスは扱わない**（`IBS_OUT_OF_SCOPE`）。
    **「つらいのは好転反応」を採らない**——熱や頭痛が出たなら体調が悪くなったということ。
74. **金額でおどさない・期間を約束しない**（`IBS_CORRECTIONS` の `money_talk`／
    `IBS_UNVERIFIED.five_years`）。「時間は何も解決しない」も採らない（決まり5と同じ線）。
75. **目次へ流すところは1つ残らず `**` を replace で落とす**（決まり49の再発）。
    `fromCare` の菌・製品と `fromButyrate` のはたらきで、`**` を含む文を足した瞬間に
    `cleanup.test.mjs` が捕まえた。
76. **【ユーザー指定・2026-09-03。以後ずっと守る】横断のまとめは `lib/digest.js`＋
    `components/Digest.jsx`（画面 `view:'digest'`＝「まとめて見る」）が置き場。**
    素材が1本ずつ別の画面に入るので、同じ種類のもの（訂正・裏が取れていない主張・
    食い違い・扱わないこと・出典）が14ファイルに散っていた。**新しい主張を1件も書かず、
    元データから毎回集めるだけ**（まとめ専用の手書きの一覧を作らない。決まり66・67・69と同じ形）。
77. **新しい素材のデータファイルを足したら、`DIGEST_SUBJECTS` にも1件足す。**
    足さないと訂正・裏が取れていない主張・出典が**静かにまとめから漏れる**
    （鍼灸アプリの `seedRegistry.js` で実際に起きた事故と同じ形）。
    `test/digest.test.mjs` が件数の下限を見張る。
78. **まとめでも採点しない・順位を付けない。** 多い順に並べ替えない（並べ替えると
    「訂正が多い素材＝悪い出典」に見える）。素材の並びは登録順のまま。
79. **横断のまとまり（検査・ストレスと自律神経・水分・体重）は、集める項目を手で書く**
    （`CROSS_TOPICS[].refs`）。語の一致で拾うと、関係ないものを関係あるように見せる
    （決まり29の `fodmapName` と同じ線）。**空箱を作らない**・参照先が消えたら黙って落とす。
80. **まとめが判定に変わらないようにする。** 検査は勧めない／ストレスをお腹の原因と決めない・
    お腹の不調を気持ちの問題にしない／水分に「1日◯リットル」という手元に無い基準を持たない／
    体重は記録しない（理由がないのに減っているときだけ受診の目安のほうへ）。
81. **本文はその項目の言い方のまま取り出す**（`bodyOf`）。訂正は `correction`、
    取り下げた説は `withdrawn`、裏が取れていない主張は `note`。まとめの側で言い換えると、
    元の画面と意味が変わったことに誰も気づけない。
82. **受診の目安への導線は共通部品**（`components/RedFlagLink.jsx`）。決まり19と書いていたのに
    実際は**27画面中12画面からしか行けず、その多くは注意が出たときだけの条件つき**だった。
    受診の目安の画面自身と「しらべる」（一覧の先頭が受診の目安そのもの）以外の全画面に置き、
    `test/digest.test.mjs` が画面のファイルを1つずつ見て機械チェックする。
83. **見張りは「アプリ自身が言っていること」だけを見る**（決まり26の再発）。
    `順位`／`危険度` を探したとき、**決まりを守ると書いてあるコメントのほうが引っかかった**。
    コメント行（`//` `/*` `*`）を落としてから見る。id に使った語（`redflag-link` の `red`）も
    同じで、素の英単語で探さない。
84. **【2026-09-03。ユーザー指定の30件＋鍼灸アプリからの移植8件を実装】**
    新しく増えたファイルの置き場をここにまとめる。**どれも既存の決まりを崩していない**
    （診断しない・点数を付けない・連続日数を煽らない・平均を出さない・
    相関を因果として書かない・通知と匿名掲示板は作らない・端末内保存のみ）。
    - **`lib/gaps.js`**（空いている日。責めない・連続日数を数えない）
    - **`lib/periods.js`**（いつもと違う期間の印。**予測も判定もしない**。旅行・薬・生理など）
    - **`lib/compare.js`**（並べるだけの層。食べた日／曜日／暮らし／前の期間／やめてみた前後／
      整腸剤の前後。**矢印を書かない・割合を先に出さない・片方が3日未満なら並べない**）
    - **`lib/weekly.js`**（今週と先週。**良し悪しを返さない**・平均を持たない）
    - **`lib/visits.js`**（通院・聞きたいこと・受診のあと。**鳴らさない**・言われたことを整えない）
    - **`lib/share.js`**（印刷・共有・書き出し。**印刷は白地に黒字**・共有が使えなければ
      ダウンロードへ落とす・貼る文字は `escapeHtml`）
    - **`lib/csv.js`**（**数式にさせない・BOM・判定と平均の列を作らない**）
    - **`lib/transfer.js`＋`lib/qr.js`**（別の端末へ渡す。**入らない QR を入ったふりしない**。
      QR のライブラリは押した時に読み込む。`src/lib` の「送る仕組みを持たない」を守るため
      **`qr.js` 自身はライブラリを読み込まない**）
    - **`lib/errorLog.js`**（端末内のエラー。外へ送らない・20件で止まる・人を責めない）
    - **`lib/speak.js`／`lib/voice.js`**（読み上げ・音声入力。**どちらも既定オフ**。
      音声入力は端末内保存の**はっきりした例外**として設定画面に理由を書く）
    - **読み物5つ**：`data/diseases.js`（当てはめる仕掛けを作らない）・`data/breathing.js`
      （やめどきをやり方より前に置く）・`data/ibsCare.js`（**型は自分で選ぶ**）・
      `data/eatingOut.js`（店と商品の名前を持たない・値段の話をしない）・`data/flora.js`
      （数値と断定を外す）
    - **共通部品**：`components/Finder.jsx`（さがす）・`ErrorBoundary.jsx`（落ちても
      行き止まりにしない。**中に他画面へのボタンを増やさない**）・`ScrollArrows.jsx`
    - **PWA**：`public/manifest.webmanifest`・`public/sw.js`（ページは network-first、
      アセットは cache-first、**通知の仕掛けは持たない**）・アイコン3枚。
      **`sw.js` の場所を `import.meta.url` から組み立てない**（束ねられた JS は `assets/` の
      下にあるので 404。基準は `document.baseURI`。実機で踏んだ）
    - **読みやすさ**：`data-text`／`data-contrast`／`data-motion` を根っこに立てて CSS だけで
      効かせる（画面ごとに書かない・**色を増やさない**）。`@media print` で白地に反転する。
85. **ホーム画面のアイコンだけ PNG を持つ**（ユーザー指定）。決まり9「画像ファイルを持たない」の
    はっきりした例外で、理由は端末の側の都合（SVG のアイコンを受けない端末がある）。
    **色は入れない**（灰色だけで焼く）。アプリの中の絵は今までどおりその場に SVG で描く。
86. **新しい画面・読み物を足したら、`DIGEST_SUBJECTS`（決まり77）と `toc.js` の派生関数の
    両方に足す。** 片方だけだと、まとめか目次のどちらかから静かに漏れる。
87. **【2026-09-05】「しらべる」の一覧は `data/knowMenu.js` が単一の正。**
    画面が増えて**見出しの無い24件の平らな一覧**になり、設定へ行くのに3画面ぶん
    スクロールしていた。4つのまとまり（受診のために3／記録の道具2／読み物19／
    このアプリのこと2）に分け、さがす欄（`Finder.jsx`）を置いた。
    `Know.jsx` は配列を読んで並べるだけ（画面に行き先を書き写さない）。
    - **1つの画面につき入口はひとつ**（同じ行き先を2か所に置かない。Ouro 項目25と同じ線）。
    - **URL はデータに書かない**——`src/data` は URL を持たない見張りがあるので、
      地図のリンクは `Know.jsx` の `LINKS` が持つ（見張りを緩めず置き場所を変える）。
    - **さがす欄は `keywords` でふだんの言い方も拾う**（実機で「せってい」が
      当たらなかった。説明文の「設定」は漢字なので読みと突き合わない）。
      **新しい主張を作らない**——中にあるものの呼び名だけ。
88. **記録の道具（通院・いつもと違う期間）は、カレンダーからも行ける**（`#cal-tools`）。
    読み物ではなく記録の仕組みなのに、読み物の入口にだけあった。
89. **下部ナビの列の数を直書きしない**（列は `NAV.length` から出す）。
    CSS に5列が固定されていて、6つめを足すと崩れる状態だった。
    **いまは5つのままにする**——常設バー（受診メモ）のほうが強い場所なので、
    受診をナビへ足すと同じ入口が2つになる。
テストは `chou/test/*.test.mjs`（278件。ルートの `npm test` の再帰探索からも実行される）。

## 目次・索引の共通ルール（ユーザー指定・全アプリ共通、2026-08-21）
一覧／目次／索引を作る時は、どのアプリでも次を守る。
1. **並びは「あ〜ん」→「A〜Z」** の順。読み（ひらがな）で並べ替える。
2. **数字も読み方で振り分ける** — 見た目の数字順で先頭に固めない
   （例「20歳未満」→ にじゅう…→ **な行**、「361穴」→ さんびゃく…→ **さ行**）。
   鍼灸アプリは `src/lib/yomi.js` の `numberToKana`/`readingInfo`、腰痛ナビは
   `youtsu-navi/src/lib/yomi.js` の `autoReading` が担当する。
3. **読みは明示、自動推定しない** — 漢字を含む項目は必ず読みをデータに持たせる（誤読防止）。
4. **タイトルは重複させない・誤りを残さない** — 機械チェックできる形（テスト）で担保する。
5. **目次の文字は大きめ**、タップで該当箇所へ飛べるようにする（飛び先はスクロール位置を
   固定ヘッダーの下に出し、一時的にハイライトする）。

## デプロイ
- `.github/workflows/deploy.yml` は **main への push** でビルド＆GitHub Pages 公開。
- 作業は `claude/acupuncture-exam-app-7p4zdh` で行い、コミット後に
  `git push origin HEAD:main` で main を fast-forward してデプロイ（ユーザー了承済みの運用）。
- コンテナが作業ツリーを古いコミットへ戻すことがある。正は origin。
  `git fetch && git reset --hard origin/<branch>` で復旧してから作業する。
- **変更後は必ず最新URLを出す（ユーザー指定・2026-08-29 に「これからは毎回URLだして」と再指示）。**
  返答の最後に、触ったアプリの公開URLを毎回そのまま書く。省略しない。
  まだ main に入っていない（＝公開されていない）ときも**URLは出したうえで、
  「main に入るまでは404」と一行そえる**——URLだけ出して公開済みのように見せない。
  同梱アプリのURL一覧：
  | アプリ | URL |
  |---|---|
  | 鍼灸国試 対策アプリ（ルート） | https://youtaix656-eng.github.io/-/ |
  | るるくる業務マニュアル | https://youtaix656-eng.github.io/-/rirakuru |
  | 分割睡眠トラッカー | https://youtaix656-eng.github.io/-/sleep-tracker |
  | 腰痛ナビ | https://youtaix656-eng.github.io/-/youtsu-navi |
  | 変革ノート | https://youtaix656-eng.github.io/-/henkaku-note |
  | Ouro | https://youtaix656-eng.github.io/-/ouro |
  | 鏡（かがみ） | https://youtaix656-eng.github.io/-/kagami |
  | 資格ラボ | https://youtaix656-eng.github.io/-/shikaku-lab |
  | 間合い（まあい） | https://youtaix656-eng.github.io/-/maai |
  | 腸（ちょう） | https://youtaix656-eng.github.io/-/chou |
- 読みやすい写真を使う。

## 継続して意識する開発方針（ユーザー指定・重要）
1. **全科目のコンテンツ拡充＋網羅管理** — 14科目（下記「科目一覧」参照）を拡充。
   「出題基準の項目 × 収録数」の網羅マップ（`CoverageMap.jsx`）で手薄な所を可視化し、
   抜け漏れなく作る。問題を足すたびに網羅状況を意識する。
   **2026-09-02：`src/data/seedRegistry.js`の`SEED_BANKS`に病理学概論・リハビリテーション医学・
   東洋医学概論・経絡経穴概論・東洋医学臨床論・はり理論・きゅう理論の7科目が登録漏れしており
   （新しい科目バンクを足したら1行追加する、という同ファイルの注意書きが守られていなかった）、
   `npm run validate`のスキーマ／重複チェックと網羅マップ集計がこの7科目・約2100問を
   完全に素通りしていたバグを発見・修正した。修正後は該当7科目にもスキーマ違反・重複なしを
   確認済み。この修正で判明した正しい科目別収録数（充足率100%・全13科目がしきい値以上）により、
   科目単位の「手薄科目」は現状ない。残る手薄はジャンル単位（`npm run validate`の
   「埋めるべき優先度トップ10」参照、2026-09-02時点は生理学・臨床医学総論・解剖学の一部ジャンルに
   集中）。**新しい科目データファイルを追加する時は、`useStore.js`への配線だけでなく
   `seedRegistry.js`の`SEED_BANKS`への登録も忘れないこと**（忘れるとCI検証・網羅マップの
   両方から静かに漏れる）。
2. **正確性・鮮度の管理** — 医療内容は正確に。毎年変わる数字（国民医療費・統計等）は
   更新前提。曖昧・要更新は「※要確認」を付け、数値問題を抽出した年1回の見直しを想定。
   **数値の鮮度チェックは定期Routineで自動化済み**（`src/data/mindmapData.js`の
   `NUMBER_FACTS`のうち「※要確認」の項目をWebSearchで確認し、更新があればユーザーへ連絡する。
   2026-08-20設定）。
3. **本番同形式の模試** — ✅実装済み（`src/components/Exam.jsx`）。午前90問／午後90問
   （科目別配分は`src/data/examBlueprint.js`）・得意な問題／苦手な問題（正答率から
   ジャンル・キーワードを自動提案）・選択式（科目/ジャンル/キーワード検索）の5モード。
   **総合問題（連問）はユーザーから投げられ次第、誤りなく正確に即座に
   `src/data/integratedQuestions.js`へ実データ追加する**（他の科目データと同様に
   `<X>_VERSION`＋`useStore.js`の`dedupeAgainst`で増分シード）。追加後は、その総合問題から
   派生できる類題（一問一答等、「標準変換プロンプト」の型に沿う）を作成するかユーザーに提案する。
4. **復習（SRS）の前面化** — ✅実装済み。ホーム画面に「今日の復習」カード
   （`dueReviewQuestions`、`src/lib/srs.js`）。間違い→復習→定着のループは
   `missTypes.js`（誤答理由：勘違い/知識不足/ケアレス）、`forgetting.js`（忘却予測）、
   `weakClusters.js`（弱点クラスタ）でも多角的に支援済み（下記「機能一覧」参照）。
5. **画像問題への対応** — ✅基盤は実装済み。`question.image`（外部URL）と
   `question.figure`（`src/data/figures.jsx`のオフラインSVG図、キーで参照）の2方式。
   図問題データは`src/data/zumondaiQuestions.js`（現状サンプルのみ、拡充余地あり）。
6. **医療概論・衛生学公衆衛生学の弱点分析→提案（2026-08-20追加・ユーザー指定）** —
   この2科目は今後、`iryouQuestions.js`／`eiseiQuestions.js`のジャンル別収録数を
   `CoverageMap.jsx`と同じロジックで定期的に分析し、手薄なジャンルを特定したら
   問題追加をユーザーに**提案**する（無断で追加しない）。追加する問題は**過去問の情報と
   ネット検索（WebSearch）をメインの情報源**とする。過去問PDFが手元に無い状態で記憶から
   問題文を捏造しない（既存のミス防止ルールと同じ理由）。

## コンテンツ投入の型（過去問→教材化）
過去問1問ごとに「原問(4択)×1＋一問一答×4（角度A核心/B定義・逆引き/C鑑別/D○×）」を
**被りなく・網羅的**に生成し、音声学習・マインドマップ・一問一答へ組み込む。
各問に tags（正式名称の重要語）、まぎらわしい対比は `COMPARISONS`、数値は `NUMBER_FACTS`
（`src/data/mindmapData.js`）へ。過去問は著作物のため公開リポジトリに素の問題文を
大量常設しない方針（派生・自作解説で保持。医療概論の同梱は個人利用の範囲）。
語呂合わせサイト等、外部の教材サイトを参考資料として渡された場合も同じ方針：
経穴名・順序・寸法などの**事実**は正確に引用してよいが、語呂合わせの**文章そのもの**
（著作権のある創作表現）はコピーしない。事実は保ったまま独自の語呂合わせ文を作る。

### 標準変換プロンプト（ユーザー指定・これを既定手順とする）
ユーザーが科目名＋過去問PDFを貼ったら、明示がなくても毎回この手順で教材化する:
1. **出題基準を先に読む** — PDFの1ページ目は国家試験の出題基準。記憶し、そこから科目名と
   ジャンル分け（大項目｜中項目 → `genre`）を導出する。
2. **ページ順の整列** — 各ページ下部の印刷ページ番号で並べ替えてから読み込む。
3. **生成物（過去問1問ごと）** — 原問(4択そのまま)×1＋一問一答×4を被りなく網羅生成:
   - 原問: 選択肢・正解は忠実に。解説は要点を"自分の言葉"で（出版社の文そのままにしない）。
   - 一問一答の角度: **A核心**（原問の事実を端的な四択）/ **B定義・逆引き**（正解語の意味・役割：語→説明 or 説明→語）/
     **C鑑別**（誤答選択肢の1つの"正しい内容"を問う四択＝ひっかけ対策）/ **D確認**（○×正誤×おまかせ：数字・年齢区分・年号があれば数値を問う）。
   - 4問の答えと論点は互いに異なること（言い換えの重複禁止）。4問で網羅しきれなければ例外的に増やす。
4. **図・表からも一問一答** を生成（網羅できるよう問題数はお任せ）。
5. **キーワード** — 各問に tags（重要語2〜4個）、表記は正式名称に統一（例「ケアマネ」→「介護支援専門員」）。
6. **マインドマップ** — 該当あれば comparisons（まぎらわしい対比）・numbers（数値の要点）を追加。
7. **厳守（誤り・被り防止）** — 医療的に正確に／曖昧は「※要確認」／原問の各選択肢(正・誤)と解説の要点を4問のどこかで必ずカバー／
   既出（同じ答え＋同じ論点）とは重複させない（既出は「重複」と記し作らない）／四択は選択肢4・正解1、○×は2択。
8. **反映先** — 音声学習・キーワード検索・目次・マインドマップすべてに追加（`subject`/`genre`/`tags`/`deck` で自動連携）。
   科目ごとに `src/data/<科目>Questions.js`＋`<X>_VERSION` を作り、`useStore.js` で `dedupeAgainst` 増分シード。
9. **読み取れないリスト（※1）** — 読み取れないページと該当問題を「1.日付 時間 2.科目名/タイトル 3.ページ下の印刷ページ数(P10等)」で控える。
   後で読み取れたらリストから削除し、**削除した旨を必ずユーザーへ連絡**する。
10. 完了後は最新URLを出し、下記「問題作成後の必須チェック」の結果を報告する。

### 問題作成後の必須チェック（毎回）
問題を作成・追加したら、取り込み/反映の前に必ず正誤チェックを行い、結果を報告する。
1. **答えの妥当性** — 正解番号が選択肢の範囲内・意図した選択肢を指すか（0/1始まりの取り違え注意）。
2. **医療的正確性** — 内容が正しいか。毎年変わる数字は最新か。曖昧は「※要確認」を付す。
3. **重複（被り）** — 問題文・答え・論点が既存や他の生成分と重複しないか（`dedupeAgainst` ＋
   答え＋論点の一致も確認）。同一問題文の別問は末尾に（第XX回）。
4. **形式** — 四択は選択肢4・正解1、○×は2択。解説あり。
アプリ内の誤りチェック機能（`src/components/QuestionTools.jsx` の `runAllChecks`）も活用する。

## データモデル / 実装メモ
- 問題: `{ id, subject, type:'choice'|'ox', question, choices[], answer(0始まり), explanation,
  round?, tags?, deck?, genre?, source?, image?, figure? }`。
- 検索の役割分担（音声学習）: **科目名=subject / ジャンル=q.genre（13/14科目で実利用中。
  医療概論=iryouQuestions.jsのみ未使用） / キーワード=tags∪連結キーワード**。
- 「回」フィールド（`round`）は科目データファイルによって`round: 34`（数値）と
  `round: "第34回"`（フル文字列）が混在する。比較・並び替え・表示は必ず
  `src/lib/round.js`の`roundKey`/`formatRound`/`isSameRound`を経由すること（直接
  `String(q.round)`比較や`第{r}回`のようなテンプレート結合をすると、フル文字列側で
  「第第34回回」のように二重表記される）。新しい画面で「回」の絞り込み・表示を追加する時も
  同様にこのヘルパーを使う。
- `source`：「4択問題」のファイル分け用（`ChoiceQuiz.jsx`）。未設定は既定で「過去問」扱い
  （`questionSourceId()`、`src/data/examScope.js`）。模試・その他を追加する時だけ明示指定。
- 科目一覧・正式表記は `src/data/examScope.js`（`SUBJECT_TAG_NAMES` / `EXAM_SUBJECTS` /
  `CHOICE_QUIZ_SUBJECTS`）を単一の正とする。14科目：医療概論・衛生学公衆衛生学・関係法規・
  解剖学・生理学・病理学概論・臨床医学総論・臨床医学各論・リハビリテーション医学・
  東洋医学概論・経絡経穴概論・東洋医学臨床論・はり理論・きゅう理論。
- 各科目の同梱データは `src/data/<科目>Questions.js`＋`<X>_VERSION`（バッチ増分方式）。
  `useStore.js`で`dedupeAgainst`により起動時に未収録分だけ取り込む。
- 音声学習（`src/components/AudioMode.jsx`）: 検索フィルタ＋連結学習モード1〜10
  （連鎖/比較/数値/穴埋め/弱点/選択肢読み/章通し/ナレーション/適応/今日の連結、よく使う順に自動並べ替え）。
  「今日のおすすめ」バナー（復習が溜まっていたら復習読み上げを提案）、セッション内の弱点分析
  （△✕をつけた問題からジャンル・キーワードを集計し読み上げ）、問題数ベースの「今日の目標」
  （タイマーの代わり）、自己採点にブックマークを追加、読み方の手動補正辞書（TTS誤読の修正、
  `settings.pronunciationFixes`）も実装済み。
- **「復習」の定義は `src/lib/reviewPool.js` を単一の正とする**（Session.jsx・AudioMode.jsx が共用。
  画面ごとに定義がズレて再発しないための集約）。
  - `isInReview`/`isDue`（`src/lib/srs.js`）：問題1問の復習対象・期限判定の基礎。
  - `dueReviewQuestions`（`useStore.js`）：ホーム「今日の復習」・`Review.jsx`が使う、期限が来た
    問題だけの単純なリスト（`isInReview`＋`isDue`のみ、忘却リスクは混ぜない）。
  - `reviewPoolFor(pool, srs)`（`reviewPool.js`）：Session.jsxの「すべて復習」・AudioModeの
    「間違えた問題を読み上げる」が使う、より広い定義。期限が来た問題を優先しつつ、一度も
    間違えていないが保持率が下がってきた問題（忘却リスク、しきい値`FORGETTING_THRESHOLD=0.4`）を
    「念のため確認」として少数混ぜる。
  - `buildWeaknessSummary`/`weaknessSummaryToText`：誤答・あいまい（△✕）問題からジャンル・
    キーワードの傾向を文章化（Session.jsxの完了画面／AudioModeのセッション内弱点分析で共用）。
  - `recommendNewPct`：新規問題と復習の比率を復習の溜まり具合から自動提案（Session.jsxの
    「今日のおすすめ」）。
  - 新しい画面で「復習」を扱う時は、この4関数を再実装せず`reviewPool.js`からimportすること。
- **Review.jsx（間違えた問題）の主な仕組み**（2026-08-19拡張）：
  - 出題対象は`reviewPoolFor(questions, srs)`（マスター後の「念のため確認」込み）。
    純粋な`isInReview`だけの`reviewQuestions`は件数表示など厳密な集計にのみ使う。
  - 絞り込み：科目（複数選択・`SUBJECT_TAG_NAMES`順）／キーワード（プルダウン）／回／
    ブックマーク／直近の誤答（今日・今週）／忘却リスク・誤答回数の下限。すべて
    `src/lib/reviewOrder.js`の`filterReview`が単一の実装（他画面から使う時もここを拡張する）。
  - リーチ（要注意）：`src/lib/srs.js`の`LEECH_THRESHOLD`（既定8）・`isLeech(state)`が単一の正。
    Review.jsxの一覧バッジと`MistakeNote.jsx`の自動並べ替え（要注意を先頭にピン留め）で共用。
  - 個別操作：`useStore.js`の`setNextDue(id, delayMs)`（スヌーズ・誤答理由別の間隔調整で共用）、
    `removeFromReview(id)`（○5回連続と同じ状態にして手動で外す）。
  - 出題順は`spaceByOrigin`で原問と派生（同じ過去問由来）を離す（Session.jsxの`spaceById`と同じ考え方、
    実装はReview.jsx内に独立）。
  - 誤答理由（型）別の再出題間隔は`src/lib/missTypes.js`の`MISS_TYPE_DELAY_MS`が単一の正。
  - 音声で復習（`onGoAudio`）は、Review画面で絞り込み中ならその条件のidだけをAudioModeへ渡す
    （`App.jsx`の`audioReview`が`true`（全体）または`{ids}`（絞り込み結果）を保持し、
    `AudioMode.jsx`の`customReviewIds`で反映）。
- **習慣化の仕組み**（「始めるまでに迷う」「戻るルールがない」対策、2026-08-19追加）：
  - `src/lib/nextTask.js`（明日の最初の1タスク）：Session.jsxの完了画面で次にやることを1つだけ
    決めて保存すると、Home.jsxの一番上に固定表示される。「何から始めるか」で迷う時間をなくす。
  - `src/lib/streakBreak.js`（できなかった日の原因分解）：`detectBrokenYesterday(history)`で
    「きのうだけ記録が無く前日には記録がある＝直前で連続が途切れた」状態を検知し、Home.jsxで
    理由（時間がなかった／やる気が出なかった等）をワンタップ記録できるカードを出す。責めるのが
    目的ではなく、記録して「また今日から戻る」ための一手間。同じ日に何度も出さないよう
    記録済み／「あとで」でdismissした日はidbで管理。
- **下部ナビ・合格ロードマップ**（2026-08-19変更）：下部ナビは6画面
  `ホーム／カレンダー／一問一答／復習／音声／模試`（`src/App.jsx`の`NAV`配列）。カレンダーは
  頻繁に使う下部ナビ相当のため即時import（lazyに戻さない）。加えて、下部ナビのすぐ上に
  常設の「🗺️ 合格ロードマップ」バー（`.roadmap-bar`、ロードマップ画面自体では非表示）があり、
  どの画面からでもロードマップを開ける。音声ミニプレーヤーはこのバーが出ている時だけ1段
  上（`.mini-player.lifted`）にずれて重ならないようにする。
  - **ロードマップのフェーズ定義は `src/data/roadmapPhases.js`（`ROADMAP_PHASES`）を単一の正とする**。
    以前はCalendar.jsx（日付範囲・色のみ）とRoadmap.jsx（やること/NG等の詳細）で別々に
    フェーズを持っていてズレる恐れがあったため統合した。**ロードマップの内容を追加・修正する時は
    必ずこのファイルだけを直す**（Roadmap.jsxに直接書き足さない）。Calendar.jsxとRoadmap.jsxは
    互いに「全期間のロードマップを見る」「カレンダーで予定に落とし込む」の相互リンクを持つ。
    Calendar.jsxはこの凡例を画面最上部（説明文の直下）に表示し、日付セルの背景色でフェーズを
    色分け（`hexToRgba`で半透明化）。Roadmap.jsxは`ROADMAP_MONTHS`＋`phasesInMonth`で
    フェーズ別カードとは別に「月別スケジュール（8月〜2月）」も自動生成する（月ごとにフェーズを
    書き直すのではなく、`ROADMAP_PHASES`の日付から導出するので二重管理にならない）。
- **右端のスクロール矢印**（`src/components/ScrollArrows.jsx`）：長い画面を1画面ぶんずつ動かす。
  **`App.jsx` で即時import し、`renderView()` の中に入れない**（入れるとその画面にしか出ない）。
  **動かない画面では丸ごと出さない**（押しても何も起きないボタンを出さない）。端まで来た側は
  **消さずに薄くする**（位置が動くと押し損ねる）。重なりは `z-index: 27` ＝ ロードマップバー(28)・
  ミニプレーヤー(29)・下部ナビ(30)・モーダル(70/100) より下。**中身の高さは ResizeObserver で見る**
  ——lazy な画面は遅れて入るので、`view` の変化だけ見ると読み込み前の短い状態で測って矢印が出ない。
  画面が広い時は `--max-w` の右端へ寄せる。Ouro 側（`ouro/src/components/ScrollArrows.jsx`）と
  同じ作りにしてあるので、**片方を直したらもう片方も見る**こと。
  ※ 画面ではなく全画面共通のUIなので、`featureRegistry.js` には登録しない（`view` を持たないため）。
- **長押しで削除**（`Home.jsx`の`useLongPress`）：「前回の続きから」カードなど、誤タップで
  失いたくない操作は、タップ＝通常動作／長押し（550ms）＝「削除しますか？ はい・いいえ」の
  確認を出す。同じパターンが必要な時はこのフックを再利用する。
- 端末だけに取り込むリンク: `#import=`（`src/lib/noteshare.js`）。重複は
  `dedupeAgainst`（問題文で判定、`src/lib/importer.js`）。同一問題文の別問は
  末尾に（第XX回）を付けて衝突回避。

## 機能一覧（早見表・2026年8月更新）— 新機能を提案・追加する前に必ずここを見る
「〇〇が無い」と判断する前に、下記と `components/`・`lib/` 全体を検索すること
（過去に誤って「未実装」と案内した失敗があるため）。
| 分野 | どこにあるか | 中身 |
|---|---|---|
| 模擬試験 | `Exam.jsx` | 午前/午後(本番同形式)・得意/苦手(自動提案)・選択式の5モード |
| 4択問題 | `ChoiceQuiz.jsx` | ファイル分け(過去問/模試/その他)→科目の2段階 |
| SRS復習 | `srs.js`／Home「今日の復習」／`Review.jsx` | 間隔反復。ホームに期限件数を前面化 |
| 忘却予測 | `lib/forgetting.js`（Analytics内`InsightsSection.jsx`） | 保持率を指数減衰で推定し先読み表示 |
| 弱点クラスタ | `lib/weakClusters.js`（同上） | タグ共起から弱点テーマを自動抽出 |
| 難易度推定 | `lib/difficulty.js`（同上） | 正答率から難問を抽出 |
| 誤答理由の分類 | `lib/missTypes.js`／`Review.jsx` | 勘違い/知識不足/ケアレスをワンタップ記録、型別に解説を出し分け |
| 自己説明ステップ | `QuestionCard.jsx`の`whyPrompt` | 誤答時に「なぜこの答え？」を自動で促す |
| 知識グラフ・連想 | `KnowledgeGraph.jsx`＋`AssocQuiz.jsx`／`AssocTrainer.jsx`／`RelationAuthor.jsx` | 経路クイズ・束グルーピング・対比識別ドリル・関係の手動オーサリング |
| 連結学習 | `ConnectedLearning.jsx`＋`lib/connectlab.js` | 今日の1問・キーワード自動提案・表記ゆれ統合・ヒートマップ |
| 語呂合わせ | `kwMeta`（保存）／`MnemonicNotebook.jsx`（一覧・編集） | 登録・一覧・関連問題数表示・**科目でしぼる検索**（`examScope.js`の科目順に1〜14で番号付き）・**ふりがな表示**（`reading`、手入力または`lib/yomi.js`の`TERM_READINGS`のみ。自動生成はしない＝誤読防止）。組み込みの語呂合わせは`src/data/defaultMnemonics.js`＋`DEFAULT_MNEMONICS_VERSION`（他の科目データと同じバッチ増分方式、`useStore.js`で初回のみ`kwMeta`へ追加。削除しても再追加されない） |
| フラッシュカード | `Flashcards.jsx` | 経穴カード（`src/data/keiketsuCards.js`、361穴完全収録＝2026-08-28完了）＋全科目対応（問題から自動生成） |
| 画像・図問題 | `figures.jsx`（オフラインSVG）／`question.image` | `zumondaiQuestions.js`にサンプルあり |
| 網羅マップ | `CoverageMap.jsx` | 出題基準×収録数の可視化 |
| エラーログ | `lib/errorLog.js`／`ErrorLogCard.jsx`（Settings内） | 端末内エラーの閲覧・消去 |
| 3分の2バッファ術 | `lib/bufferSession.js`／`Session.jsx`／`Calendar.jsx` | 学習時間を基礎タスク:バッファ=2:1（設定で調整可）に自動分割。完了後はマネージャービュー（振り返り）でバッファ用途を自動判定、ハリオが声かけ。詳細は下記セクション |
| 鍼灸過去問題の傾向と対策 | `lib/pastExamTrends.js`／`PastExamTrends.jsx`（Home内） | 収録済み過去問（round付き＝原問のみ）を実際に集計し、複数回にまたがる頻出ジャンル・頻出キーワードをデータドリブンに可視化。科目別の対策優先度、学習法・音声学習での活かし方、やるべきこと／やってはいけないことも提示。頻出テーマ・キーワードから一問一答へワンタップで絞り込める |
| 機種変更・端末移行（2026-08-19拡張） | `MigrationGuide.jsx`（Home/Settings内導線）／`Settings.jsx`内 `SyncQR.jsx`・`SyncScan.jsx`・`FileBackupCard.jsx`・`CloudBackup.jsx`・`P2PTransfer.jsx` | QR受け渡し（`lib/transferCodec.js`で圧縮＋`lib/chunk.js`で自動分割・アニメーション連続表示、テキストのコピー＆ペーストでも可、`lib/sync.js`の`summarizeHistoryForTransfer`で古い履歴を要約し軽量化）／バックアップファイルの保存・復元・共有（Web Share API、非対応端末はダウンロードにフォールバック）／Googleドライブ連携（`lib/googleDrive.js`、任意・要OAuthクライアントID自己発行、appDataFolderのみ・プライバシー方針の明示的な例外）／WebRTC直接転送（`lib/webrtcTransfer.js`、公開STUNのみ・TURN無し、シグナリングはQR/テキストで手動交換）。`MigrationGuide.jsx`は`lib/migrationAdvice.js`の`recommendMigrationMethod`で現在のデータ量から最適な方法を自動提案 |
| クラウド自動同期（2026-08-19追加・2026-08-25拡張） | `CloudBackup.jsx`の「自動同期」トグル／`useStore.js`（`runCloudSync`＋同期エフェクト）／`lib/progressMerge.js`／`lib/googleDrive.js` | Googleドライブ連携で`settings.googleDriveAutoSync`をONにすると、アプリを開いた時・**このタブに戻ってきた時**（`visibilitychange`/`focus`、60秒スロットル）・進捗（srs/history/memos/links/examResults/bookmarks/**quizProgress/examProgress/reviewProgress/audioProgress/session**）が変わった数秒後に、サイレント認証（同意画面を出さない`requestAccessToken(clientId, {silent:true})`）でappDataFolderの軽量ペイロード（`googleDrive.SYNC_FILENAME`、問題データは含まない）を確認・アップロードする。単純な新しい方で上書きだと片方の端末の進捗が消えるため、`lib/progressMerge.js`の`mergeProgress`で種類ごとにマージ（srsは問題ID単位でlastAnswered/dueが新しい方、historyはUNION+重複除去、examResultsはid単位UNION、memos/links/settings/bookmarksはキー単位UNION＋競合時は全体updatedAtが新しい側を優先、**quizProgress/examProgress/reviewProgress/audioProgress/sessionは`mergeResumeState`が各自身の時刻（at、sessionだけstartedAt）でより新しい方を丸ごと採用**）。実際に反映すべき変化があったかは`progressChanged`が中身まで比較して判定する（**件数だけの比較は既存キーの値だけが更新されたケースを見逃すバグがあったため2026-08-25に修正**）。`storage.js`の`syncMeta`（`updatedAt`）がマージ判定の基準時刻。サイレント認証・通信失敗は静かに諦める（初回だけ手動の「保存」または「復元」でGoogleログインの同意を済ませる必要がある）。他端末の進捗を取り込んだ時は`cloudAutoSyncToast`でトースト通知（画面遷移はしない）。デバウンス（最大5秒）を待たずに試したい時は`syncCloudNow`（設定画面・機種変更ガイドの「🔄 今すぐ同期」ボタン）。手動の全体バックアップ（`BACKUP_FILENAME`）とは別ファイルなので競合しない。**quizProgress等（一問一答・模試等の「続きから」）は当初「別端末で今まさに進行中のセッションを裏で無言で上書きしかねない」という理由であえて対象外にしていたが、2026-08-25にユーザーの明示指示で対象に含めた**（自身のタイムスタンプで新しい方を丸ごと採用するため、両端末で同時に別の活動を始めた場合は片方が後勝ちで置き換わる。QR／バックアップファイル／WebRTCでも同じ値が引き継がれる、`storage.js`の`exportAll`/`importAll`・`sync.js`の`buildSyncPayload`参照）。**「URLを開いたら常に最新化する」ための同期トリガーは全部で10系統**（2026-08-25追加、`useStore.js`）：①起動直後（300ms後、5秒デバウンスを待たない専用の初回同期）②タブ復帰時（`visibilitychange`/`focus`）③オフライン→オンライン復帰時（`online`イベント）④開いたままの端末向けの定期同期（5分間隔）⑤バックグラウンドへ退避する直前のプッシュ同期（`visibilitychange`→hidden／`pagehide`、これだけは共通スロットル対象外＝取りこぼし防止優先）⑥起動時同期が失敗した場合の1回だけの自動リトライ（15秒後、無限リトライはしない）⑦データ変更から5秒後のデバウンス同期（従来からある本線）⑧手動「🔄 今すぐ同期」ボタン（`syncCloudNow`）⑨Service Workerの自動更新チェック（`main.jsx`、起動時・1時間毎、`sw.js`はnetwork-firstでアプリコード自体も常に最新を取りに行く）⑩QRコード／URL（`#sync=`）での明示的な取り込み（`sync.js`）。②③④は共通の間引き（`lastBgSyncRef`、1分）を通し、①の初回同期もこの間引きに登録することで起動直後の重複発火を防いでいる（起動直後にfocusイベントが偶発的に発生するブラウザがあるため）。 |
| 用語集（目次・索引） | `Toc.jsx`（`view:'glossary'`）＋`data/toc.js`＋`data/glossaryTerms.js` | アプリ内用語をあ〜ん順で索引化。タップで説明・関連画面/問題へのリンク。会話や「これを目次に追加して」からの候補は承認するまで反映されない（詳細は「用語集（目次・索引）」セクション） |
| 全機能一覧 | `src/data/featureRegistry.js`＋`FeatureIndex.jsx` | 上表と同じ内容の単一の正。検索・カテゴリ絞り込み付き |

上表は概要用のスナップショット。**正確な最新の全機能リストは `src/data/featureRegistry.js`**
（`{id,title,icon,category,view,desc,tags}`の配列）。**新機能を追加・削除・改名したら、
このファイルにも必ずエントリを追加/更新する**（`npm run validate` が `view` の実在を
機械チェックする。CLAUDE.mdのこの表は概要のみで自動生成ではないため、大きく変わったら
このセクションも合わせて更新する）。

## 3分の2バッファ術（2026-08-19 追加）
河野ゆかり著『「仕組み化」勉強法』の考え方（「やる気があるから勉強する」のではなく
「勉強が始まる形になっているから、やる気があとからついてくる」）を取り込んだ学習計画機能。
- **ロジックは `src/lib/bufferSession.js` を単一の正とする**。学習予定時間（分）を
  基礎タスク:バッファ=2:1（`DEFAULT_BASE_RATIO`）で自動分割する（`planStudySession`）。
  比率はハードコーディングせず、Settings画面（`settings.bufferBaseRatioPct`、40〜80%で調整可）
  から変更できる。基礎タスクの問題数は過去の解答間隔（`history`のタイムスタンプ差の中央値、
  休憩とみなす間隔は除外）から逆算し（`averageAnswerSeconds`）、履歴が少ない初回はジャンル別
  デフォルト解答時間（`DEFAULT_ANSWER_SECONDS`）にフォールバックする（`estimatedAnswerSeconds`）。
- **体調連携は実装済み・シフト連動は未実装**（2026-08-24更新）：`baseRatioFor`は
  `shiftContext`（'work_day'|'off_day'）や`conditionScore`（0-100）を渡せる設計。
  `conditionScore`は`Session.jsx`が`lib/mood.js`の`moodToConditionScore(loadTodayMood())`
  （今日の調子＝元気80/普通50/しんどい20、音声学習・復習のノルマ調整と同じ値）をそのまま渡すため、
  「今日の調子」を記録している日は±5%の範囲で比率が自動調整される。`shiftContext`は
  `Session.jsx`/`Calendar.jsx`のどちらも未指定のままで、勤務シフト連携はまだ無い。将来、
  勤務シフト連携を追加する時は`planStudySession`の呼び出し側に`shiftContext`を渡すだけで良い設計。
- **Session.jsx への統合**：学習画面の開始前に「⏱ 時間で計画する」カードがあり、
  分数を選ぶと基礎タスク／バッファの問題数・時間が即座に計算される。「この計画で基礎タスクを
  始める」を押すと通常の学習セッションと同じ仕組みで開始し、`session.buffer`にプラン
  （`StudySession`相当のオブジェクト）を保持する（既存の`session`データ構造に追加フィールドとして
  同居させているだけで、一問一答・原問・音声学習・マインドマップのデータ構造とは衝突しない）。
  出題画面では基礎タスクであることを「🧩 基礎タスク・」とラベル表示し、残り5問以下になると
  ハリオが「あと◯問だけ頑張ろう」とリマインドする（`harioBaseTaskReminder`、`data/haripan.js`）。
- **マネージャービュー（振り返り）**：基礎タスクの問題を解き終えると、通常の完了画面より先に
  `ManagerReview`（Session.jsx内のローカルコンポーネント）が表示され、「予定通り完了したか」を
  Yes/Noで記録する（Noの場合は自由記述の理由メモを任意で追加）。**文言は一貫して
  「悪いのは実行役ではなく、無理な計画を立てたマネージャー」という前提**（自己否定を招く表現は
  使わない）。判定結果は`resolveBufferUsage(completed)`が`'review'`（ご褒美復習）／
  `'catchup'`（積み残し消化）を返し、通常の完了画面に「🧩 バッファ枠」カードとして表示。
  ハリオの声かけは`harioBufferEncourage(usage)`（同じく`data/haripan.js`）。カードのボタンから
  バッファ用途に応じたセッション（ご褒美復習＝`reviewPoolFor`で復習対象のみ／積み残し消化＝
  元の科目プールの続き）をそのまま開始できる。
- **カレンダー連携**：`Calendar.jsx`の日付詳細に「🧩 今日の学習ブロック（目安）」カードがあり、
  選択中の日が属する合格ロードマップのフェーズ（`phaseForDate`、`focus`/`mix`）と、
  60分プラン時の基礎タスク／バッファ目安（`planStudySession`）を並べて表示する
  （実際の時間入力・開始は学習画面で行う設計。カレンダー側はあくまで目安の提示のみ）。
- ハマりやすい点：Session.jsxの出題画面は完了画面・開始画面などの早期`return`が複数ある
  コンポーネント本体に直接書かれているため、**このセクション以降に新しいフックを増やす時は
  必ずコンポーネント本体の先頭（他の`useMemo`/`useState`と同じ場所）に置くこと**。
  条件分岐の後ろにフックを置くと「Rendered fewer hooks than expected」で本番ビルドが
  クラッシュする（このセクションの実装中に一度混入し、Playwrightのconsoleエラー監視で発見・修正した）。

## パフォーマンス方針（2026-08-17 追加）
- **コード分割**：`App.jsx`はホーム/カレンダー/一問一答/復習/音声/模試（下部ナビの6画面）と
  `MiniPlayer`/`HistoryPanel`だけ即時import。それ以外の画面は`lazy(() => import(...))`＋
  `<Suspense>`。**新しい画面を追加する時も基本はlazy importにする**（頻繁に使う下部ナビ相当のみ例外）。
  **`AuthGate`/`Pomodoro`は2026-09-02にlazy化済み**（下記「軽量化（2026-09-02追加）」参照。
  ロック未設定・ポモドーロ未使用のユーザーには不要なコードだったため）。
- **科目データの動的import**：`useStore.js`の`subjectDataModules()`が全科目データを
  `Promise.all([import(...)])`でまとめて動的読み込みする。**新しい科目データファイルを
  追加したら、ここにもimportを追加する**（トップレベルの`import X from '../data/...'`に
  戻さない。バンドルが再び1つの巨大ファイルに戻ってしまう）。
- **長いリストは無条件に全件描画しない**：`MindMap.jsx`の「中心にする言葉」（数千語になり得る）は
  検索フィルタ＋先頭80件キャップ方式（`CENTER_CHIP_CAP`）。新しく数百件以上になり得る
  一覧を作る時は、同じパターン（検索欄＋件数キャップ、または開閉アコーディオン）を使う。
  外部の仮想化ライブラリは導入しない方針（外部ランタイム依存なしのため）。
  **ただし目次・索引（`KeizetsuIndex.jsx`等）は対象外**——完全な一覧を見せることが目的なので、
  検索欄はあってもキャップはしない（2026-09-02の棚卸しで確認・区別済み）。

## 軽量化（2026-09-02追加）
ユーザーから「コードを軽くしサクサク動くようにする10案」を依頼され、実測に基づいて特定した
ボトルネックを全て直したもの。
- **`src/lib/mindmapDataLoader.js`が単一の入口**：`src/data/mindmapData.js`
  （COMPARISONS 352件＋NUMBER_FACTS 167件、合計約14万字）が、QuestionCard.jsx・AudioMode.jsx・
  Quiz.jsx・Review.jsx・Session.jsxから**トップレベルで直接import**されていたため、
  下部ナビの即時import画面（Quiz.jsx等）経由で丸ごと起動時バンドルに含まれていた
  （実測：929KB中の約150KB）。`useMindmapData(trigger)`フックに統一し、答えを開示した時・
  完了画面に来た時など実際に必要になる瞬間まで動的importで遅延読み込みするよう変更。
  **`mindmapData.js`を新しく使う画面を増やす時は、必ずこのローダー経由にする**——
  1箇所でも直接importが残っていると、Viteが「動的importが効かず結局eagerバンドルに戻る」
  という警告を出し、効果が丸ごと無効化される（実際にQuiz.jsx等の直import残りで一度踏んだ）。
- **`Pomodoro.jsx`（986行）・`AuthGate.jsx`（179行）をlazy化**：App.jsxが設定のON/OFFに関わらず
  常時eager importしていたため、ポモドーロ・ロックを使わない人にも常駐していた。
  他の30以上の画面と同じ`lazy()`+`<Suspense>`に統一。
  この2点（mindmapData動的化＋lazy化）で**起動時JS 929KB→591KB**（約36%削減）。
- **`src/lib/storage.js`：IndexedDB書き込みのデバウンス＋重複排除＋離脱時フラッシュ**。
  以前は`useStore.js`の状態保存（約20系統、各自の`useEffect`）が状態変化のたびに個別へ
  即時`idbSet`していたため、1問答えるたびに複数のIndexedDBトランザクションが同時発火していた
  （高速回転モード等の連打でもたつきの原因になり得る）。同リポジトリのOuroアプリ
  （`ouro/lib/storage.js`）で実証済みの400msデバウンス＋同一キーの重複排除パターンを移植。
  `db.js`に`idbSetMany()`（複数キーを1トランザクションでまとめて書く）を追加し、保留中の
  書き込みは`visibilitychange`(hidden)・`pagehide`で必ず即時flushする（データを失わないため）。
  **`read()`は保留中の値（pending）を先にチェックする**ため、`saveXxx()`直後に`loadXxx()`で
  読み直す既存コードは変更不要（`exportAll()`のバックアップ書き出し含め、全て`read()`/`loadXxx()`
  経由なので自動的に最新値を返す）。Playwright QAで「答えた直後はIndexedDB未反映→400ms後に
  反映→リロード後も正しく残る」「visibilitychangeで即flushされる」の両方を実機確認済み。
  この仕組みは`storage.js`の`saveXxx`/`loadXxx`経由のキーだけが対象で、他の多数のlib
  （missTypes.js・snoozeLog.js等）が独自に直接`idbGet`/`idbSet`する別キー群は対象外
  （元から個別の即時書き込みのままで影響なし）。
- **`src/lib/genreBreakdown.js`が単一の正**：Quiz.jsx・Session.jsx・Review.jsx・Exam.jsxの
  完了画面がそれぞれ独自に「ジャンル別正答率を集計して正答率の低い順に並べる」ロジックを
  重複実装していたため`buildGenreBreakdown(pairs)`に統一（呼び出し側は自分の解答データを
  `{genre, correct}`の配列に変換して渡すだけでよい設計。4画面でデータの持ち方が違う
  ——answerLog配列／idキー付きMap／order配列＋missIdSet／order配列＋答えの配列——ため、
  集計＋並び替えの共通部分だけを切り出した）。
- **`Home.jsx`の`overallStats(history)`/`studyStreak(history)`にuseMemoを追加**：
  同ファイル内の他21箇所は既にuseMemo化済みだったのに、この2つだけ毎レンダー計算していた。
- **CSS未使用セレクタの棚卸し**：`.cluster`/`.cluster-head`/`.cluster-kw`/`.cluster-count`/
  `.cluster-q`/`.cluster.focused`/`.pair-item`/`.pair-q`/`.pair-link`/`.related-cand`
  （ConnectedLearning.jsxが後に`cluster-dot`/`cluster-q-card`等へ作り直した際の残骸）を削除。
  **`lv-${x}`・`kz-freq-${x}`・`cat-${x}`・`k-${x}`のようなテンプレートリテラルで動的に
  組み立てるクラス名は、素朴な文字列検索では「未使用」と誤検出される**——実際に約半数が
  この誤検出だったため、削除前に必ず該当コンポーネントで動的構築されていないか確認すること。
  残りの低確信度な候補（kw-picker・kwd-image・rel-reason・review-streak・sess-resume-head等）
  は実害が小さく誤検出リスクの方が高いため見送った。
- **`npm run build:analyze`**（`ANALYZE=1 vite build`）でバンドル構成のtreemapを
  `dist/stats.html`に出力できるようにした（`rollup-plugin-visualizer`、devDependency）。
  通常の`npm run build`では生成しない。次に太った箇所を疑う時は、これで数字を見てから直す。

## 容量削減（2026-09-02追加）
ユーザーから「容量を少なくしたい」と依頼され、実測に基づく30案のうち安全に実装できるものを
すべて実施したもの。リスクが大きい／効果に見合わない案は実装せず、調査結果と判断理由をここに残す
（CLAUDE.mdの「頼まれたものだけを足す」「自信がない情報は断定しない」の方針どおり、
大掛かりな変更を独断で行わない）。
- **`public/haripan.png`を320×320・159KBから132×132・30KB（81%減）に置き換え**：
  `Mascot.jsx`での表示は`width="64" height="64"`（2倍の128pxあれば十分）なのに、
  5倍のピクセル数を毎回ダウンロードしていた。実は既に縮小済みの`haripan-sm.png`
  （132×132・30KB）が用意されていたのに一箇所も参照されておらず未使用のまま容量だけ
  食っていたので、その中身を`haripan.png`へ採用し`haripan-sm.png`は削除した
  （新たな画像処理をせず、既にあった適切なアセットを使うだけで済んだ）。
- **CSS未使用セレクタの残り棚卸し**：前回見送った低確信度の候補
  （`.kw-picker`・`.kwd-image`・`.rel-reason`・`.review-streak`・`.sess-resume-head`・
  `.sess-resume-main`・`.sess-frac`）を該当コンポーネントで目視確認し、すべて動的構築でも
  なく本当に未使用と確定したため削除した。
- **`KeiketsuLibrary.jsx`／`KeizetsuPageImages.jsx`に保存容量の目安を表示**：
  貼り付けた教科書材料・撮影したページ写真は上限なく増えるため、件数と概算バイト数
  （`lib/storageHealth.js`の`formatBytes`、既存の共有ユーティリティを流用）を一覧の先頭に出す
  ようにした。**調査の結果、写真側は`lib/image.js`の`fileToDataUrl`が既に1280px・
  JPEG品質0.72に縮小・再エンコードしてから保存しており（コメントに「IndexedDBの肥大化を
  防ぐため」と明記済み）、無制限に肥大化する実害は無いことを確認した**——今回追加したのは
  あくまで現在地を見せる可視化で、圧縮ロジックの変更は不要だった。
- **`npm run check:size`の予算をガードレールとして引き下げ**：直前の軽量化バッチで実測
  591KB/95KBまで下がったのに予算が1MB/150KBのままだと再肥大化に気づけないため、
  700KB/110KB（実測+今後の余裕）へ引き下げた。
- **調査したが実装しなかった項目とその理由**（すべて実測・確認済み）：
  - `subjectDataModules()`が起動のたび全14科目・約3.6MBぶんを`Promise.all`で
    ダウンロードする設計は、新規コンテンツの増分検知（`dedupeAgainst`）に必要なため
    そのまま。遅延・段階化すると「ダウンロードのタイミング」は変えられても
    「合計転送量」は変わらないうえ、増分検知の対象科目が抜け落ちるリスクがあるため見送った。
  - `storage.saveQuestions(baseQuestions)`によるIndexedDBへの全問題保存（JSON換算約1.04MB）
    は、JSバンドル側（Service Workerキャッシュ）と重複しているように見えるが、
    ①`questions`はクラウド同期の対象に含まれておらず端末ローカルの唯一の正であること
    ②ユーザーが取り込んだ独自問題や問題ツールでの修正を区別なく1本の配列として保持する
    設計であること、の2点から、削除すると取り込み・編集機能を壊すリスクが高いため
    アーキテクチャ変更は見送った。起動時の再書き込み自体は既存の条件分岐
    （`!(q && q.length>0) || importSeed || mutated`）で大部分は避けられている。
  - 原問と派生4問（A核心/B定義/C鑑別/D確認）の解説文に一部重複した言い回しがあることを
    `torinQuestions.js`で実際に確認した（例：`tk-mensetsu-1`と`tk-mensetsu-1b`）。
    **ただしこれは標準変換プロンプトの設計どおり**——各派生問題は単独で完結した解説を持つ
    必要があるため、意図的な重複であり削るとむしろ設計方針に反する。実装しなかった。
  - `figures.jsx`のSVGパスは元々整数座標のみ（7KB）で、`keiketsuCards.js`も画像を持たず
    テキストのみ（116KB）だったため、どちらも精度過剰・埋め込み画像の問題は無かった
    （確認のみ、変更なし）。
  - `knowledgeBase.js`（原穴・絡穴等のマスタ、219行）と`keiketsuCards.js`の経穴カードが
    別々に要穴フラグを持つのは意図的な設計（`npm run validate`が両者を突き合わせて矛盾を
    検知する仕組み）で、無駄な二重管理ではないため変更しなかった。
  - `examResults`は既に最大100件のキャップあり、`srs`は問題総数（約3300件）に自然に
    上限されるため、`history`のような無制限増加の問題はどちらも無いことを確認した
    （変更なし）。
  - React本体をpreact/compat等へ置き換える案は、互換性リスクが既存機能全体に及ぶため
    見送り。CSSの画面別分割・科目単位のオンデマンド初回セットアップも、効果に対して
    アーキテクチャ変更が大きすぎるため見送った（将来、容量が再び問題になった時に
    改めて検討する）。

## 機能間の連携強化（2026-09-02追加）
容量削減バッチの完了後、ユーザーから「実装後このアプリ内で連携させたほうがいい機能あれば
最大で30こまで教えて」と依頼され、既存の連携状況を調査したうえで6件の未連携（本当に
連携が欠けている箇所のみ・水増ししない）を報告し、ユーザー指定で全6件を実装したもの。
いずれも既存のCLAUDE.md方針（単一の正・`focusKeyword`/`onConsumeKeyword`型の
「一度だけ消費する中継state」パターン）をそのまま踏襲し、新しい仕組みは作っていない。
- **時間帯別パフォーマンス分析（Analytics.jsx）→ホームの学習導線**：`src/lib/timeOfDay.js`に
  `isNowBestTime(history, {minSample, now})`を追加（`bucketOf`も外部公開）。「今」が
  自分の最も正答率が高い時間帯に当たっていれば、Home.jsxに「🕐 今は集中しやすい時間帯です」
  カードを出す。
- **模試の伸びしろ分析→今日集中すべき科目**：`src/lib/todayFocus.js`の`todayFocusSubjects()`に
  `examResults`（省略可・後方互換）を追加。直近の本番同形式の模試（午前/午後/通し）1回ぶんの
  `examScoreContribution.js`の失点貢献度（出題数の重み×間違えた割合）をスコアに加味し、
  該当科目が最有力の理由なら「模試で伸ばすと効く科目」と表示する。Home.jsx・Session.jsxの
  両方の呼び出し元に配線（`store.examResults`をそのまま渡すだけ）。
- **語呂合わせノート⇄フラッシュカード**：Flashcards.jsx（全科目対応モード）の裏面に、
  その問題のキーワードに登録済みの語呂合わせ（`kwMeta`）があれば表示し、「📔 語呂合わせ
  ノートで見る」ボタンを出す。逆にMnemonicNotebook.jsxの各エントリから「🃏 カードで見る」で
  Flashcardsへ飛び、該当科目へ自動切り替え＋該当カードへ自動ジャンプする
  （App.jsxの`focusFlashcardKeyword`/`openFlashcardKeyword`、`effectiveTags`で
  キーワード→問題の対応づけはquery.jsを共用）。
- **教科書ページ写真→OCR画面**：KeizetsuPageImages.jsxの各サムネイルに「🔤 OCRへ送る」を
  追加。Ocr.jsxは`initialImage`（dataURL文字列）を受け取ると自動でOCRを開始するよう
  `runOcr`を拡張（Fileだけでなくdata URL文字列も受け付ける。プレビュー表示の分岐が必要な
  だけで判定ロジック自体は変えていない）。App.jsxの`ocrInitialImage`/`sendPhotoToOcr`で中継。
- **連結学習⇄知識グラフ**：連想トレーニング（AssocTrainer）・連想クイズ（AssocQuiz）・
  関係オーサリング（RelationAuthor）は既にKnowledgeGraph.jsx内部のセクションとして
  統合済み（独立画面ではない。featureRegistryの`view:'kgraph', sub:true`参照）。
  知識グラフ→連結学習の方向（`onOpenKeyword`）は既存済みだったが、逆方向が無かったため
  ConnectedLearning.jsxの深掘り（KeywordDetail）に「🕸️ 知識グラフで見る」を追加。
  KnowledgeGraph.jsxは`focusConcept`/`onConsumeFocusConcept`を受け取ると、その概念が
  `conceptList`（解いた問題から実際に生成されたグラフのノード）に存在する時だけ
  「つながりを探す（AとBの経路）」の概念Aへ自動セットし、該当セクションへスクロールする
  （Roadmap.jsxの`focusLevel`と同じ`ref`＋`scrollIntoView`＋`setTimeout`パターンを流用）。
  **`conceptList`に無い概念（未解答の問題にしか出てこないキーワード等）を渡した場合は
  何も起きない**——これは意図した仕様で、知識グラフは「実際に解いた問題」からしか
  概念を生成しないため（`solved.length===0`なら画面自体が空状態表示になる）。
- **科目バランス警告→ホーム**：`stats.js`の`subjectBalanceWarning(history, questions)`
  （Analytics.jsxが既に使っている判定をそのまま再利用）をHome.jsxからも呼び、
  警告があれば同じ内容をホームにもカード表示する（弱点科目チップから一問一答へ直行、
  「📊 分析で詳しく見る」からAnalytics.jsxへ）。判定ロジックの複製はしていない。
- 上記6件のうち5件（模試の伸びしろ分析以外）はPlaywright QAで実機確認済み
  （IndexedDBへ直接`srs`/`history`を注入して解答済み状態を再現し、ボタン押下→画面遷移→
  自動選択・自動スクロール・自動OCR開始までを確認）。`node --test`（2249件）・
  `npx vite build`・`npm run validate`はすべて通過。

## 用語集（目次・索引）（2026-09-02追加）
ユーザー指定で「目次・索引パターン」（既存の`KeizetsuIndex.jsx`・Ouro/鏡/間合い等の
`data/toc.js`＋`lib/focus.js`パターン）をこのアプリ本体にも実装したもの。
**既存の`view:'toc'`（`TableOfContents.jsx`＝取り込んだ問題を「科目→キーワード」で
一覧・演習する画面）とは別物**——新しい画面は`view:'glossary'`（`Toc.jsx`）で、
あ〜ん索引・用語の説明パネル・候補承認フローを持つ。両画面は互いにボタンでリンクしている。
- **`src/lib/yomi.js`を拡張**（既存の`numberToKana`/`readingInfo`/`buildKanaIndex`は
  7画面（Exam/AudioMode/KeizetsuIndex/Quiz/Review/MindMap/Session）が使っており、
  シグネチャ・挙動を変えると壊れるため**非破壊で追加**した）：
  - `foldKana`/`kanaRow`：既存の内部関数（`FOLD`/`rowOf`）を外部公開しただけ（実装は共通）。
  - `numberToReading`：`numberToKana`の別名（実装は共通、目次系コードからの呼び名を統一）。
  - `normalizeAlnum`：全角英字・ローマ数字（Ⅰ〜Ⅻ・ⅰ〜ⅻ）を半角ラテン文字へ正規化する。
    `readingInfo`のA〜Z判定はこれを経由するよう変更した（「ＷＨＯ」「Ⅰ型」等がA〜Z枠へ
    正しく入るようになる）。数字が全角の場合の判定にも同じ正規化を使うよう統一した。
  - `readingInfo`/`buildKanaIndex`に`opts.strict`を追加：**既定（false）は今までどおり**
    （readingが無くても先頭が仮名ならその仮名を読み扱いにする、既存7画面向けの緩い挙動）。
    `strict:true`の時だけ「readingが無い項目は数字・英数字混じり以外、必ず『その他』へ
    落とす」を厳密に適用する（用語集はreadingを人が明示するデータのため、こちらを使う）。
  - `buildKanaIndex`に`opts.warnOtherThreshold`を追加：「その他」（漢字グループ）の件数が
    しきい値を超えたら`import.meta.env.DEV`の時だけ`console.warn`する（読みの入れ忘れの
    早期発見用ガードレール。本番ビルドでは出さない）。
- **`src/data/glossaryTerms.js`が用語集の本体データ（単一の正）**。各項目は
  `{id, title, reading, category, description, descriptionStatus, aliases, destinations}`。
  `descriptionStatus`は`'verified'`（既存アプリのデータと突き合わせ済み）と
  `'needs_review'`（未確認）の2値。**`'verified'`を付けてよいのは、この静的ファイルへ
  人が直接書いた項目だけ**——候補フロー（下記）経由の追加は`proposeAddCandidate`が
  無条件で`needs_review`に上書きする（渡した値を無視する。テスト
  `test/tocCandidates.test.mjs`の「会話由来の候補は常にneeds_reviewになる」参照）。
  初期シードは14件（経絡経穴概論の基礎＋陰陽五行・臓腑）。既存の`knowledgeBase.js`
  （十二経・原穴等）・`src/lib/yomi.js`の`TERM_READINGS`と矛盾しないことを確認済み。
  `effectiveGlossary(extra, removedIds)`が「本体データ＋候補承認で増えた分（extra）−
  候補承認で消えた分（removedIds）」を合成する関数——**`glossaryTerms.js`自体は
  静的ファイルで実行時に書き換えられない**ため、承認された追加・削除は必ずこの合成を
  経由する（`data/toc.js`・`lib/tocCandidates.js`が共用）。
- **`src/data/toc.js`が目次専用データを持たず、用語集から毎回導出する**
  （`buildTocEntries(glossary)`）。別名（aliases）はcanonicalとは別の1行として展開し、
  タップすると`type:'system'`のdestinationでcanonicalの項目IDへジャンプする
  （本文はcanonical側にしか持たせない＝二重管理にしない）。`tocSections()`は
  `buildKanaIndex(..., {strict:true, warnOtherThreshold})`を使う。`duplicateTitles()`は
  canonical＋別名を合わせた統合後のtoc全体でチェックする。`resolveDestination(dest)`が
  4種類のdestination（page/function/question/system）を実行可能な「意図」
  （navigate/relay/startQuestion/jumpTerm）に正規化し、`openTermAction(targetId)`が
  「用語を開く」時の状態（常にタブは索引固定・アンカーIDの組み立て）を、
  `termPanelViewModel(entry)`が詳細パネルの表示用データ（説明未登録時のプレースホルダー・
  needs_reviewバッジの要否・destinations空時のメッセージ）を、それぞれReact非依存の
  純関数として切り出してある——**このアプリには元々コンポーネントのレンダリングを
  試験する仕組み（jsdom等）が無い**ため、UIの振る舞いはこれらの純関数をnode:testで
  検証し、実際の見た目・操作はPlaywright QAで確認する方針にした（外部テストライブラリを
  新規導入しない、という既存方針を維持するため）。
- **`src/lib/focus.js`＋`src/components/useFocusJump.js`を新規実装**（Ouro/鏡/間合いの
  `flashTo`/`FLASH_ATTR`/`useFocusJump`と同じ設計をそのまま移植）。ハイライトの印は
  **`className`ではなく`data-flash`属性**に付ける——飛び先のカードは開閉操作で
  `className`が変わることがあり、印をclassで付けると次の描き直しでReactがclassNameを
  丸ごと書き直し、印だけが消える（鏡アプリで実際に踏んだバグと同じ形なので、
  最初から属性方式で実装した）。CSSは`src/styles/index.css`の`[data-flash]`。
- **destinationsは4種類**（`type`）：
  - `'page'`：`onNavigate(target)`で画面遷移するだけ（画面内の特定箇所へは飛ばない。
    既存の画面の多くは用語単位の細かいアンカーを持たないため、正直に「遷移するだけ」とし、
    無いアンカーを捏造しない）。
  - `'function'`：App.jsxの既存の中継関数（`openKeyword`→連結学習・`openGraphConcept`→
    知識グラフ・`openFlashcardKeyword`→フラッシュカード）を`target`名で呼ぶ。
    **新しいジャンプの仕組みは作らず、既存の`focusKeyword`型の中継をそのまま再利用した**。
  - `'question'`：`target`を問題IDとして`questions`から検索し、見つかれば
    `startCustomQuiz([q])`でその1問だけの演習を開始する（複数画面をまたいで
    「特定の1問をハイライトする」汎用の仕組みは無いため、代わりに「その問題だけの
    演習を始める」という実質的に同じ体験を既存機能の組み合わせで実現した）。
  - `'system'`：用語集内の別の項目IDへ`lib/focus.js`のflashTo相当（`useFocusJump`）で
    ジャンプ・ハイライトする。別名→canonical、要穴→原穴/絡穴/募穴、のような
    用語集内リンクはこの形。
  - **`page`/`function`/`question`は他画面の詳細アンカーまでは作り込んでいない**
    （既存の数十画面すべてに用語単位のアンカーを追加するのはこの回のスコープを大きく
    超えるため）。同一画面内で完結する`system`だけが、真の意味での
    「運んでハイライトする」を実装している。この区別は正直に上記のとおり。
- **候補フロー（会話内容からの追加・削除提案）は`src/lib/tocCandidates.js`**：
  - 候補の生成トリガーは3つだけ——a)「■用語追加：〜」という会話中の明示的な合図
    （Claudeが`proposeAddCandidate`を手動で呼ぶ）、b) 教材生成時に付与された
    `tags`フィールド（`suggestCandidatesFromTags`を**明示的に**呼んだ時だけ）、
    c) ユーザーが「これを目次に追加して」と指示した場合（`Toc.jsx`の
    「これを目次に追加して」フォーム）。**`suggestCandidatesFromTags`はどのコンポーネント・
    どのuseStore.jsの自動処理からも呼んでいない**（`test/tocCandidates.test.mjs`の
    「suggestCandidatesFromTagsはどこからも自動で呼ばれていない」がsrc/components・
    src/lib全体をソースレベルで機械チェックする）。b)を実際に使う時は、標準変換
    プロンプトで新しい科目データを追加した後、このセッション内でこの関数を手動で
    呼び出す（または将来`scripts/`に手動実行スクリプトを足す）運用とし、**自動実行の
    仕組みは今回あえて配線していない**（ユーザー指定の「絶対に発火させない」を
    もっとも安全に満たす形）。
  - 候補は`glossaryExtra`/`removedIds`（本体データ相当）とは別の
    `storage.js`の`tocCandidates`キーに保持し、**承認（`acceptCandidate`）するまで
    本体データには一切書き込まない**。承認時に初めてreading・重複・分類・正規化の
    4チェック（`runTocChecks`）を実行し、1つでも落ちれば反映せず候補はpendingのまま
    エラーを表示する。「しない」（`rejectCandidate`）は候補を`rejected`にして
    履歴へ「見送り」として残すだけで、`glossaryExtra`/`removedIds`には一切触れない。
  - 確定結果（追加/削除/見送り/取り消し）は`storage.js`の`tocHistory`キーへ積む。
    `undoLastTocAdditions(n)`は**履歴上の直近n件の「承認された追加」だけ**を
    `glossaryExtra`から取り消す（手で書いた本体データ`GLOSSARY_TERMS`は対象外——
    履歴の`add`レコードは`glossaryExtra`への追加時にしか作られないため自然にそうなる）。
- **スコープ判断（意図的に対応していない項目）**：
  - `glossaryExtra`/`removedIds`/`tocCandidates`/`tocHistory`の4キーは、
    **バックアップ・QR・クラウド自動同期には含めていない**（`exportAll`/`importAll`へは
    配線していない）。ユーザー指定の25ルールにこの要件は無く、今回はスコープを
    絞った。端末を変えると候補フローで積み上げた分は引き継がれない——将来
    「機種変更・端末移行」の対象に含めるかはユーザー確認のうえで判断する。
  - `suggestCandidatesFromTags`を呼び出す実際のUIボタン・npm scriptは**今回は作らず**、
    関数として実装のみ（トリガーbは前述のとおり手動運用）。
- テストは`test/yomi.test.mjs`（既存3件＋新規9件）・`test/focus.test.mjs`（6件）・
  `test/toc.test.mjs`（16件）・`test/tocCandidates.test.mjs`（12件）に分けて配置。
  `node --test`（2363件）・`npx vite build`・`npm run validate`はすべて通過。
  UIの実際の見た目・操作（索引タップ→詳細パネル→destinationsボタン→画面遷移/
  ジャンプ、候補の追加する/しない、クイック追加フォーム）はPlaywright QAで確認する
  （このアプリにコンポーネントレンダリングのテスト基盤（jsdom等）が無いため）。

## 「やったほうがいいことベスト18」から実装した分（2026-09-03追加）
ユーザーに「このアプリのためにやったほうがいいことベスト18」を聞かれ、既存機能と重複しないか
grep調査した上で13件を提案・そのうち11件（過去問PDF等の素材が要る2件＝総合問題の実データ投入・
図問題の拡充は除く）をユーザー指定で実装したもの。
- **論点の被り候補41件をアプリ内で確認できるように**（`QuestionTools.jsx`の誤りチェックタブ）。
  `npm run validate`のCLI出力にしか出ていなかった`src/lib/questionSchema.js`の
  `findLogicalDuplicates`をそのまま再利用し、一覧表示＋「確認済みにする」ボタンを追加。
  確認済みのグループキーは`storage.js`の`logicalDupDismissed`キーに保存し、次回以降その
  グループを再表示しない（新しい判定ロジックは作らず、既存の検証ロジックをUIに出しただけ）。
- **用語集（`glossaryTerms.js`）を14件→20件に拡張**。郄穴・八会穴・五兪穴・奇経八脈・表裏・
  虚実の6件を追加（既存の要穴の説明文が既に触れていた郄穴・八会穴・五兪穴を用語として独立させ、
  要穴側にも相互のsystem destinationを追加）。いずれも十分に確立した教科書的事実のため
  `descriptionStatus: 'verified'`で人が直接データファイルに書いた（候補フロー経由ではない）。
- **アクセシビリティ設定**（`Settings.jsx`）：`settings.a11y`（`fontScale`/`reduceMotion`/
  `highContrast`）を新設。文字の大きさは`.app`ルートへのCSS `zoom`で全体拡大（435箇所ある
  px固定の`font-size`指定を個別に書き換えるのは非現実的なため、`html`の`font-size`変更では
  効果が限定的と判断し、レイアウトごと拡大する`zoom`を採用。Chromium/Safari16.4+で有効、
  Firefoxでは効果なしという既知の制約がある）。低モーションは`.app.reduce-motion`で
  `animation-duration`/`transition-duration`を実質0にする。ハイコントラストは
  `--text-sub`/`--text-mute`/`--border`を白へ寄せる（元々黒背景×白文字なのでこの3変数が主対象）。
- **印刷機能**：`src/styles/index.css`に`@media print`を追加し、`:root`の配色変数を
  白背景×黒文字へ反転（各コンポーネントの多くが`var(--bg)`等を参照しているため変数上書きだけで
  広く効く）。下部ナビ・ロードマップバー・ミニプレーヤー・トースト・ヘッダーは`display:none`
  （`.no-print`クラスでも個別に隠せる）。加えて、`MistakeNote.jsx`に既にあった「別タブに白背景の
  印刷用HTMLを開いて`window.print()`」方式を`WeeklyJournal.jsx`にも展開（今週の集計＋方針の記録を
  印刷）。**この「別タブ方式」と「`@media print`によるCSS変数反転」は独立した two-layer**——
  印刷ボタンが無い画面でもCtrl+Pで最低限読める見た目になる、という位置づけ。
- **フラッシュカードの読み上げ**（`Flashcards.jsx`）：`window.speechSynthesis`を直接使う
  最小実装（AudioMode.jsxのような連続再生・一時停止の状態管理は持たない単発読み上げ）。
  声・速度・高さ・発音補正辞書（`settings.voiceURI`/`speechRate`/`speechPitch`/
  `pronunciationFixes`）は音声学習と共通の設定値をそのまま再利用する。
- **オフライン初回起動時のフォールバック画面**（`public/sw.js`）：キャッシュが1件も無い状態で
  オフラインになった時（＝一度もオンラインで開けていない端末）に返す応答を、プレーンテキストの
  「オフラインです」から簡易な案内HTML（再読み込みボタンつき）に差し替えた。通常はここに来る前に
  キャッシュ済みの`index.html`が返るため、ほとんどのユーザーは目にしない経路。
- **`manifest.webmanifest`にshortcutsを追加**：ホーム画面アイコン長押しで「今日の復習」
  「一問一答」「模試を始める」に直行できるようにした（`url: "./?view=review"`等）。
  受け口は`App.jsx`の`initialViewFromUrl()`——**許可リストはVIEW_TITLESではなくNAV（下部ナビ6画面
  のid）を使う**（VIEW_TITLESは見出し表示用の一覧で全画面を網羅しておらず、`journal`等が漏れて
  いることを実装中に発見したため。不正な値・NAV外の値は既定の'home'にフォールバックする）。
- **通知バリエーション**：既存の毎日リマインド（`haripanReminder`）に、本番同形式の模試
  （午前/午後/通し）から14日以上経過している時の一言を追加。`haripan.js`に新規
  `daysSinceLastMockExam(examResults)`を追加し、`haripanReminder`の第4引数として渡す
  （第1〜3引数の意味・順序は変えていないため既存呼び出し・既存テストは無変更で通る）。
- **用語集4キー（glossaryExtra/glossaryRemovedIds/tocCandidates/tocHistory）をバックアップ・
  QR・WebRTC・クラウド自動同期の対象に含めた**（実装当初は意図的に対象外にしていたが、今回の
  ユーザー指定で「実際に使い込まれて端末間で持ちたいニーズが出た」と判断）。`exportAll`/
  `importAll`・`sync.js`の`buildSyncPayload`/`syncToBackup`（短縮キー`ge`/`gi`/`tc`/`th`）・
  `progressMerge.js`の新規`mergeById`/`mergeIdSet`（examResults等と同じ「id単位でUNION、
  同idはremote優先」パターン）・`useStore.js`の`runCloudSync`（quizProgress等と同じく
  ReactのstoreではなくIndexedDBから直接読み書き、Toc.jsxがローカルstateで完結させているため）
  の4箇所に配線。**既知の制約**：Toc.jsxは起動時に一度だけこれらを読み込みローカルstateで
  保持するため、他端末からの同期で裏側のIndexedDBが更新されても、既にToc.jsxを開いたままの
  画面には即座に反映されない（再度開き直すと反映される。quizProgress等の「続きから」データと
  同じ制約）。
- **経穴カードSRS（`lib/flashcardSrs.js`）をバックアップ・クラウド同期対象に含めた**：
  このファイルは元々`idbGet`/`idbSet`を直接使う独立実装で、バックアップ・QR・クラウド自動同期の
  どこにも登録されていなかった（実際に調査して見つけたバグ。一問一答の本体SRS(`KEYS.srs`)は
  最初から対象だったのに、経穴カードだけ機種変更のたびに学習進捗が消えていた）。
  `saveFlashcardSrsRaw(map)`を新規追加（`gradeFlashcard`のような1枚採点のロジックを経ずに
  マップ全体を置き換える、復元・マージ結果の書き戻し専用）。マージは本体SRSと同じ形
  （cardId単位・lastAnswered/dueが新しい方）なので既存の`mergeSrs`をそのまま再利用した。
- **見送った項目**（過去問PDF等の素材が無いと着手できないためユーザーへ相談中）：
  総合問題（`integratedQuestions.js`、`INTEGRATED_VERSION=0`のまま）の実データ投入、
  図問題（`zumondaiQuestions.js`、サンプル5問のまま）の拡充、模試配分（`examBlueprint.js`）と
  公式出題基準の突き合わせ（出典PDFが無いと照合できない）。
- 検証：`node --test`（2414件）・`npx vite build`・`npm run validate`・
  `node scripts/check-bundle-size.mjs`（起動時JS 600KB/700KB・CSS 95KB/110KB）はすべて通過。
  Playwright QAで実機確認済み：アクセシビリティ設定（文字拡大・低モーションのクラス付与）・
  用語集/フラッシュカード/問題ツールの新UI・`?view=review`/`?view=exam`のshortcutリンクが
  正しい画面を開くこと・不正な`?view=`値がhomeへフォールバックすること。WeeklyJournal.jsxの
  印刷ボタンはビルド成功のみ確認（既存のMistakeNote.jsxと同一パターンのため実機クリックまでは
  行っていない）。

## 症例の連問（2問セット出題）と既存問題の内容訂正の仕組み（2026-09-03追加）
ユーザーから3件の指摘（①「上記症例の続き」の連問がバラバラに出題される・その片方だけ間違えた時に
復習で2問セットにならない、②投球動作の問題が苦手・図と類題がほしい、③経絡経穴概論のある問題の
選択肢が変）を受けて対応したもの。②③の対応の過程で、**既存問題の内容を訂正しても保存済みの端末には
届かないアーキテクチャ上の欠落**を発見・修正した。
- **`src/lib/casePairs.js`が単一の正**：「（上記◯◯の続き）」で始まる設問（症例ベースの連問）を、
  新しいデータフィールドを持たず既存の収録順から導出する。標準変換プロンプトの追加順
  （原問1→その派生[一問一答、idの末尾が英字]→続きの原問2→その派生…）を前提に、続きの設問から
  遡って**派生（id末尾が英字）を読み飛ばしながら**直近の非派生設問を探す（直前の1件だけを見ると、
  無関係な別問題の派生に結びつくバグが実際にあった。第31回顔面神経麻痺の症例で発見）。
  `buildCaseLinkMap(pool)`（全体の元の収録順を渡すこと。フィルタ済みの一部を渡すと遡り探索が
  正しく働かない）・`partnerOf(id, linkOf, pairOf)`・`keepCasePairsAdjacent(order, linkOf, pairOf)`
  （id配列向け・シャッフル後や周回で同じidが複数回含まれても壊れない）・
  `keepCasePairsAdjacentObjects`（問題オブジェクト配列向け、Review.jsx用）を持つ。
  3問以上つながる症例チェーンにも対応（`chainOf`が根本から辿る。末尾側のidを先に見つけても
  正しい順に並べ直す実装にしている——最初の実装ではここでバグを踏んだ）。
- **`src/lib/sessionOrder.js`（Session.jsx・Quiz.jsxが共用）**：`buildOrder`等すべての出題順
  構築関数の最終段（`spaceById`の直後）で`keepCasePairsAdjacent`を適用する。`spaceById`
  （原問と派生を離す、既存の逆方向の要求）とは対象が違うため競合しない——派生＝同じ過去問由来の
  一問一答、連問＝別の過去問設問だが同じ症例を参照するもの。
- **`Review.jsx`の`spaceByOrigin`**：`questions`（フィルタ前の全体）から`buildCaseLinkMap`を
  一度だけ計算し、`spaceByOriginRaw`の後段で`keepCasePairsAdjacentObjects`を適用する。
- **`useStore.js`の`recordAnswer`**：誤答時（gradeが△✕相当、または`correct===false`）に、
  その設問が連問の相方を持てば、相方のSRS状態も`wrongCount>=1・correctStreak=0・due=今`に
  強制的に書き換える（ユーザー指定「片方または両方を間違えたら必ず2問セットで復習に出す」）。
  相方が未解答・マスター済みでも関係なく発火する。
- **既存問題の内容訂正が保存済み端末に届かない問題を発見・修正**：`dedupeAgainst`
  （src/lib/importer.js）は**問題文（question）の一致**だけで重複判定するため、問題文を変えずに
  choices／explanation／tags／figure等だけを直す訂正は、バッチ増分方式（`<X>_VERSION`）の
  「新規追加」ルートに乗らない——既にその問題を保存済みの端末には届かない（増分方式は追加専用で、
  既存内容の上書き経路をそれまで持っていなかった）。**`src/lib/questionPatches.js`を新設**し、
  `QUESTION_PATCHES`（idをキーにした上書きフィールドのオブジェクト）と`applyQuestionPatches(questions)`
  を実装、`useStore.js`の起動処理で`baseQuestions`確定後・`setQuestions`直前に毎回適用する
  （冪等なのでバッチ増分の有無に関わらず安全に毎回重ねられる）。**今後、問題文を変えずに
  choices/explanation/tags/figure等だけを直す訂正は、データファイル本体の修正（新規ユーザー向け）と
  `questionPatches.js`への追記（既存ユーザー向け）の両方をセットで行うこと**（片方だけだと
  新規ユーザーと既存ユーザーで内容が食い違う）。問題文そのものを直す場合は文言が変われば
  自動的に新規として追加されるため、こちらは対象外（`questionPatches.js`に入れない）。
- **投球動作の図（`src/data/figures.jsx`の`pitching-phases`）**：投球動作4局面（早期コッキング期・
  後期コッキング期・加速期・フォロースルー期）を簡略化した棒人間で並べた模式図。内容は
  WebSearchで裏付け（後期コッキング期＝肩最大外旋位・internal impingementのリスクが高い局面、
  加速期＝肩内旋・前腕回内で上腕を加速、肘内側部への外反ストレスが加速期にかけても続く、
  フォロースルー期＝肘後方への伸展ストレス）。既存の`tk-toukyu-hiji-followthrough-1`
  （torinQuestions.js、TORIN_VERSION 67で図を追加）に加え、4局面それぞれを正解とする類題3問
  （`tk-toukyu-naibu-impinge-jiki-1`＝後期コッキング期、`tk-toukyu-gaihan-kasoku-jiki-1`＝加速期、
  `tk-toukyu-hayaki-cocking-jiki-1`＝早期コッキング期）を追加し、4問で4局面をちょうど1問ずつ
  網羅する（答えの重複なし）。
- テストは`test/casePairs.test.mjs`（10件）。`node --test`（2584件）・`npx vite build`・
  `npm run validate`はすべて通過。Playwright QAで実機確認済み：キーワード「投球動作」で
  絞り込むと図つきの4問（既存1＋新規3）が表示されること、キーワード「手五里」で絞り込むと
  訂正後の選択肢（無関係な「ともに足の少陽胆経」が消え、経脈を入れ替えた引っかけに差し替え）が
  表示されること。連問の出題順隣接自体は、実データ（東洋医学臨床論587問）でシャッフル後の
  出題順に対し全41組の連問が正しく隣接することをNode上のスクリプトで確認済み
  （UIでの2問連続表示の実機クリック確認までは行っていない）。

## 学習（10・60・300・900）の受験生視点QAで見つけた5件の修正（2026-09-03追加）
ユーザーに「受験生として学習10・60・300・900を一通り解き、改善点を最大10個」と依頼され、
実際にPlaywrightで解いて見つけた5件（放置リスクとともに報告済み）を、ユーザー指定で
すべて修正したもの。
1. **「もう一度」が誤答復習・○の見直し／高速回転・バッファ枠のときだけ、対象プールを
   科目全体に差し替えてしまうバグを修正**（`Session.jsx`）。以前は非標準セッション
   （`session.label`あり）の「もう一度」も標準セッションと同じ`pool: poolFor(questions,
   session.subject)`を使っていたため、「誤答復習完了！」の見出しのまま無関係な新問を
   出題していた。**標準セッション（`isStandardSession`）だけ従来通り科目全体から新しく
   引き直し、非標準セッションは`session.ids`から実際に出題した問題集合を再構築して
   そのまま渡す**（Quiz.jsxの`restart()`が`sessionPool`を保持して再演習する既存パターンと
   同じ考え方に統一）。
2. **誤答復習・○の見直し等の並べ替え済みプールで、症例の連問（casePairs.js）が無関係な
   2問を誤って結びつけうる問題を修正**。`buildCaseLinkMap`は「元の収録順のままの配列」が
   前提だが、`wrongQs`（誤答復習）や`maruPool`（○の見直し）は解いた順・自己採点の新しい順に
   並んでおり、この前提を満たさない。**`src/lib/sessionOrder.js`の`buildOrder`等5関数すべてに
   `caseLinkMap`引数（省略可）を追加**し、渡された場合はpool自身から作り直さずそれを使うよう
   変更。**`useStore.js`の`casePairMap`（常に全体の`questions`から導出、既存）を`store`の
   返り値に追加**し、Session.jsx・Quiz.jsxの`begin()`/`buildIdsForTarget()`呼び出し全箇所と
   Quiz.jsxの`beginWith()`（もう一度の再シャッフル）に配線。Review.jsxも独自に
   `buildCaseLinkMap(questions)`を計算していたのをやめ、`store.casePairMap`を使うよう統一
   （二重計算の解消も兼ねる）。`test/sessionOrder.test.mjs`に4件追加。
3. **完了画面の誤答一覧で、○×問題だけ「正解」ラベルが無く記号だけが並んでいた表示を修正**
   （`Session.jsx`）。`正解 ${q.answer===0?'○':'✕'}`のように、4択と同じ「正解 」接頭辞を
   ○×にも付けるようにした。
4. **「ジャンル別正答率（苦手順）」が母数（n）を無視して並んでいた問題を修正**
   （`src/lib/genreBreakdown.js`）。全科目セッション等でn=1のジャンルばかりになると、
   たまたま1問外しただけの0%が、真に弱い（n多め・正答率も低い）ジャンルより上位（苦手扱い）に
   来てしまっていた。**並び順だけラプラススムージング（`(correct+1)/(total+2)`）の昇順に変更**
   （表示する正答率・件数は従来通り素の値のまま）。`test/genreBreakdown.test.mjs`に1件追加。
5. **「← 前の問題」で解答済みの問題まで戻り、もう一度自己採点すると二重記録されるバグを修正**
   （`Session.jsx`）。`QuestionCard`は`key={id+pos}`で問題ごとに作り直されるため、戻ると
   未解答状態に見える。**`session.recordedIds`（このセッションで実際に記録した問題idの配列）を
   新設し、`recordCurrent`は既に記録済みのidなら`recordAnswer`等を一切呼ばない（no-op）**
   （SRS・履歴・リーチ判定・selfKindCounts・メモ自動タグの二重上書きを防ぐ）。あわせて
   `advance()`を、解答済みの位置を飛ばして次の未解答位置まで進むよう一般化した
   （見直し後に元いた最先端まで自然に戻る。逆に、意図的にスキップした未解答の問題を後から
   埋めた時は、その次の未解答位置へ正しく進む）。見直し中の問題には
   「📝 この問題はすでに解答済みです（見直し用に表示中。もう一度答えても記録は上書きされません）」
   と明記する。
検証：`node --test`（2607件）・`npx vite build`・`npm run validate`・
`node scripts/check-bundle-size.mjs`（起動時JS 604KB/700KB）はすべて通過。Playwright QAで
1〜3・5を実機確認済み（②はcasePairs.test.mjs・sessionOrder.test.mjsの単体テストで検証、
実データでの隣接ズレ0件は前回バッチで確認済みのアルゴリズム自体は変更していない——変更は
「どのpoolから対応表を作るか」の呼び出し側の配線のみ）。

**追加修正（同日）**：再QAで見つけた1件（軽微・ユーザー指定で修正）——
「もう一度」で○の見直し・高速回転を再演習すると、開始時と同じ「うっかり○を先頭に、古い順」
という並び（`useMaruReview.js`の`orderMaruStatus`が決める優先順位）が失われ、毎回シャッフルし
直されていた。**`Session.jsx`の「もう一度」ボタンは、`session.label`が`○の見直し`／
`○の高速回転`の時だけ`ids`をそのまま渡し直す**（`buildOrder`を経由させない＝開始時の
`onStartReview`/`onStartFast`と同じ経路）。**`Quiz.jsx`の`restart()`にも同じ問題があった**
（`beginWith(sessionPool, true, ...)`で`maruSessionKind`の有無を見ずに常に再シャッフルして
いた）ため、`doShuffle`を`maruSessionKind == null`にして同様に修正。誤答復習・バッファ枠は
開始時から毎回シャッフルしていたので、もう一度でも従来通り再シャッフルする（変更なし）。
`node --test`（2607件）・`npx vite build`・`npm run validate`・`check-bundle-size.mjs`は
すべて通過。この修正はSession.jsxの既存の`onStartReview`/`onStartFast`と全く同じ配線を
「もう一度」の分岐にも適用しただけ（新しい仕組みは作っていない）。

## 音声学習（AudioMode.jsx）：科目×誤った問題の組み合わせ＋次の問題への自動送り間隔（2026-09-04追加）
ユーザーから「科目選択／誤った問題／問題解答解説を音声学習で規定の時間たったら次の問題へ進む／
前の問題・次の問題へいけるように」という要望を受け、着手前に必ず調査する既存ルールに従って
grep調査したところ、**要望の大半（科目選択・誤った問題モード・問題文/解答/解説の読み上げ・
ボタン操作不要の自動連続再生・前へ／次へボタン）は既にAudioMode.jsxに実装済み**だった
（`audioEngine.js`の`runFrom`ループが計画を最後まで自動で読み進める設計のため、元々
「規定の時間で次へ」に近い挙動はあった）。**本当に欠けていたのは2点だけ**——
①科目を選ぶと「誤った問題」モードが強制的に解除されていた（両立できなかった）、
②解答・解説を読み終えてから次の問題へ移るまでの間隔が`gapSeconds`
（問題文→正解の間）に混ざっており、独立して調整できなかった——のでこの2点だけを直した
（無いと誤認した機能を重複実装しない、既存方針どおり）。
- **`src/lib/audioEngine.js`**：`settings`に`nextGapSeconds`（既定1秒）を追加。
  `playOne`のstepループに`step.waitNextGap`（真なら`nextGapSeconds`秒待ってから次のstepへ）を
  追加。既存の`step.wait`（固定ミリ秒）・`step.waitGap`（問題文→正解の間、`gapSeconds`）とは
  別の待機ポイントとして独立させた——3つを混同すると、後から「次へ進む間隔だけ」を
  ユーザーが調整できなくなる。
- **`src/components/AudioMode.jsx`**：`buildStepsRaw`内の各分岐（flashcard/summary/compare/
  number/cloze/choices/読み上げ両面/前面のみ/後面のみ）末尾にあった`steps.push({ wait: 700 })`
  （1問の読み上げが終わってから次の問題に移るまでの固定700ms待機、全9箇所）を
  すべて`steps.push({ waitNextGap: true })`に置き換えた。新設の`nextGap`状態
  （`settings.nextGapSeconds`から初期化、0〜15秒のスライダーUI「解答・解説のあと、
  次の問題まで待つ時間（秒）」を「問題文と正解の「間」（秒）」の下に追加）で
  ユーザーが調整できるようにし、`engine.configure({ nextGapSeconds: nextGap })`を
  `useEffect`で同期。**700ms固定だったものを可変にしただけで、既存の「自動で次へ進む」
  という骨格（`runFrom`ループ）自体は変えていない**。
- **科目×誤った問題の組み合わせ**：`plan`のuseMemoで`source === 'review'`の時、
  従来は`reviewPool`（または`customReviewIds`）をそのまま使っていたのを、
  `filterSubject`が設定されていればさらに`.filter((q) => q.subject === filterSubject)`で
  絞り込むよう変更（依存配列に`filterSubject`を追加）。`applyFilter(patch)`は、
  **科目だけを選択／変更した場合に限り**`source`を`'review'`のまま維持する
  （`onlySubjectTouched = 'subject' in patch && !ns.genre && !ns.keyword && !ns.round`の時だけ
  早期returnし、`setSource`を呼ばない）。ジャンル・キーワード・回を同時に選んだ場合は
  従来通り通常の検索モード（`'filter'`）に切り替わる——**これは今回の要望の対象外として
  意図的にスコープ外にした**（要望は「科目選択＋誤った問題」の組み合わせのみ）。
  情報カード（`source === 'review'`の表示）も、科目で絞り込んだ後の件数・科目名を
  反映するよう更新（0件になった場合は「科目名では」を明記した専用メッセージを出す）。
- 検証：`npx vite build`・`node --test`（2607件、既存テストに影響なし）に加え、
  Playwrightで**IndexedDBへ直接`shinkyu:srs`を注入**（UIクリックでの誤答生成は
  ナビゲーションのセレクタが安定せず失敗したため、既存の「IndexedDBへ直接srs/historyを
  注入して解答済み状態を再現する」パターンに切り替えた）して解剖学の問題6問を
  誤答状態にし、実機で確認：「間違えた問題を読み上げる」→科目セレクトで「解剖学」を選択
  →カードの文言が「🔁「間違えた問題」を読み上げ中（6問・科目「解剖学」に絞り込み中）」に
  正しく変わり「誤った問題」モードが維持されること／前へ・次へボタンで位置表示
  （`1 / 6`→`2 / 6`）が正しく進むこと／次の問題まで待つ時間のスライダーが表示されること。
  コンソールエラー0件。

## 復習（Review.jsx）を学習（10・60・300・900）並みに拡張（2026-09-04追加）
ユーザーに「復習の機能について、学習(10・60・300・900)と比べてもっと良くできる機能」を
最大30個尋ねられ、一覧化→ユーザー確認→実装の順で進める既存ルールに従って提案・
そのうち29件（1〜30のうち3番「3分の2バッファ術」を除く全件）をユーザー指定で実装したもの。
- **#1・#5：前へ／次へナビゲーション＋二重記録防止**：出題中に「← 前の問題」「次の問題 →」を
  追加し、Session.jsxと同じ`recordedIds`パターン（このセッションで実際に記録した問題idの配列）を
  導入。`handleAnswered`は既に記録済みのidなら何もしない（no-op）、`handleNext`は解答済みの
  位置を飛ばして次の未解答位置まで進む。見直し中の問題には「📝 解答済みです」の注記を出す。
- **#2：○にした問題の見直し・高速回転**：Session.jsx・Quiz.jsxと同じ`useMaruReview.js`＋
  `MaruReviewCard.jsx`をReview.jsxでも共用（extendedReviewPoolを対象プールに）。
  新しい仕組みは作らず、既存の共有フック・共有コンポーネントをそのまま再利用した。
- **#4：もう一度（同じ順を保持）**：`startWith(pool, opts)`に`opts.label`（セッション種別）・
  `opts.forcedFast`・`opts.preserveOrder`を追加。`preserveOrder:true`の時は原問/派生を離す
  ための並べ替え（`spaceByOriginRaw`のバケット分割）をスキップし、渡された配列の順序を
  そのまま使う（症例連問の隣接だけは`keepCasePairsAdjacentObjects`で維持）——○の見直し等の
  優先順位（うっかり○が先頭）を再計算で崩さないため。完了画面に「🔁 もう一度（同じ◯問・同じ順）」
  ボタンを追加し、完了時点の`order`配列をそのまま渡し直す。
- **#6：○×問題の正解ラベル修正**：誤答一覧で`正解 ${q.answer===0?'○':'✕'}`のように
  「正解 」接頭辞を付けるよう修正（Session.jsxで先に直したのと同じ根本原因のバグが
  Review.jsxにも残っていた）。
- **#7：ジャンル絞り込みUI**：`src/lib/reviewOrder.js`の`filterReview`に`genre`パラメータを
  追加（既定は空文字で後方互換）。科目チップの下にジャンルのセレクトを追加。
- **#8：誤答理由（型）別のワンタップ開始**：誤答理由チップに▶ボタンを追加
  （`quickStartMissType(typeId)`）。弱点タグの「▶で即復習」と同じUIパターン。
- **#9：要注意（リーチ）だけの高速周回**：`leechOnlyPool`（要注意問題を滞留日数の長い順に
  ソート）を追加し、`startLeechFast()`が`forcedFast:true, preserveOrder:true`で開始する。
- **#10：忘却リスク上位を先頭固定する表示オプション**：`pinRiskTop`チェックボックス。
  選んだ並び順（`orderMode`）とは別に、忘却リスク70%以上の問題を一覧・出題の先頭へ固定する
  `applyPinRiskTop`を追加（既存の並び替えロジックは変えず、後段で並べ替えるだけ）。
- **#11：今日の目標をリング＋進捗バーで**：Session.jsxと同じ`.progress`バーをリングの下に追加
  （表示形式を合わせただけで、目標計算ロジック=`reviewGoal.js`は変更なし）。
- **#12・#15・#16：完了画面に誤答理由・要注意の状況カードを追加**：直近の誤答理由トレンド
  （既存`missTypeTrend`）・今週解消できた要注意件数（`reviewDwell.js`の既存`resolvedLeechesSince`）・
  誤答理由ごとの平均滞留日数（新規`reviewDwellByMissType`、`reviewDwellBySubject`の「科目別」を
  「型別」に変えたもの）をまとめて表示。
- **#13：一時停止**：出題中に「⏸ 一時停止して後で続ける」ボタンを追加。1問ごとの自動保存
  （既存の`storage.saveReviewProgress`）をそのまま使うので、開始画面の「前回の続きから」で
  再開できる（新しい保存の仕組みは作っていない）。
- **#14：スヌーズ済み問題の一覧・まとめて解除**：`snoozeLog`のキー一覧から先送り中の問題を
  折りたたみ一覧で表示し、「まとめて解除」で全問の`setNextDue(id, 0)`を呼ぶ。
- **#17：「念のため確認」を除外する絞り込み**：`excludeNagame`チェックボックス。
  `dueReviewQuestions`は元々`isInReview`のみ（念のためを含まない）なので、既定の
  「今日の復習」ボタンには影響せず、他の絞り込みと組み合わせた時だけ効く設計にした
  （`filtering`の条件に含めず、両方の分岐に`applyExcludeNagame`を後がけする形で実装）。
- **#18・#25：語呂合わせノートとの連携**：弱点タグに登録済みの語呂合わせ（`kwMeta[tag].mnemonic`）
  があればその場に表示し、無ければ「📔 語呂合わせを登録」ボタンでMnemonicNotebook.jsxへ
  ワンタップ遷移（`App.jsx`の`focusMnemonicKeyword`/`openMnemonicKeyword`、既存の
  `focusFlashcardKeyword`と同じ「一度だけ消費する中継state」パターン）。MnemonicNotebook.jsx側は
  `focusKeyword`/`onConsumeFocusKeyword`を受け取り、未登録なら追加フォームにキーワードを
  プリセットし、登録済みならその場で編集モードへ入る。
- **#19・#20・#21・#22・#24・#29：確認のみ・変更なし**（既に満たされていたことをgrep・
  コード読解で確認した）：
  - #19（原問と派生を離す仕組み）：`spaceByOrigin`が全ての開始経路で既に一貫して適用済み。
  - #20（音声復習への絞り込み条件伝搬）：`onGoAudio`に渡す`shownList`は既に全フィルタ条件
    （科目・ジャンル・タグ・回・ブックマーク・忘却リスク・誤答回数・誤答理由の型）を
    反映済みのid配列なので、これ以上渡す条件は無かった。
  - #21（完了画面のジャンル別正答率の並び順統一）：Review.jsx・Session.jsxとも既に
    `genreBreakdown.js`の`buildGenreBreakdown`を共用済み。
  - #22（もう一度での誤答理由別間隔調整の再適用）：`onMissType`はQuestionCard側の
    ボタン操作のたびに呼ばれる仕組みなので、もう一度で再演習した問題でも同じ経路で効く。
  - #24（自己説明ステップの条件）：Review.jsxは元々`whyPrompt`を常時渡しており、
    Session.jsxとは独立に機能している（学習側が持たない機能なので「同条件」という
    前提自体が誤りだった。Review側はそのままでよい）。
  - #29（誤答理由フィルタと科目フィルタの併用）：`filterReview`は元から両方を
    同時に受け取れる設計だった（`subjects`・`missType`は独立したAND条件）。
- **#23：カレンダー連携の強化**：`Calendar.jsx`の日別詳細に、科目別滞留日数
  （`reviewDwell.js`の既存`reviewDwellBySubject`）から最も滞留が長い科目を1件添えるように。
- **#26・#27：知識グラフ・フラッシュカードへの相互リンク**：出題中の問題カードの下に
  「🕸️ 知識グラフで見る」（`onOpenGraphConcept`、`conceptsOf(current, links)[0]`を渡す）・
  「🃏 カードで見る」（`onOpenFlashcardKeyword`、`curTags[0]`を渡す）を追加。
  どちらも`App.jsx`の既存の中継関数（`openGraphConcept`/`openFlashcardKeyword`）を
  そのまま再利用（新しいジャンプの仕組みは作っていない）。
- **#28：復習セッションの経過時間表示**：出題中のヘッダーに`⏱ 分:秒`を追加
  （`sessionStartRef`＋1秒ごとのtick。`started`かつ出題中のみタイマーを回す）。
- **#30：完了画面の演出統一**：見出しをSession.jsxの`doneTitle`と同じ考え方で
  `${sessionLabel} 完了！`（ラベル無しの通常復習は従来通り「復習完了」）に、
  全問○の時の終了ボタン文言を「復習リストに戻る」→「🏠 終了する」に統一。
- テストは`test/reviewDwell.test.mjs`（`reviewDwellByMissType`2件追加）・
  `test/reviewOrder.test.mjs`（`filterReview`のジャンル絞り込み1件追加）。
  `node --test`（2610件）・`npx vite build`・`npm run validate`・
  `node scripts/check-bundle-size.mjs`（起動時JS 612KB/700KB）はすべて通過。
  Playwright QAで実機確認済み：IndexedDBへ直接`srs`/`history`/`missTypes`を注入して
  要注意8問・誤答2問（ケアレス型の記録つき）・○採点履歴3問を再現し、
  「要注意だけ高速周回」「○にした問題をふりかえる」「ジャンル」「念のため確認を除外」
  「忘却リスク上位固定」の各UIが表示されること、要注意だけ高速周回を開始すると
  タイマーに種別ラベル・経過時間が表示され前へ/次へで位置が正しく進むこと、
  前へ戻ると「解答済み」注記が出ること、一時停止すると「前回の続きから」で
  再開できる状態に戻ること、誤答理由「ケアレス」の▶から即開始した回を全問○で終えると
  完了画面の見出しが「誤答理由「ケアレス」 完了！」になり「もう一度（同じ◯問・同じ順）」
  ボタンが出ることを確認。コンソールエラー0件。

## 復習画面（Review.jsx）の追加20項目＋ユーザーからのUXレビュー反映（2026-09-04追加）
上のバッチ（29項目）の後、ユーザーに「学習(10/60/300/900)と比べてもっと良くできる機能」を
20項目提案・承認を得て全実装。加えて同じ回でユーザーから「復習画面レビュー（統合版）」という
UXレビュー文書（👍良い点／🔴🟡🟢優先度別の指摘）を渡され、実際にバグかどうかを1件ずつ検証した
うえで対応したもの。
- **60問ごとの休憩画面**：`SET_SIZE=60`。`handleNext`が60の倍数の位置に来ると
  `showBreak`をtrueにし、Session.jsxと同じ「ひと区切り」カード（続ける／終了して後で続ける）を挟む。
- **周回速度ログ（G-100由来）を復習にも導入**：`roundLog.js`はSession.jsxと同じキーを共有するため、
  混ざらないよう`toReviewRoundTarget(batchValue)=10000+batchValue`で名前空間を分けた
  （標準の復習・`batch>0`の時だけ記録。ラベルつきセッションや「すべて」は対象外）。
  完了画面に「🔁 通算◯回目・所要時間・前回比」を表示。
- **「🎯 今日のおすすめ」**：1回の問題数セクションに、今の絞り込みの在庫数から
  `REVIEW_TARGETS`の中で無理のない問数を提案するボタンを追加（Session.jsx同等機能の移植）。
- **「念のため確認だけ」に対象数スライダー**（`nagameCap`、5〜50・既定20）を追加。
  「🩹 今日は少しだけ（忘却リスク上位5問）」ボタンも新設（しんどい日の最小構成）。
- **要注意リストのCSV書き出し**（`leechListToCsv`、`reviewDwell.js`に新設）と
  「要注意をまとめてブックマーク」ボタンを追加。
- **「今日集中すべき科目」の声掛け**：Session.jsxが既に使っている`todayFocus.js`の
  `todayFocusSubjects`をReview.jsxからも呼び、開始前画面に「🧭 たまには苦手分野の
  「◯◯」の復習にも挑戦してみましょう」＋「この科目にしぼる」ボタンを追加
  （新しい判定ロジックは作らず既存を再利用）。
- **忘却リスクの科目別ヒートマップ**（`riskBySubject`）・**誤答理由×科目のクロス集計**
  （`missTypeBySubject`）をそれぞれ開始前画面にカード表示。
- **完了画面の追加**：①「🔀 もう一度（新しく組み直す）」（○の見直し・○の高速回転以外の
  セッションで、今の絞り込み・最新のsrsからプールを引き直す。○系は優先順位が意味を持つため対象外）
  ②「🔊 間違えた分だけ音声で聞き直す」（`onGoAudio(misses.map(q=>q.id))`）
  ③「▶ 続けて今日の復習へ」（`要注意だけ高速周回`の直後、今日の期限がまだ残っていれば）
  ④「明日の最初の1タスク」に弱点テーマ・要注意からの追加候補チップ
  ⑤要注意の解消件数を「今週◯問（先週は◯問）」で先週比表示（`resolvedLeechesSince`を2区間で計算）
  ⑥誤答理由（型）で絞り込んだセッション限定で、その型の正答率が「これまで約◯% → 今回◯%」に
  変わったかを表示（`sessionLabel`の`誤答理由「◯◯」`を`MISS_TYPES`で逆引きし、セッション開始前の
  履歴から算出。3件未満は「まだ判断できない」として非表示）
  ⑦誤答・あいまい一覧の各行にその問題のメモがあれば表示。
- **一覧（開始前の問題一覧）の拡張**：①○×/四択の種別バッジ（`QuestionCard.jsx`と同じ
  `.badge`/`.badge.ox`/`.badge.choice`を再利用）②登録済みの語呂合わせがあればその場表示、
  無ければ登録ボタン（従来は弱点タグのチップにしかなかったものを一覧全体に拡張）
  ③知識グラフ・フラッシュカードへのボタン（従来は出題中の1問にしかなかったものを
  一覧の各行にも追加。`onOpenGraphConcept`/`onOpenFlashcardKeyword`は既存の中継関数を再利用）。
- **UXレビュー文書の指摘への対応**（実際にコードを読んで、バグか仕様（説明不足）かを判定）：
  - 🔴「数字の不整合"(復習リスト/今日の復習/一覧の件数がズレる)」→ **計算バグではない**。
    `reviewQuestions.length`（一度でも間違えた総数）・`dueReviewQuestions.length`
    （期限が来ている数、念のため確認は含まない）・`shownList.length`（絞り込み後の表示件数、
    念のため確認を含む）はそれぞれ別の定義で正しい。**直したのは説明不足の方**——3つの数字
    タイルに`title`属性で定義を明記、一覧見出しに「※内訳：復習リスト◯件＋念のため確認◯件」を
    追記した。
  - 🔴「平均誤答数の計算バグ疑い」→ **バグではない**。`missTypeAnomaly`は誤答理由ボタンで
    手動分類した誤答"だけ"を数える（全誤答の一部）。一覧に出ている他の「誤答」件数
    （弱点タグのwrong等）は全誤答を数えるため数字が食い違って見えていた。表示文言に
    「誤答理由ボタンで分類した分のみ」と明記して解消。
  - 🔴「図表問題の画像未表示」→ QuestionCard.jsxに、設問文が「図」「写真」を含むのに
    `question.image`も`question.figure`も無い場合に**「この問題は判断せず、あとで
    報告してください」**という警告バナーを追加（根本解決には画像データの追加が要るため、
    まずは誤答扱いにしないための注意喚起）。
  - 🔴「削除の確認・アンドゥなし」→ 「🗑 リストから外す」を押す直前のsrs状態を保持し、
    直後に「↩️ 元に戻す」の取り消しパネルを一覧の先頭に表示（直近5件・端末内メモリのみ）。
    `useStore.js`に`restoreReviewState(questionId, prevState)`を新設。
  - 🟡「科目別マスター率が全部0%」→ 実際に0%だった科目は「未着手（0/◯）」という
    グレー表示に変更（圧迫感のある赤い0%バーを避ける。**判定ロジックは変更なし**、
    表示だけの改善）。
  - 🟡「○×/4択の区別不能」→ 上記の種別バッジで解消（QuestionCard.jsxには元々あったが
    Review.jsxの一覧にだけ無かった）。
  - 🟡「弱点テーマの誤答数に母数がない」→ チップの表示を「誤答◯」→「誤答◯／◯」
    （分母＝挑戦回数）に変更。
  - 🟡「スライダーに現在値表示がない」→ 既存の忘却リスク下限・誤答回数下限のスライダーは
    元から`({minRisk}%以上だけ)`等の現在値表示を持っていた（レビュー時点で未実装だった
    新設の`nagameCap`スライダーには最初から現在値表示つきで実装）。
  - 🟡「色分けの凡例がない」→ 一覧の直前に「バッジの色：赤=要注意・高リスク／
    黄=中程度／グレー=軽め」という凡例を追加。
  - 🟡「誤答理由ボタン選択後のフィードバック不明」→ QuestionCard.jsxに`justPickedType`
    状態を追加、選択直後0.9秒だけ「✓ 「◯◯」で記録しました」と表示してから次の問題へ進む。
  - 🟡「99+通知バッジの圧迫感」→ 下部ナビのバッジを`store.reviewQuestions.length`
    （復習リスト総数＝常に99+に張り付きがち）から`store.dueReviewQuestions.length`
    （今日やるべき件数）に変更（`App.jsx`）。「達成感が無い」という指摘の実質的な原因は
    分母が違うことだったため。
  - 🟢「誤答理由ボタンのレイアウト崩れ（ケアレスだけ改行）」→ `.misstype-row`を
    `flex-wrap: wrap`から`nowrap + overflow-x: auto`に変更（横スクロールで崩れを防ぐ）。
  - 🟢「内部識別子`#2`等の露出」→ 開発用のコメント記法（`（#2）`等）が、コメントではなく
    実際にレンダリングされるJSXの文字列内に混入していたバグ。**Review.jsxの1箇所だけでなく
    リポジトリ全体を`grep -rn "（#[0-9０-９]" src/components/*.jsx | grep -v "//\|{/\*"`で
    横断検索し、Exam.jsx（重複2箇所）・CoverageMap.jsx（5箇所）・Home.jsx（1箇所）にも
    同種の漏れを発見・全て除去**（指摘された1箇所だけを直して終わりにしない）。
  - 🟢「タップ領域が窮屈」→ 一覧の操作ボタン行（`.list-item .btn-row`）のボタンに
    `min-height: 40px`・余白拡大・`flex-wrap: wrap`を追加。
  - 🟢「文字サイズ小さめ」「ブラウザUIとの重複」→ **対応済み事項の再確認**——文字サイズは
    Settings画面のアクセシビリティ設定（`settings.a11y.fontScale`、2026-09-03実装済み）で
    全体を`zoom`拡大できるため追加対応なし。ブラウザUIとの重複は具体的な再現手順が
    文書に無く、断定できる情報が無いため見送り（自信のない情報は断定しない、の方針どおり）。
- テストは`test/reviewDwell.test.mjs`に`leechListToCsv`1件追加（15件）。
  `node --test`（2611件）・`npx vite build`・`npm run validate`・
  `node scripts/check-bundle-size.mjs`（起動時JS 624KB/700KB）はすべて通過。
  Playwright QAで実機確認済み：IndexedDBへ`srs`/`history`/`missTypes`を注入して
  要注意4問・近マスター1問・ケアレス型記録2問を再現し、開始前画面の9項目
  （今日集中すべき科目の声掛け・色凡例・科目別忘却リスク・誤答理由×科目・今日のおすすめ・
  念のため確認スライダー・CSV書き出し・まとめてブックマーク・科目別マスター率）が
  すべて表示されること、一覧の先頭項目に種別バッジ・要注意滞留日数・忘却リスクが
  正しく出ること、「要注意だけ高速周回」を最後まで解いて全問○で終えると完了画面に
  「🔀 もう一度（新しく組み直す）」「▶ 続けて今日の復習へ」ボタンと
  「誤答理由・要注意の状況」カード（型別の平均滞留日数）が出ることを確認。コンソールエラー0件
  （QAスクリプト側の不具合として、端末内ロック未設定時に出る初回セットアップ画面
  （AuthGate、「あとで設定する（スキップ）」）をスキップし忘れて`.bottom-nav`が見つからず
  詰まる事故が起きたが、これはQAスクリプトの手順漏れでアプリ側のバグではない）。

## ホーム画面（Home.jsx）の実機QAで見つけた修正（2026-09-04追加）
ユーザーに「ホームの機能を動作して修正するところあれば最大20こ」と依頼され、実際に
IndexedDBへデータを注入してPlaywrightでホーム画面の全カード・メニュー37項目をタップして
検証したうえで20件を提案・全件をユーザー指定で実装したもの。
- **App.jsxの`renderView()`で、未知の`view`値（壊れた`lastView`等）が`default:`に落ちた時、
  `case 'home':`と別々にpropsを渡していたバグを修正**（実機QAとは別に、コードを読んで発見した
  もっとも実害の大きいバグ）。`default`側は`onQuickReview`・`onGoAudioReview`・`installPrompt`・
  `onInstall`が渡っておらず、この経路でHomeが描画されると「復習だけ5問」「🎧音声で復習」等の
  チップを押しても何も起きない状態になっていた。`case 'home': default:`で同じJSXを共有する形に
  統合し、以後この種のズレが起きない構造にした。
- **`Analytics.jsx`に見出し（`<h2 className="view-title">`）が無かったのを追加**
  （空データ時・通常時の両方の`return`）。他の全画面にあるのにこの画面だけ無かった。
- **`src/lib/timeOfDay.js`の`isNowBestTime`に絶対的な下限（`minAccuracy`既定0.5）を追加**。
  以前は「他の時間帯"より"相対的に高い」だけで判定していたため、全時間帯が低調な人でも
  一番マシな帯（例：22%）が「正答率が高い傾向」と表示されてしまっていた（実データ注入で
  再現・確認済み）。`Analytics.jsx`の「時間帯別の正答率」セクションにも同じ下限を適用。
- **`Home.jsx`のメニューに「用語集（目次・索引）」を追加**（`onNavigate('glossary')`）。
  以前は「まだ使ったことのない機能」の日替わり提案か`目次`画面の中のボタン経由でしか
  辿り着けなかった（大きく作り込んだ機能なのに常設の入口が無かった）。
- **「試験日まで残り◯日」バナーの遷移先を`settings`→`calendar`に変更**（カレンダーには
  既に試験日カウントダウンがあり、文脈として自然）。
- **「忘れそうな1問」カードのボタンを、Review.jsxを開くだけから、その1問だけを
  `onQuickReview({ids:[...]})`で直接開始するよう変更**（要注意カードと同じ考え方に統一。
  以前は「確認する」と言いながら、指摘した当の問題までは連れて行けていなかった）。
- **「弱点分析」（Dashboard.jsx）と「分析・攻略率・合格診断」（Analytics.jsx）の内容が
  科目別正答率・14日間の学習量でほぼ重複していた問題**——画面統合はせず、Dashboard.jsx側に
  「こちらは"生の"正答率だけのシンプル版」という位置づけの説明文と、Analytics.jsxへの
  クロスリンクボタンを追加（`App.jsx`の`case 'dashboard'`に`onNavigate`propsを追加）。
- **「○にした問題をふりかえりませんか」カードのボタンを、学習画面固定から
  学習画面／一問一答の2択に変更**（Quiz.jsxにも同じ○の見直し機能があるため、
  普段どちらを使っているかHome側からは分からないので両方の入口を出す）。
- **模試（Exam.jsx）の途中経過を「前回の続きから」に追加**（`loadExamProgress`/
  `clearExamProgress`。以前は学習・一問一答の2つだけで、模試はExam.jsxを開くまで
  途中経過に気づけなかった）。長押し削除は既存の学習・一問一答と同じ`useLongPress`パターン。
- **「今日集中すべき科目」と「科目バランスの偏りに注意」が同じ科目を別々の理由で
  重複表示することがあった問題**——バランス警告に既に出ている科目は、集中すべき科目の
  チップから除く（`dedupedFocusSubjects`。バランス警告の方がより具体的な指摘のため
  そちらだけに任せる）。あわせて集中すべき科目チップの理由テキストに色（`var(--warn)`）を付け、
  埋もれて見えていた指摘を目立たせた。
- **クラウド同期バナーの「再ログインが必要」（🔑）と「同期できていません」（⏰）を
  別アイコンに変更**（以前はどちらも⚠️で、対処法が違うのに見分けが付かなかった）。
- **「配分の目安」カードに実際に調整できるボタンを追加**（比率のズレが15%以上の時、
  復習寄りなら「学習（新規）を増やす」、新規寄りなら「復習を増やす」で該当画面へ）。
  以前は数字を見せるだけで、次に何をすればよいかの導線が無かった。
- **「まだ使ったことのない機能」カードに「🔍 全機能一覧でまとめて探す」ボタンを追加**
  （日替わり1件で見逃すと二度と出ないため、探し直せる場所への案内を添えた）。
- **11月末（合格実力ゴール）の日付が`Home.jsx`にハードコードされ、単一の正である
  `ROADMAP_PHASES`（`kind:'goal'`のフェーズ）と二重管理になっていた問題を修正**——
  `goalPhaseEnd`をROADMAP_PHASESから毎回導出するようにし、ハードコードされた日付を削除。
- **QA中に自己発見・その場で修正したもの**：長いスクロール対策として一度
  「▶ 今すぐ学習を始める」という近道ボタンをMascotの直後に追加したが、実機QAで
  Mascot.jsx自身が既に「📅 今日の進捗（タップで学習へ）」という同じ`session`への
  リンクを持っていることが分かり、真下に完全に同じ機能のボタンが2つ並ぶ状態になった
  ため、追加した方を削除して元に戻した。**同じ画面に機能を足す時は、その画面が既に
  持っている導線と重複していないか、追加した直後に実機で確認すること**（このミスの
  再発防止として記録）。
- 検証：`node --test`（2612件）・`npx vite build`・`npm run validate`・
  `node scripts/check-bundle-size.mjs`（起動時JS 627KB/700KB）はすべて通過。
  Playwright QAで実機確認済み：IndexedDBへ`srs`/`history`/`examProgress`/`settings`を
  注入し、模試の続きからカードが表示され模擬試験画面へ正しく遷移すること、用語集メニューが
  用語集（目次・索引）画面へ正しく遷移すること、分析画面に見出しが表示されること、
  弱点分析画面のクロスリンクボタンが分析画面へ正しく遷移すること、コンソールエラー0件を確認。
  `test/timeOfDay.test.mjs`に`minAccuracy`の下限を検証するテストを1件追加。

## ミス防止ルール（2026-08-17 追加・失敗の再発防止）
- **新機能を「無い」と判断する前に必ず調査する** — 上の機能一覧に加え、
  `grep -rln "<関連語>" src/components/*.jsx src/lib/*.js` で横断検索してから回答・実装する。
  似た名前の既存コンポーネント（Assoc*・Insights*・Connected*等）を見落としやすい。
- **自信がない・未確認の情報は断定しない** — 「実装済みかどうか未確認です」と明言してから、
  確認後に訂正する。
- **10個・全部のような大きな依頼は、まず一覧化→ユーザー確認→実装の順で進める**
  （すでに存在するものを重複実装しない）。
- 大きな変更ほど、実装後にブラウザ（Playwright＋Chromium）で実際に操作して確認する。
  ビルドが通ることと機能することは別。
- **過去問PDFの内容は、既存収録との突き合わせ作業中であっても記憶で再現しない** —
  同じ会話内で既にPDFを読んでいても、問題文・選択肢・問題番号を記憶から書き出すと
  番号のズレや「実際は収録済みなのに未収録と誤判定する」類の間違いが起きる
  （2026-08-19、第34回過去問の収録漏れ調査中に自己発見・修正）。既存収録と照合する時は
  ページ内容を再度読み直すか、直前のツール出力を直接参照し、記憶のみで一覧化しない。
- 同じ種類の間違いが起きたら、このセクションに追記して残す。
- **他の同梱アプリの機能と、鍼灸アプリ本体の機能を混同しない** — 2026-08-24、「知識ベース
  （動画・書籍要約＋二段階チェック、`lib/knowledge.js`）に定期見直しを追加」を提案・承認を得たが、
  実装前に確認したところ`lib/knowledge.js`は**腰痛ナビ（youtsu-navi/）側**のファイルで、
  鍼灸アプリ本体には存在しなかった（鍼灸アプリの「知識」機能は`knowledgeGraph.js`/`KnowledgeGraph.jsx`
  のみで、これは概念どうしのつながりを扱うもので動画・書籍要約とは別物）。CLAUDE.mdの構成上、
  腰痛ナビの要点再掲セクションと鍼灸アプリ本体のセクションが近接しているため取り違えた。
  着手前に対象ファイルが実際にどのアプリのものか`ls`/`grep`で確認する。
- **「新機能を10個提案」のように自分から出した提案自体が、既存機能と重複していないか確認する** —
  2026-08-20、「模試スコアの時系列グラフ」を新機能として提案・承認を得たが、実装前に調査したところ
  `Exam.jsx`の`ExamHistory`（モード選択画面、午前午後の記録カード）に合否予測・パスライン付き
  棒グラフ・受験回数/合格ライン到達/ベストスコアの集計が既に実装済みだった（未実装と誤認して
  提案してしまうところだった）。提案する側であっても、着手前に必ず機能一覧＋grep調査を行う。

## 検証
- `npm run build` と `node --test`（現状240件）を通す。
- 可能なら Chromium（`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`）で
  プレビュー描画を確認（`npm run preview` → Playwrightでスクリーンショット）。
