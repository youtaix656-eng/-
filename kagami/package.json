{
  "name": "kagami",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "鏡（かがみ） — 心理的な操作の「型」に気づくためのアプリ（端末内保存のみ）",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
