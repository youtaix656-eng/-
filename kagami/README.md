# 鏡（かがみ）

「そう言われると断りにくい」の正体に、名前をつけるためのアプリ。

公開先：https://youtaix656-eng.github.io/-/kagami

人を動かす言い回しには、昔から名前と研究があります。名前を知らないうちは
「自分が弱いからだ」と感じますが、名前がつくと**その場で気づける**ようになります。

**操るための道具ではなく、気づくための道具**です。言われた言葉を貼ると、
54件の型のどれに当たるかを**この端末の中だけ**で調べます。

## できること

| 画面 | 中身 |
|---|---|
| 🔎 貼って調べる | 言われた言葉・届いたメッセージを貼ると、当たった型と**当たった語**が出る。本文のどこに当たったかを印で示す |
| 🪞 自分の言い方を見る | これから送る文章を貼る。使う癖はそのまま人に向く癖になるので、送る前に一度見る |
| 🎭 操作の型 | 54件を8つのまとまりで。なぜ効くのか・見分け方・言われる形・できること・出典 |
| 🛡 返し方 | 10件。**相手を言い負かす言葉は置いていない**（後述） |
| ⊕ 自分の側で起きること | **自分の側の癖**（6件）と、**長く続いた時に起きること**（5件）。落ち度の話にしない・診断しない |
| ☖ 人間分析 | 気になる相手との**距離の決め方**（15件・ふるまい79項目＋共通する芯4つ＋場面5つ）。各型に**黒い心理学での対応策**（`counters`）付き。見たふるまいにチェックすると近い型が出る。**読みでも引ける検索（AND・ハイライト・履歴・0件の候補）／場面・芯でしぼる／型の一覧／逆引き**。各型の**対応策は開かずに見え、世に出回っている呼び名（沈黙の圧力・捕食者のテンポ…）と言い方の例つき**。やってみた記録（○△✕）を残すと、**自分の記録の中の相対だけ**で並び順が変わる。**場面（どこで起きたか）でしぼれる。採点しない。**見立ては端末内に保存でき、あとから開いて続きから直せる |
| ✕ 当てにならない見抜き方 | 「本音が読める」とされているもの（7件）。信じると何もしていない人を疑ってしまうので、**信じないための材料**として置く |
| 📝 記録 | いつ・どこで・どの型に当たったか。相手の名前・連絡先は持たない |
| 📇 目次 | 全207項目を読み順（あ〜ん → A〜Z）で。**世に出回っている呼び名（間欠強化・ツァイガルニク効果など）からも引ける**（索引の「→ 見よ」項目） |
| 📚 出典 | どの型がどこから来ているか |

## 決めていること（触るときはここを守る）

1. **点数・危険度・順位を付けない。** 「危険度80点」を出すには他人の事例という
   手元にない基準が要る。出せるのは「どの型の言い回しに、どの語で当たったか」だけ。
2. **当たった＝相手が悪い、ではない。** 同じ言葉はふつうの会話でも使う。
   判定に使った語は必ずそのまま見せる（見せない判定は当てずっぽうの断定と同じ）。
3. **当たらなくても黙らない。** 「短すぎて調べられない」「当たらなかった」を言い分ける。
   当たらない＝問題がない、ではないことも画面に書く。
4. **返し方に「言い負かす言葉」を置かない。** 言い合いは立場が弱い側ほど不利になる
   （言い返せる関係なら、そもそも困っていない）。置くのは相手の同意が要らないことだけ——
   時間を置く・持ち帰る・記録する・人に話す・その場を離れる・窓口に聞く。
5. **出典に URL を書かない。** その場で確かめられない状態でそれらしいリンクを書くのは、
   出典があるように見えて実は無いという、いちばん質の悪い形になる。書名・発表者・年で辿れる形にし、
   確かめきれていないものには `check: true`（画面に「※要確認」）を付ける。
6. **氏名・連絡先を持たない。** 記録は場面（職場／家庭／勧誘／ネット／その他）だけ。
   本文は既定で伏せて保存し、伏せ字で**桁数を数えられないようにする**
   （〔電話番号〕のような札に置き換える。`●●●●` だと別々の番号が形で見分けられる）。
7. **`src/lib/` はネットワークに触れない。** 貼った文面が端末から出ないことの根拠がここ。
   テストが `fetch` などが無いことを機械チェックする。
8. **後読み（lookbehind）を使わない。** 古い Safari が構文エラーになり、読み込みごと落ちる。
9. **色を持たない。** 黒地に白い線（銅版画の図版）の一枚絵として作る。使ってよいのは
   黒・白・その間の灰だけで、`styles.css` の色はすべて R=G=B。テストが機械チェックする。
10. **絵文字を使わない。** 絵文字は環境によって色付きで描かれるので、モノクロが崩れ、
   しかも端末ごとに形が変わる。画面に出す印は `src/data/glyphs.js` が単一の正で、
   **文字として描かれる記号だけ**（☉ ☾ ✦ △ ⊕ ※ …）。色は周りの文字から受け継ぐ。
11. **飾りは SVG でその場に線を引く。画像ファイルを持たない。**
   ウロボロス・六芒星と目・十字の印は `src/components/Ornament.jsx`。色は `currentColor` だけ。
12. **飾りより読みやすさを優先する。** 飾りの線は薄く、文字の下に濃く敷かない。
   当たった語は**白地に黒（反転）**で示す——モノクロで最も強い強調がこれ。
13. **言葉の型とふるまいの型を分ける（`channel`）。** 眉間の凝視・沈黙の圧力・
   捕食者のテンポは**言葉として現れない**ので、貼った文面からは絶対に見つからない。
   `detect.js` は `channel: 'behavior'` を必ず外し、画面は
   「文面からは見つからない型が◯件あります」と**毎回言う**。
   読めないものを読めるふりをしない、が守りたい線。
14. **同じ型を二度足さない。** 世に出回っている呼び名（間欠強化・希少性の原理など）は
   `aka`（名前＋読み）で既存の型に繋ぐ。別名は目次に「→ 本来の型名」として並ぶので、
   その呼び名で調べに来た人も同じ1件に辿り着く。
15. **型（相手のすること）と癖（自分のしていること）を混ぜない。** `habits.js` は別のデータ。
   混ぜると「当たった＝相手が悪い、ではない」の線が崩れる。癖の側でいちばん大事なのは
   **本人を責めないこと**——つけこまれるのはつけこむ側がいるからで、癖は原因ではない。
   「自業自得」「甘い」の類と「性格を変えろ」はテストが弾く。
   その場でできる一手も、**相手の同意が要らないこと**だけにする（返し方と同じ線）。
16. **見抜き方の技法を「使える道具」として置かない。** 表情・言い淀み・反応の速さ・
   目線から嘘を見抜く方法は広く出回っているが、人が嘘を見抜ける割合はコイン投げを
   わずかに上回る程度にとどまる。**当たらない技法を信じると、外れたことにも気づけず、
   何もしていない人を嘘つきだと決めつける。** `myths.js` は
   「よく言われていること」と「分かっていること」を必ず分けて書く。
17. **自分の中で起きること（`states.js`）で診断しない。** 病名を当てない、
   当てはめて判定する仕掛け（チェックリスト）を作らない、本人の弱さの話にしない、
   必ず「人に相談する」へ繋ぐ。この4つはテストが機械チェックする。
18. **人間分析（`people.js`）を属性で分けない。** 元にした文章は 高齢者／女性／男性 で
   ふるまいを分けていたが、そのまま持ち込むと「女性は◯◯」という決めつけの道具になる。
   分けるのは**ふるまい**だけ。その文章自身が結論として挙げていた3つの芯
   （一貫性のなさ・境界線の軽視・責任を取らない）が、性別も年代も問わない共通点。
   元の章立てが実際に持っていた情報は「どこで起きたか」なので、それは**場面**
   （職場／家庭／道・店／子ども連れの場／ネット）として残してある。
   属性でくくる言い方が入っていないことをテストが機械チェックする。
   **元の文章のふるまいを1つも落としていないこと**も、テストが名指しで確かめる。
19. **人間分析の見立てを保存する時は、判定ではなく入力（チェックしたふるまい）を残す**
   （`cases.js`。腰痛ナビのカルテと同じ考え方）。型やふるまいをあとから直しても、
   過去の見立てを読み直せる。**氏名・連絡先の欄を持たず**、呼び名だけ。
   呼び名とメモに混ざった電話番号・メール・リンクは自動で伏せる。
   端末を見られる可能性があるなら本名ではなく呼び名を、と画面に書く。
   **1つ当たっただけで型を出さない**（誰にでも当てはまる）。
   点数・順位・診断名を出さず、出すのは「見たふるまい」と「取れる距離」だけ。
   決めるのは距離であって、その人がどういう人間かではない。
20. **人間分析に型を足したら、`counters`（黒い心理学での対応策）も必ず書く。**
   使い返しに挙げるのは `SELF_DEFENSE_TACTIC_IDS`（こちらが呑まれないための型10件）から。
21. **頼まれたものだけを足す。** 渡された文章を入れる時に、頼まれていない注意書き・制限・
   禁止リスト・見出しを自分の判断で足さない。必要だと思ったら、足す前に一度きく。
22. **型カードは `PersonTypeCard.jsx` を、見立てと型の一覧の両方で使う。**
   画面ごとに書くと、片方だけ直したときに必ず食い違う。
   **目次の飛び先 `toc-person-*` は型の一覧の側に置く**——見立てに出ている型にしか
   置かないと、目次から飛んでも着かない（実際にそうなっていた）。
23. **やってみた記録（`tried.js`）で点数を作らない。** 残すのは ○△✕ だけで、
   効き目の割合も、相手がどう変わったかも記録しない。分かるのは
   「自分がそれをやれたかどうか」まで。並べ替えは**自分の記録の中の相対**だけで、
   **1回の記録では順番を変えない**（`MIN_TRIES`＝2）。
24. **対応策の並べ替えは `tried.js` に寄せる。** 画面で順番を決めない
   （場面・自分の記録・元の並び、の順で `orderCounters` が決める）。
25. **黒い心理学の手を出すときは、世に出回っている呼び名を必ず添える。**
   「答える前に間を置く」だけでは、それが「沈黙の圧力」だと分からない。
   呼び名は `tactics.js` の `akaNameOf` / `tacticLabel` から取る——
   **画面ごとに `aka[0]` を書かない**（片方だけ直した時に食い違う）。
   目次でも呼び名を先に出す。使い返す10件すべてに呼び名があることをテストが見る。
26. **同じ画面に2種類の飛び先があるときは、両方に分岐を書く。** 癖と状態、型と芯のように
   1画面に2つのデータが並ぶと、片方の anchor しか見ずに取りこぼす（実際に踏んだ）。
27. **画面に出る文にマークダウンを書かない。** `**強調**` はそのまま「**」と表示される
   （実際に出した）。テストが機械チェックする。
28. **人間分析は3つの枠に分ける**（`People.jsx` の `TABS`＝調べる／型を読む／保存したもの）。
   **目次から飛ぶ時は、飛び先のある枠へ「描く前に」切り替える**——`useEffect` で切り替えると、
   飛ぶ時（`requestAnimationFrame`）にはまだ古い枠が描かれているので着かない（実際に踏んだ）。
   初回は `useState` の初期値で、以降は描いている途中で直す。
29. **上書きで消さない**（`lib/caseTools.js`）。見立てを直すたびに前の版を残す
   （`SNAPSHOT_MAX`＝20）。同じ人でも3か月前と今では見えているものが違う。
   **比べても採点しない**——出すのは共通・片方だけの3つだけで、良くなった／悪くなったは書かない。
30. **古い見立てを勝手に消さない・勝手に直さない**（`STALE_DAYS`＝90日で印を付けるだけ。
   競合台帳・知識ベースと同じ線）。
31. **距離の段（`STAGES`）に良し悪しを書かない。** 右へ行くほど正しいのではなく、
   いまどこにいるかを置くだけ（戻してもよい）。テストが「正しい・べき・勝ち負け」を弾く。
32. **その手が効かなかった時の次の一手を必ず持つ**（`COUNTER_NEXT`）。行き止まりにしない。
   次の一手も**相手の同意が要らないこと**だけにする（返し方と同じ線）。
   やる順番（`COUNTER_STEP`）は**段が上ほど強いのではなく、後戻りしにくいという意味**。
33. **自分の癖で使いにくい手は、知らせるだけで止めない**（`habits.js` の `hardCounters`）。
   押せなくすると、その人がいちばん要る手に手が届かなくなる。
   **癖は自分で選ぶ**（貼った文面から自分の癖は分からないので、判定を持たない）。
34. **持ち出し・取り込みは `lib/personIO.js`。** 持ち出すのは**入力（チェックしたふるまい）と
   記録だけ**で、判定も、さがした語（検索履歴）も入れない。
   **取り込みは必ず確認を出してから**（今あるものは消さない・同じ id はあとから直したほうを残す）。
   読めない形は黙って捨てず、理由を出す。
35. **消したものは1件だけ、20秒だけ戻せる**（`makeUndo`／`UNDO_MS`）。
   **「人間分析だけ消す」も必ず確認を出し、何が残るかを書く**（型・癖・状態の記録は残る）。
36. **重なりの表（型 × 見立て）に点数を書かない。** 出るのは件数だけ。
   同じ型が何人にも並ぶことに意味があるのであって、多い人が「悪い人」ではない。
37. **相談用の文（`toConsultText`）に判定を入れない。** 事実を見た順に並べるだけ。
   言い合いになりにくいのは、事実だけを時系列で置いた時。

38. **おすすめは3つ・番号順に出す**（`lib/tried.js` の `recommendThree`）。
   **並びは「後戻りのしにくさ」（`COUNTER_STEP`）が先**で、自分の記録と場面の合い方は
   **同じ段の中だけ**で効かせる——記録が2回付いただけの手が①に来ると、
   いきなり後戻りしにくい手から始めることになる。番号の印は `glyphs.js` の `ORDER_MARKS`。
39. **「相手はどうなるか」（`counters[].effect`）に、効き目の大きさを書かない。**
   「◯％の人が従う」は実験の条件次第で変わるので、型に書けば必ず嘘になる（型カタログと同じ線）。
   書くのは**その場で何が起きやすいか**だけで、**必ず外れる場合を残す**
   （「〜が多い」「それでも続くなら」「〜とは限らない」）。テストが機械チェックする。
40. **「相手が変わる」と書かない。** 変えられるのは距離であって、相手の人格ではない。
   改心・反省させる・性格が直る、の類はテストが弾く。
   画面にも毎回「相手の性格が変わるという意味ではありません／逆に出ることもあります」を出す。

41. **落ちても行き止まりにしない**（`components/ErrorBoundary.jsx`）。受け皿が無いと、
   1か所落ちただけで下部ナビごと消えて再読み込みしか手が無くなる（実際に踏んだ）。
   **勝手に消さない**——「データは消していません」と書いて、書き出す口へ寄せる。
42. **外から来た見立ては `cases.normalizeCase()` を必ず通す。** `checkedIds` や `note` が
   無いだけで画面が落ちる。取り込みも、端末に入っているものも、ここを通してから画面へ渡す。
43. **消したものを端末に残さない。** `undoCase`（消した直後だけ戻せる覚え書き）は
   保存に混ぜない——混ぜると、消したはずの本文が localStorage に残り続ける。
44. **「元に戻す」は、いる場所に出す**（`.undo-bar` は `position: fixed`）。
   画面の上に置くと、一覧の下のほうで消したときに見えないまま20秒で戻せなくなる。
45. **隠した手は型ごとに持つ**（`personView.hiddenByType`）。型をまたぐと、
   ひとつ隠しただけで別の型のおすすめ3つが黙って減る。
   **隠した手は「まず1つ」「今日試す1つ」からも外す**（隠したものを先頭で勧めない）。
46. **「まだ試していない手」は見立てごとに数える**（`untried(counters, tries, caseId)`）。
   人をまたぐと、A さんで試した手が B さんでも「試した」扱いになる。
47. **消す操作には必ず確認を出す**（記録・見立て・やってみた記録・すべて消す）。
   **作った記録は必ず消せる**——消す口の無い記録を作らない（自由記述が入るならなおさら）。
48. **「すべて消す」は、実際に消えるものを全部書く**（記録・見立て・やってみた記録・
   自分の癖・しぼり込み・設定）。書いていないものを消さない。
49. **画面を移っても書きかけを捨てない**（`App.jsx` の `uiRef`）。
   **端末には保存しない**——「貼った文面は残さない」「選んだだけでは残らない」の約束は
   そのまま。タブを閉じれば消える、開いている間だけの覚え書き。
50. **目次の飛び先は目次（`toc.js` の `anchor`）が持っているものを使う。**
   画面側で組み立て直すと二重になり、片方だけ直したときに黙って食い違う
   （「ふるまいでさがす」が実際に飛べなくなっていた）。
   飛び先が固定バーの下に隠れないよう、CSS に `scroll-margin-top` を必ず置く。
51. **絞り込んだら件数の表示も変える**（「210項目」のままにしない）。
52. **数え方が画面の見た目と違うときは、数えている数をそのまま出す**
   （「短すぎる」は空白を除いた数なので、いま何文字ぶんとして数えているかを書く）。
53. **目次から飛んだ先を光らせる印を、`className` に足さない。** 飛び先のカードは
   「開く」で className が変わるので、class で印を付けると次の描き直しで React が
   className をまるごと書き直し、**印だけが消える**——運ぶところまでは動くので、
   「飛べているのに光らない」という気づきにくい形になる（実際に踏んだ。目次の
   `☾ 操作の型`・`▷ 別の呼び名`・`✦ 型のまとまり` の全項目がこの状態だった）。
   印は React が書き戻さない属性（`focus.js` の `FLASH_ATTR` ＝ `data-flash`）で付け、
   CSS も `[data-flash]` で受ける。`focus.js` が `classList` を使っていないことと、
   `styles.css` に `[data-flash]` があることをテストが機械チェックする。

54. **保存できなかったことを黙らない。** `storage.js` の `writeRaw` が失敗を飲み込んで
   手元の変数へ逃がしていたので、`save()` は常に true を返し、画面は「保存しました」と
   出したまま何も残らず、再読み込みで全部消えていた（実際に踏んだ）。
   書けたかを返し、`useStore` の `saveFailed` を画面のいちばん上に出す。
55. **上書きで版を消さない**（`mergeCases`）。取り込みであとから直したほうを採るときも、
   こちらの「移り変わり」と「いつ見たか」は残す。**「いつ見たか」は早いほうを残す。**
56. **消したものは何件か戻せる**（`UNDO_KEEP`＝3）。1件だけだと、続けて消したときに
   前のぶんが黙って戻せなくなる。**戻したらそのまま編集中に入る**（入らないと、
   次に保存した時に同じ人が2件できる）。**閉じる口も置く**（画面の下をずっと覆わない）。
57. **編集中の見立てを消しても、書きかけを捨てない**（選んだふるまいと呼び名は残す）。
58. **記録の本文にも上限を持つ**（`records.TEXT_MAX`＝4000）。呼び名40字・メモ400字には
   上限があるのに本文だけ無制限で、1万字以上がそのまま残っていた。**切ったことは画面に出す。**
59. **同じ本文を二重に記録しにくくする。** 記録したことを画面をまたいで覚え（`ui.check`）、
   同じ書き出しの記録が既にあれば知らせる（止めはしない）。
60. **まるごと持ち出せる**（`lib/backup.js`＋設定画面）。人間分析だけ書き出せて記録は
   書き出せない状態だった。**貼った文面が入るので、置き場所に気をつけると必ず書く。**
   受け皿（ErrorBoundary）の「設定から書き出して」は、これがあって初めて本当になる。
61. **下部ナビで戻っただけで、調べる側（言われた側／自分が書いた側）を切り替えない。**
62. **読めない文字にしない。** 記録の型・「よく当たっている型」・出典の「この出典を使う型」・
   調べた結果の型は、押せば型の一覧へ行ける。
63. **どの画面にもさがす欄を置く**（`components/Finder.jsx` が単一の正）。
   **0件のときに黙らない・件数は絞り込み後の数を出す・ラベルを必ず付ける。**
64. **ふだんの言い方でも引ける**（`personSearch.SYNONYMS`）。「せかす」で「急がせる」が出る。
   **新しい主張を作らない**——対応先はデータの中にある語だけで、型の中身は増やさない。
65. **さがした語を消せるようにする**（人間分析の「さがした語を消す」）。
66. **一覧は少しずつ出す**（記録は30件ずつ）。120件で35,000pxを1枚に描いていた。
67. **飛び先の余白は `rem` で持つ**（px 決め打ちだと、文字を大きくしたときにまた隠れる）。
68. **横に長い表は、左端の見出しを残す**（`position: sticky; left: 0`）。
69. **長い文で画面が伸び続けないようにする**（`.quote` に高さの上限）。
70. **入力欄には必ずラベルを付ける**（placeholder は入力すると消えるので、
   何の欄か分からなくなる）。テストが機械チェックする。

71. **地に敷く面（おもて）は、開くたびに変わる**（`components/Figures.jsx`＋`lib/figure.js`）。
   **前に出したものを避けて選ぶ**（ただの乱数だと同じものが続けて出て「変わらない」と見える。
   選んだものは `settings.lastFigure` に覚えておく）。1枚足せば候補が自動で増える。
72. **画像を持ってよいのは地の面だけ**（2026-08-30 ユーザー了承）。飾り（`Ornament.jsx`）は
   今までどおりその場で線を引く。全部で400KBを超えないことをテストが見張る（いまは6枚で76KB）。
73. **絵は手で塗らない。** `tools/draw-figures.js` がキャンバスに描いたものを
   `node tools/make-figures.mjs` で `src/assets/figures/*.webp` へ焼く。
   **画像を直接いじらない**——次に焼いた時に消える。直すときは描く側を直して焼き直す。
74. **絵にも色を入れない。** 描く側で使ってよいのは `rgb(v,v,v)` を組み立てる `g()` だけ。
   焼くときに**1点でも色が付いていたら止める**（出来上がりを機械が見る）。
75. **地の絵に文字を焼き込まない**（`fillText` を書かない・`alt=""` で読み上げに出さない）。
   文字は画面が出すもので、絵は地に徹する。
76. **飾りより読みやすさを優先する**（`.figure-bg` の濃さは 0.6 以下・文字の多い下ほど
   薄く落とす・`pointer-events: none`・読み上げからは外す）。テストが濃さの上限を見張る。

## 構成

```
src/data/tactics.js   型カタログ（単一の正。1件足せば一覧・目次・判定が自動で追従）
src/data/habits.js    つけこまれやすい形（自分の癖。型とは層が違うので混ぜない）
src/data/states.js    長く続いた時に起きること（診断しない・判定の仕掛けを持たない）
src/data/myths.js     当てにならない見抜き方（決めつけを配らないための材料）
src/data/people.js    人間分析（属性ではなくふるまいで分ける）
src/lib/analysis.js   見たふるまい → 近い型（点数を出さない）
src/lib/cases.js      見立ての保存（判定ではなく入力を残す・氏名の欄を持たない）
src/lib/personExport.js 見立てを文章にする（判定を書き足さない）
src/lib/personSearch.js さがす（読み・AND・ハイライト・候補。後読みを使わない）
src/lib/tried.js      やってみた記録（○△✕だけ。点数も割合も作らない）
src/lib/caseTools.js  見立てを読み直す（版を残す・比べる・古さの印・元に戻す）
src/lib/personIO.js   人間分析の持ち出し／取り込み（判定は持ち出さない・形を必ず検める）
src/components/ErrorBoundary.jsx  落ちた時の受け皿（勝手に消さない・ホームへ戻れる）
src/components/Finder.jsx  どの画面でも同じ形のさがす欄（0件でも黙らない）
src/lib/backup.js     まるごと持ち出す／取り込む（記録も入る）
src/data/replies.js   返し方
src/data/sources.js   出典
src/data/toc.js       目次（各データから自動生成。書き写さない）
src/lib/detect.js     語の一致だけで型を当てる（AIを呼ばない・端末内）
src/lib/privacy.js    個人情報を見つけて札に置き換える
src/lib/records.js    記録（氏名・連絡先を持たない）
src/lib/storage.js    localStorage のみ。**ネットワークに触れない**
src/lib/focus.js      目次から飛んだ先へ運ぶ（React に依存しない。印は data-flash）
src/data/glyphs.js    画面に出る印（絵文字を使わない）
src/components/Ornament.jsx  飾りの線画（ウロボロス・六芒星と目・十字）
src/components/Figures.jsx   地に敷く面（開くたびに変わる。焼いた画像を出すだけ）
src/lib/figure.js     どの面を出すか選ぶ（前に出したものを避ける）
tools/draw-figures.js  面の絵（キャンバスに描く。灰色だけ）
tools/make-figures.mjs 面を webp へ焼く（色が入っていたら止める）
src/lib/yomi.js       読み・並び・行分け（全アプリ共通の目次ルール）
```

型を1件足すときは `tactics.js` だけを直す。画面にも判定にも if を足さない。
`cues`（判定用の語）は**その型に特徴的な言い回しだけ**にする——
「です」のようにどこにでも出る語を入れると、何を貼っても全部の型が当たって役に立たなくなる
（テストが機械チェックする）。

## 開発

```bash
npm install
npm run dev      # 開発サーバー
npm test         # node --test（282件）
node tools/make-figures.mjs   # 地の面を焼き直す
npm run build    # dist/ へ
```

テストはリポジトリ直下の `npm test`（`node --test` の再帰探索）からも実行される。

## これはできない

- 相手が本当に何を考えているかは分からない。分かるのは**言葉が一致したかどうか**だけ。
- 言い方を変えられれば当たらない。**読んで嫌な感じがしたことのほうが確か**。
- 身の危険を感じるとき、その場から離れられないときは、このアプリではなく人に頼る。
  緊急のときは110番、急を要しない警察への相談は #9110、契約・勧誘は消費者ホットライン 188、
  家庭内の支配や暴力は DV相談＋。※番号・名称は変わることがあるので公式の案内で確かめる。
